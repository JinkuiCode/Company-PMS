当前项目的 UI 过于传统，需要向现代 B2B SaaS 风格重构。请修改前端代码（包括全局 CSS 变量和 Element Plus 主题配置），严格落实以下视觉规范：
>
> 1. **色彩系统 (Color Palette)**：
>    - 页面主背景色改为极浅灰：`#F4F7FE` 或 `#F8F9FA`。
>    - 侧边栏 (Sidebar) 改为纯白明亮主题，去除深色背景，选中项使用主色调的 10% 透明度作为背景。
>    - 主题色 (Primary) 改为现代靛蓝/紫色调，如：`#6366F1` 或 `#5A57E6`。
> 2. **容器与卡片 (Cards & Containers)**：
>    - 所有内容区块（Card）必须是纯白背景 `#FFFFFF`。
>    - **彻底去除所有卡片的实线边框 (border: none)**。
>    - 统一使用大圆角：`border-radius: 16px`。
>    - 添加柔和的弥散阴影：`box-shadow: 0 4px 20px rgba(0, 0, 0, 0.03)`。
> 3. **表格规范 (Table Design)**：
>    - 彻底去除表格的垂直边框线（Vertical borders）。
>    - 表头背景色改为纯白或极浅灰，字体加粗，颜色变淡（如 `#6B7280`）。
>    - 增加表格行的上下 Padding，使行高更宽松。
> 4. **状态标签 (Badges/Tags)**：
>    - 状态展示（如：进行中、已完结）必须使用胶囊形状（Pill shape, `border-radius: 999px`）。
>    - 采用柔和背景色+深色文字+前置小圆点的设计（例如：浅绿背景 `#E1FCEF` + 深绿文字 `#10B981`）。
> 
> 请先给出全局 CSS/SCSS 变量的修改方案，再以‘项目档案’列表页为例，输出重构后的 Vue 代码。