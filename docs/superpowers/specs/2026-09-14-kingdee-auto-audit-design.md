# 金蝶辅助资料自动提交审核

用户已确认：新增保存后自动提交、审核；只有回查为已审核才标记同步成功；失败明确提示且不重复新增；先测试已审核记录更新，不自动反审核；测试通过后部署，补审生产样本，结束恢复只读；不推送、不合并。

采用显式 Save → Submit → Audit 并按状态跳过已完成环节，不依赖本现场尚未验证的 Save 自动审核参数。使用现有单一 I0001 应用签名、类别 xsxm、按编号查重和内码绑定。查询使用 FDocumentStatus，测试账套已只读核实样本当前 A。A/D/Z 提交、B 审核、C 直接验证完成；未知状态或内码变化停止。每次调用检查 ResponseStatus，成功后回查实际状态和名称。已审核资料只尝试正常保存，不调用反审核。

只读模式同时阻止保存、提交、审核；网络超时或响应格式异常不自动重试，保留不确定结果保护。提交审核失败由原同步与操作日志记录，不新增权限旁路。回退程序不能撤销金蝶已完成的审核。

本批修改限 backend/app/services/kingdee.py、对应测试、SOP/change.md、部署验收材料。复用 codex/pms-release-readiness，不动其他任务文件。测试样本复用 PMS-TEST-20260914-001；生产仅补审 PMS-ACCEPT-20260914-001，新增路径另用测试样本 PMS-AUDIT-TEST-20260914-001 验证。创建新测试样本名称清楚标注验收用途，保留记录。
