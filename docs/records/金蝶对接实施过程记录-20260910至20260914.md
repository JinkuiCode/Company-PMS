> 归档说明：下文保留历史阶段结论，不代表当前运行状态。2026-09-14 生产日常同步已开放，使用 I0001，自动审核及占用提示已验收；当前状态以 SOP 5.0 和正式日常同步启用记录为准。旧分支与待部署文字不作为操作指令。

# 金蝶对接实施过程记录（历史快照）

归档时间：2026-09-14。下文保留各阶段原始状态，不能作为当前待办；当前有效流程以金蝶第三方应用对接SOP.md 为准。

# 金蝶云星空第三方应用对接 SOP（PMS）

版本：1.0 应用配置与只读验收版｜编制日期：2026-09-10｜更新日期：2026-09-14

**当前状态（2026-09-14）：测试账套 test20260910 的第三方应用授权、密钥轮换、本地 ael 登录、受 PMS 权限保护的连接测试，以及指定项目样本的新建和更新均验收通过。仅指定 I0001，验收后已恢复 K3_READ_ONLY=true，测试样本保留。正式 PMS 此前仅发布登录失败提示前端修复；本批未修改正式服务器或正式账套。正式账套/专用用户切换尚待安排，今日开发结束后统一上传合并。**

## 1. 适用范围与通俗说明

本流程适用于企业内网 PMS 后台对接金蝶云星空企业版。现场已查看版本为 `9.1.0.20250807`；其他版本菜单名称可能不同。

原来 PMS 拿用户密码登录金蝶；现在给 PMS 单独登记一个“应用”，由应用 ID、应用密钥和指定金蝶用户共同完成认证。应用密钥可独立维护，换集成账号不用改业务代码。

本次实现使用金蝶官方 Python SDK 生成请求签名。不要将它写成“标准 OAuth AccessToken”或“完全没有 Session”：官方 SDK 内含 Cookie/Session 处理。本方案也不保证免许可证。按现场管理方确认的命名用户规则，测试使用已有 I0001；正式上线使用一个专用账号，许可数量以其角色组分配为准。

管理员负责登记应用；PMS 运行时使用绑定的业务账号，不使用 Administrator。

## 2. 开始前确认

| 项目 | 本次要求 |
|---|---|
| 金蝶地址 | `http://10.10.1.248/k3cloud`，保持现有内网接入 |
| 目标账套 | 必须先确认下表，不能仅凭浏览器当前账套判断 |
| 测试用户 | `I0001`，不改其现有角色组 |
| 应用名称 | `PMS系统对接` |
| 授权用户 | 指定 I0001，避免全用户授权 |
| 业务对象 | 辅助资料 `BOS_ASSISTANTDATA_DETAIL`，销售项目类别 `xsxm` |
| 变更窗口 | 实际启用/换账号前暂停 PMS 同步，等待进行中的请求结束 |
| 回退条件 | 保存原配置的受保护备份；不得把密码、密钥发到聊天或提交 Git |

2026-09-10 通过金蝶数据中心列表核实：

| 数据中心名称 | API 账套 ID | 核对结果 |
|---|---|---|
| 20251210 | `6938df0b584a60` | PMS 原配置 |
| 2023121101 | `65771a661f427f` | Edge 登录账套；用户确认是生产环境 |
| test20260910 | `6aa25ec7d9f511` | 用户新建并指定的测试账套；本次最终目标 |

**名称、编号和 API 账套 ID 不是同一个字段。最终目标为 test20260910。**应用授权和 PMS 配置必须落在同一个目标账套。2023121101 为生产环境；已放弃该账套中未保存的应用草稿，原两项应用保持原状。真实写入不在当前授权范围。

过程记录：用户曾批准先配置生产，官方应用市场已生成生产登记 359667；测试账套随后就绪，因此未将该登记保存/启用到生产账套。不要把“应用市场登记成功”记为“ERP 应用授权成功”，也不要将绑定生产账套的应用信息直接用于测试账套。

## 3. 在金蝶建立第三方应用

1. 用管理员身份进入正确账套，核对页面顶部的数据中心名称。
2. 进入 **系统管理 → 第三方系统登录授权**。先查找 `PMS系统对接`，已存在时核对记录，避免重复创建。
3. 点 **新增**，应用名称填 `PMS系统对接`。应用密钥由系统生成；不要截图或复制到普通文档。
4. 点 **获取应用ID**。本现场弹出“第三方系统登录授权-手工配置”，要求到金蝶应用市场生成应用信息。
5. 按该弹窗提供的官方地址进入金蝶应用市场，核对当前环境类型、环境标识、账套和服务地址。此链接会向 `appmarket.kingdee.com` 传送产品唯一码、内部服务地址和账套 ID，提交表单还包含选择的企业信息；按企业数据传输管理要求取得授权后再继续。若需要登录，由有权限的管理员完成。本次企业为江苏亚电科技股份有限公司（已认证），用途应选择“测试环境”。不要使用其他账套的应用信息。
6. 将应用市场返回的“应用信息”完整填回弹窗，确认输入框确实已有内容后点 **确认**。须核对导入后的完整应用 ID；不能手工编造、截短或借用“导入”等其他应用的 ID。现场自动化曾遇到页面提示复制成功但浏览器剪贴板为空，不能仅凭提示判定填入成功；空值会报“请填写正确的应用信息”。
7. 在 **指定用户登录** 页签，双击空白用户单元格，点选择按钮打开用户列表；按“用户名称”输入 `I0001` 并回车，等待筛选完成。核对用户说明“金奎宇”，勾选该行，确认“已选 1 行”及右侧选中列表均为 I0001，再点 **返回数据**。只在单元格直接输入文本可能未绑定用户，保存会报“请维护允许登录用户信息，再保存”。上方“测试用户”字段不等于授权名单；保持“允许全部用户登录”不勾选。
8. 保存，返回列表查到 `PMS系统对接`，确认未禁用；重新打开检查应用 ID 和指定用户名单已保留。
9. 点 **查看密钥**。现场会提示该敏感操作写入上机操作日志；确认后弹出 **二次认证**，用户名 Administrator、认证方式“命名用户身份”，须管理员本人输入密码并点确定。密码不交给他人、不写入 SOP。读取完整应用密钥后仅存入受保护配置，不能把页面脱敏值当成可用密钥。**本版本现场确认：二次认证成功后，真实密钥在“当前密钥为……”提示框中，原应用密钥字段仍含遮盖字符，不能读取该字段用于配置。应在不回显页面全文、不截图、不记录原文的前提下，将提示框中的密钥直接送入受保护配置，再关闭提示框。**不要点击“重置密钥”或改动其他应用。生成集成密钥不是本实现的必需步骤，本次尚未使用。

页面中“链接有效性”“链接有效时间”“生成测试链接”属于单点登录链接配置，不能据此推断 WebAPI 凭据有效期，也不能用浏览器 SSO 成功替代接口验收。IP 白名单如需开启，应先明确 PMS 后端实际出口 IP；本次不更改既有网络策略。

## 4. 配置 PMS

配置入口：`backend/.env.local`，模板：`backend/.env.kingdee.example`。部署系统环境变量优先于此文件；环境变量存在旧值时，修改文件不会覆盖它。

| 配置项 | 用途 | 本次填写方式 |
|---|---|---|
| K3_AUTH_MODE | 明确选择认证方案 | `app` |
| K3_READ_ONLY | 禁止 PMS 发送保存请求 | 代码默认 `true`；指定业务写入样本获准后才显式设为 `false` |
| K3_URL | 金蝶服务地址 | `http://10.10.1.248/k3cloud` |
| K3_ACCT_ID | API 账套 ID | 填已确认目标账套的 ID |
| K3_USERNAME | 已授权的集成用户 | 测试 `I0001` |
| K3_APP_ID | 完整应用 ID | 从该应用的实际授权信息取得 |
| K3_APP_SECRET | 应用密钥 | 仅写受保护配置，不写入本文 |
| K3_LCID | 语系 | 简体中文 `2052` |
| K3_ORG_NUM | 组织编码 | 默认 `0`；若业务指定组织，按金蝶组织编码设置 |
| K3_PASSWORD | 原密码认证回退凭据 | 应用模式不使用；不需要重新询问个人密码 |

macOS/Linux 将文件权限设为仅所有者读写：

```bash
chmod 600 /Users/jin/Code/PMS/backend/.env.local
```

Windows 部署通过文件 ACL 限制到运行服务的账号和受权管理员。文件不进入 Git、共享文档或故障工单。Git 历史可能仍含旧版硬编码凭据，本次移出源码不等于已清除历史；后续换专用账号时停用原集成凭据。

在实际后端 Python 环境安装依赖：

```bash
cd /Users/jin/Code/PMS/backend
python -m pip install -r requirements.txt
```

`python` 必须是部署服务使用的解释器。本项目 macOS 一键启动使用 Python 3.12 与 `backend/.venv-deps312` 依赖目录，不能只向其他虚拟环境安装。官方 wheel 固定在 `backend/vendor/`，来源和 SHA-256 见该目录 README。

完成配置后重启 PMS 后端，使进程加载新值。本地通过项目的 `start-pms.command` 启动；生产环境通过原服务管理方式重启。不要在未确认实例/部署路径时重启其他服务。

本次配置实际保存在 `/Users/jin/Code/PMS/backend/.env.local`，权限为 `600`；修改前备份为 `.runtime/kingdee-validation/env-before-app.backup`，同为 `600`，不进入 Git。本次只读诊断进程已加载新配置，但这不代表其他工作目录中的服务也已加载。

现场执行 `PMS_STATUS_ONLY=1 ./start-pms.command`，发现 `8000/5174` 服务来自 `.worktrees/pms-server-sync-20260910`，未重启、替换或向该目录复制凭据。切换运行目录可能替换另一项任务的版本，需先确认目标运行版本；不能仅因本目录验证成功就宣称运行中的 PMS 已完成切换。

2026-09-11 并行工作核对：UI 表单框架开发位于 `.worktrees/pms-server-sync-20260910`，实施计划明确不修改后端接口、权限或 ERP 流程；金蝶任务继续在项目根目录维护后端认证、受保护配置和本文。金蝶只读诊断使用独立进程，不占用 8000/5174，也不连接 PMS 业务数据库。当前可并行；最终运行版本切换须在 UI 验收窗口确认后安排。共享文档或变更日志合并时保留双方记录，不以整文件覆盖。

2026-09-12 开发机收口：统一表单与 OA 地址修复已经进入 `master`，金蝶代码迁入 `.worktrees/kingdee-app-auth` 的 `codex/kingdee-app-auth` 分支。新工作树不包含 `.env.local`，不会读取或复制现场密钥；本轮不连接 PMS 服务器，不执行运行实例切换或金蝶真实业务写入。

密钥轮换操作：由管理员在目标账套的“PMS系统对接”中完成“重置密钥”、页面确认与二次认证，并完成保存；本次浏览器工具要求认证凭据变更由用户亲自提交。随后安全读取新密钥并更新受保护配置、只读复验，不把密钥发送到聊天或截图中。本次用户于 2026-09-11 完成重置并确认保存；配置更新及新旧密钥对照验证均已完成。

## 5. 验证顺序与通过标准

### 5.1 本地自动测试

使用测试 Python，在 backend 目录执行：

```bash
PYTHONPATH=. .venv311/bin/python tests/kingdee_app_auth_contract.py
PYTHONPATH=. .venv311/bin/python tests/config_contract.py
PYTHONPATH=. .venv311/bin/python tests/project_archive_lifecycle_contract.py
PYTHONPATH=. .venv311/bin/python tests/project_archive_lifecycle_concurrency_contract.py
PYTHONPATH=. .venv311/bin/python tests/rbac_permission_contract.py
PYTHONPATH=. .venv311/bin/python tests/operation_log_contract.py
```

专项测试使用虚构凭据与内存 HTTP 传输，覆盖应用签名、空配置拒绝、错误响应拒绝、显式密码回退、保存不重试和凭据不输出。退出码为 0 才算该项通过；本地测试通过不代表真实应用已授权。

### 5.2 真实连接与只读验证

在实际加载应用配置的后端环境执行。诊断工具仅在自身进程中使用内存数据库配置，不连接 PMS 业务数据库，不要求本机安装 MSSQL 驱动，也不改变服务进程的数据库配置：

```bash
cd /Users/jin/Code/PMS/backend
python scripts/check_kingdee_connection.py
```

macOS 当前一键启动依赖环境可使用：

```bash
cd /Users/jin/Code/PMS/backend
PYTHONPATH="$PWD/.venv-deps312" /Users/jin/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 scripts/check_kingdee_connection.py
```

通过标准：退出码 0，输出 `auth_mode=app`、正确 `account_id`、`username=I0001`、`read_only=true`、`success=true`。应用模式会实际发送一个 `1=0` 的辅助资料查询；仅实例化 SDK 不算连接成功。

需要再核对一个已有项目时运行 `python scripts/check_kingdee_connection.py --project-code 已核对的项目编号`。该操作只查询，不输出业务名称，不创建资料。应使用不含引号的常规项目编号。输出 `project_found=false` 仅表示该编号未查到，不表示有新增权限。

PMS 的原连接测试入口 `/api/erp/test` 继续要求 `project:archive:sync` 权限。不要为了验收绕过 PMS 权限或直接开放匿名接口。

### 5.3 业务保存验收（另行指定样本后执行）

开始前须确认当前账套确为 test20260910，经批准将 K3_READ_ONLY 改为 false 并重启；只读模式下保存会直接拒绝，不发送金蝶请求。样本验收结束后按阶段要求恢复 true。该开关同时约束 app 与 password 模式。

1. 由业务负责人指定可用的测试项目编号、名称、账套和是否允许新建/更新，记录原值。
2. 通过 PMS 原同步入口同步一次，核对金蝶销售项目类别下的编号、名称、内码和操作用户。
3. 检查 PMS 同步状态、同步日志和统一操作日志；再以同一编号验证更新路径，确认未重复新增。
4. 若保存结果“不确定”，停止重试，先在金蝶核对是否已落库，再按实际结果处理。不能直接把状态改成失败后再次新增，也不能自动删除金蝶资料。
5. 样本清理也须按已确定的处理方式执行，不擅自删除业务数据。

本次已批准到真实连接及只读查询；上述写入验收尚未执行，不得标成通过。

## 6. 正式上线：替换专用账号

1. 准备一个有效的金蝶专用用户，给其所需组织/角色组及辅助资料查询、新增、修改权限。只分配业务需要的权限；许可按现场规则分配一个集成账号。
2. 暂停 PMS 同步，等待进行中请求完成，特别核对不确定状态。
3. 管理员在 `PMS系统对接` 的指定用户名单中加入新用户；先不要撤掉 I0001。
4. 修改受保护配置 `K3_USERNAME` 为新用户。应用仍为同一个时，AppID/AppSecret 通常保持原值；若实际重新建了应用，必须同时替换二者。
5. 重启后端，确认没有遗留的进程/环境变量继续使用旧用户，运行只读检查与指定样本验收。
6. 验收通过后，从该应用指定用户名单移除 I0001，并移除 PMS 保存的旧个人密码。不要删除或禁用个人账号本身。
7. 记录切换时间、操作者、应用、旧/新账号及结果。旧业务单据的创建人不会因为替换配置而自动改变。

若切换失败，在尚未移除旧用户时恢复 `K3_USERNAME=I0001` 并重启；已移除时由管理员恢复原指定授权后再验证。

## 7. 常见故障处理

| 现象 | 核对与处理 |
|---|---|
| 本地测试成功，真实认证失败 | 检查实际环境变量、目标账套、完整应用 ID、真实密钥、应用启用状态和指定用户名；不要只看“测试用户” |
| 只改配置后效果没变 | 检查部署进程使用的源目录、环境变量优先级和是否重启 |
| 查询拒绝 | 核对绑定用户的目标组织与辅助资料查询权限；本次同步会停止，不会继续新增 |
| 查询成功、保存拒绝 | 查询权不代表新增/修改权，检查字段校验及业务权限，不扩大到所有用户/全权限作为通用修复 |
| 签名/时间戳错误 | 确认系统时钟、完整 AppID、配套 AppSecret 和官方 SDK 版本；勿复制脱敏密钥 |
| 网络连接错误 | 确认后端能访问内网金蝶地址；此现象不证明账号或许可证不足 |
| 保存超时/结果不确定 | 先核查金蝶实际数据，禁止自动重试造成重复写入 |
| 更换用户后失败 | 新用户必须同时出现在应用指定名单并具备目标业务权限 |

密钥泄露或轮换由受权管理员处理：暂停同步，在该 PMS 应用中重置密钥，立即同步更新受保护配置并重启验证。不得重置其他应用；轮换后旧密钥不能作为回退凭据。

## 8. 回退原密码方案

1. 停止新同步并核对在途请求结果。
2. 在受保护配置中明确设置 `K3_AUTH_MODE=password`，恢复经核对的原账套、用户和密码配置。
3. 重启后端，运行只读检查；应显示 `auth_mode=password`。应用认证失败不会自动触发此回退。
4. 恢复业务前核对不确定状态和金蝶实际资料。本回退只切换认证，不回滚数据库或撤销已成功的业务保存。
5. 记录原因与结果；是否暂时禁用 PMS 应用由管理员按使用情况决定。不要调用用户级全局注销影响个人其他登录。

## 9. 本次验收记录

| 项目 | 状态/证据 |
|---|---|
| 已获批准 | 2026-09-10 应用认证四项范围、测试 I0001、成功后 SOP |
| 当前代码 | 开发机与 GitHub `master` 均为 `ce0ef8f`；临时开发分支已清理，服务器尚未同步该提交 |
| 官方 SDK | 3.0.0，hash 见 vendor/README.md |
| 本地专项测试 | 2026-09-11：12 项在 Python 3.11/3.12 通过，证据 `.runtime/kingdee-validation/evidence.json`；2026-09-12：迁入最新 `master` 后增至 13 项，在开发机 Python 3.12 通过 |
| 回归测试 | 配置、生命周期、并发、权限、操作日志通过；权限测试环境存在既有 bcrypt 版本提示 |
| 目标账套 | test20260910 / 6aa25ec7d9f511；已确认 Administrator 登录 |
| 应用市场登记 | 用户明确批准数据传输后完成，测试登记编号 359672；用途“测试环境”，账套 ID 一致 |
| 应用授权保存 | 已保存 PMS系统对接，仅指定 I0001；未允许全部用户，页面已转“修改” |
| 凭据取得 | 已使用重置后新密钥更新受保护配置；文件权限 600，不记录密钥原文 |
| 真实应用连接/只读 | 2026-09-11 12:08（UTC+8）轮换后通过：app / 6aa25ec7d9f511 / I0001 / success=true，退出码 0；仅 1=0 查询，证据 `.runtime/kingdee-validation/live-connection-after-rotation.json` |
| 运行中的 PMS | 2026-09-12 用户已断开服务器连接；本轮仅开发机收口，未检查、切换或重启服务器运行实例 |
| 密钥轮换 | 用户已重置并确认保存；旧密钥独立客户端请求失败（退出码 1），新密钥请求成功（退出码 0）；未改 AppID、指定用户或角色组 |
| 业务保存 | 未执行，另行指定样本 |
| 上线专用账号 | 待提供，不属于本次测试账号 |

### 本次实际排错与后续关闭条件

首次请求返回 HTTP 200，但响应内容是“会话信息已丢失，请重新登录”，因此判定失败，未当作业务数据不存在。随后确认二次认证并未把真实密钥填回原输入框；从真实提示框取得正确密钥后，原代码即通过认证，无需修改签名算法、放宽授权用户或退回密码模式。

2026-09-10 读取提示框时，工具结果意外包含当时密钥；首次重置尝试被自动审批阻止，未绕过。2026-09-11 用户亲自完成重置并确认保存后，新密钥以本机一次性输入通道直接更新至受保护配置，未在工具输出中回显。旧密钥新建连接失败、新密钥新建连接成功，本项已关闭。临时导入服务随后关闭，临时脚本已移除；SOP、项目日志及验证证据均不保存密钥原文。

应用配置、密钥轮换与 2026-09-11 的只读接口验收已经完成。代码已进入 GitHub `master`，但整体 PMS 对接启用仍待重新连接服务器后先只读核对运行目录、版本和受保护配置，再按发布与回退说明部署并从 PMS 原有受权入口验证。本轮仅做开发机收口，未重启或覆盖服务器。业务样本写入与正式专用账号切换按第 5.3、6、9.1 节另行安排，尚未通过的项目不能提前勾选。

## 9.1 从测试切换到正式账套

暂停同步并等待在途请求结束，先设 K3_READ_ONLY=true；核对正式账套 ID。在正式账套核对或重新建立专属应用和指定用户，不能假设数据库复制后应用信息仍可跨账套使用。同步替换账套、AppID、AppSecret、用户及组织配置，重启后执行只读验收。正式写入另行安排样本和窗口，通过后再关闭只读模式。回退时恢复整组原配置，不能只换用户名或账套单个字段。

## 10. 官方参考

- [金蝶官方 SDK 与第三方系统登录授权说明](https://open.kingdee.com/K3Cloud/Open/ApiCenterReportDetail.aspx)
- [用户提供的星空单点登录整体介绍](https://vip.kingdee.com/knowledge/597886665582790144?specialId=503171243265122048&productLineId=1&isKnowledge=2&lang=zh-CN)
- 应用 ID 生成入口以目标账套“获取应用ID”弹窗提供的金蝶官方链接为准；不要把含环境标识或测试签名的完整链接写入 SOP。


## 12. 2026-09-14 登录故障修复与前端正式发布

本节覆盖第 11 节之后用户追加批准的操作，前文“未连接服务器、未调整本地用户”为此前阶段记录。

- 已查明：本地原有 admin，而正式启用账号为 ael；后端运行正常，普通登录页却漏掉登录接口 401 的错误提示。
- 已完成：备份本地数据库后，仅同步本地登录名与服务器 ael 的密码校验值，保留本地用户 ID、角色、业务数据，并记录脱敏操作日志。未重置服务器密码，临时凭据文件和接收服务已清理。
- 已发布：仅正式 PMS 前端登录失败提示修复，未部署金蝶后端或测试配置。正式前端与本地修复预览均实测显示“用户名或密码错误”，健康检查正常。备份目录 C:\backup\PMS-login-feedback-20260914-084550；详情见 `docs/releases/PMS登录修复发布记录-20260914.md`（当前位于发布准备工作树）。
- 源码状态：修复在 codex/pms-release-readiness，未合并 master 或推送；正式服务器 HEAD 仍为 8108a7e，叠加本次前端补丁。本地修复预览为 http://127.0.0.1:5176/login，代理现有本地后端；默认 5174 尚为 master 页面。
- 仍待验收：用户自行输入服务器 ael 密码完成本地成功登录；之后继续原有受权金蝶连接测试。金蝶业务样本写入、正式账套/专用用户切换未实施，不能把此次前端发布当作金蝶正式上线完成。

排查登录时先区分三种结果：提示“用户名或密码错误”代表请求到达后端但校验失败；服务连接/网络提示应检查后端和代理；页面没有提示不能直接认定密码错误，应检查登录页面对接口错误的处理。不得为排错绕过登录、重置正式用户或扩大权限。


## 13. 2026-09-14 登录后受权连接验收完成

- 用户确认使用服务器 ael 密码成功登录本地 PMS；浏览器已进入 5176 的项目档案页面，后端 /api/auth/me 返回 200。
- 沿用该登录会话及原有请求客户端，从同源临时验收页调用 POST /api/erp/test，经过 project:archive:sync 权限校验，HTTP 200 且业务结果 success=true、message=金蝶连接测试成功。
- 本次后端仍为项目根目录 master@ce0ef8f、端口 8000；应用签名认证、测试账套 test20260910 / 6aa25ec7d9f511、指定用户 I0001，保持只读。连接测试仅执行 1=0 空结果查询，无 Save 或项目同步。
- 临时验收页已关闭、文件已移除；未读取或导出登录令牌。证据更新至 .runtime/kingdee-validation/local-runtime-20260914.json。
- 第 11、12 节所列的本地成功登录及登录后受权连接测试待办已关闭；第 9 节有关早期工作树和待验收状态为历史记录。测试环境认证阶段已验收完成，不代表金蝶业务同步或正式上线完成。
- 后续仍需明确业务样本和字段映射后安排写入验收；正式账套/专用用户替换按第 9.1 节执行。用户要求今日开发结束后再统一上传合并，本次不推送、不合并。


## 14. 业务同步预检与待批准范围（2026-09-14）

只读预检确认金蝶测试账套存在销售项目类别 xsxm，查询字段 FId.FNumber 可用；拟用编号 PMS-TEST-20260914-001 在该类别下和本地 PMS 均未查到。本次预检不证明拥有新增或修改权限。

现有字段映射为：PMS 项目编号→FNumber，项目名称→FDataValue，描述固定为“PMS 项目档案同步”→FDescription；类别固定 xsxm→FId.FNumber。客户、负责人、产品类别、设备系列、日期等目前不传金蝶。现有更新只修改名称与描述，不修改编号。

发现待修复问题：query_assistant_data 接受 category_code，但 FilterString 只使用项目编号且直接拼接编号。试写前应限定类别和编号、正确处理引号，并在同类别同编号返回多条时停止，而不是取首条更新。保留原认证、权限、业务字段与 API 路径；用类别隔离、引号、重复结果和错误响应回归验证。

拟定样本（尚未批准或创建）：PMS-TEST-20260914-001，名称“PMS金蝶对接验收-20260914”，只用于测试账套销售项目。首次从本地 PMS 新建并同步，随后仅将名称改为“PMS金蝶对接验收-20260914-更新验证”并再同步，核对同一 FEntryID、记录数不增加、PMS 状态和日志一致。测试结束保留样本，不自动删除。

实施需明确批准查询修复及上述样本新建/更新；只在验收窗口将本地 K3_READ_ONLY=false 并重启本地后端，测试后立即恢复 true。正式服务器、正式账套不参与；返回结果不确定则停止自动重试，先只读核查。代码回退可还原本批补丁，但不能撤销已经写入的金蝶记录。


## 15. 指定样本新建与更新验收（已完成，2026-09-14）

### 15.1 批准范围与查询修复

用户明确批准第 14 节方案：查询类别隔离、引号处理、重复检测，并在测试账套新建及更新指定样本。现已在 codex/pms-release-readiness 修复：以 FId.FNumber 与 FNumber 联合筛选，类别和编号中的单引号倍写；TopRowCount 与 Limit 均为 2，返回多条时失败关闭，不任取首条更新。API、认证与 RBAC 保持原样。

### 15.2 实际操作与结果

| 项目 | 已验证结果 |
|---|---|
| 金蝶账套 | test20260910 / 6aa25ec7d9f511 |
| 目标 | BOS_ASSISTANTDATA_DETAIL，销售项目 xsxm |
| PMS 登录身份 | ael，本地用户 id=1，显示名仍为系统管理员；原角色保留 |
| 金蝶执行身份 | 应用指定用户 I0001 |
| PMS 测试档案 | id=2，PMS-TEST-20260914-001；本地产品类别 Bench（不传金蝶） |
| 新建名称 | PMS金蝶对接验收-20260914 |
| 更新后名称 | PMS金蝶对接验收-20260914-更新验证 |
| 金蝶内码 | 6aa74dc6db7b77；新建后与更新后完全一致 |
| 新建前 | 只读查询该类别、编号，未找到记录 |
| 新建操作 | 本地 PMS“新增档案”保存，再点该样本“同步”；金蝶只读回查核实编号、名称、内码 |
| 更新操作 | 打开该档案编辑，仅改名称，点“保存并同步 ERP”；回查同一内码，未重复新增 |
| PMS 同步结果 | erp_synced=1、erp_sync_status=success、erp_sync_by=1 |
| 同步日志 | id=1 create/success，id=2 update/success |
| 统一操作日志 | id=49 创建、50 首次同步、51 修改、52 再次同步，全部成功 |
| 样本处理 | 保留在本地 PMS 与测试账套，不删除、不转移至正式账套 |

映射保持：项目编号→FNumber，项目名称→FDataValue，描述固定“PMS 项目档案同步”→FDescription，类别固定 xsxm→FId.FNumber。更新仅提交名称、描述，不修改编号；客户、负责人、产品类别、设备系列、序列号和计划日期当前不传金蝶。

### 15.3 写入窗口与只读恢复

本批通过临时本地验收启动器加载修复工作树，凭据仍从项目根目录 backend/.env.local 读取，未复制或输出。启动器只允许测试账套、app 模式及 I0001；写入窗口带 10 分钟自动关闭兜底。真实新建/更新完成后主动结束写入进程，重新以 read_only=True 启动，未等待自动到期。受保护配置本身始终保留 K3_READ_ONLY=true。

当前本地 8000 后端运行源为 .worktrees/pms-release-readiness/backend（本次验收进程 PID 4688），由 .runtime/kingdee-validation/run-acceptance-backend.py 的不带 --write 模式启动。5176 为同工作树前端，5174 原 master 前端仍代理该后端。默认 start-pms.command 的记录仍指向根目录，不能把它当作本次临时运行来源；今天合并收口时，应统一恢复正式本地启动来源、核对配置并重新验证健康状态。不要把本次临时启动器当作服务器发布脚本。

### 15.4 验证及证据

4 项查询范围契约先失败后通过；13 项应用认证契约、项目档案生命周期、并发及操作日志契约通过。独立代码审查未发现阻碍验收的问题。内存测试验证类别隔离、含引号编号与重复拒绝，真实测试账套验证正常新建/更新路径；未为测试引号或重复场景额外写入金蝶数据。

证据位于 .runtime/kingdee-validation/sample-before-create.json、sample-after-create.json、sample-after-update.json 和 sample-acceptance-20260914.json。证据包含指定样本和结果，不包含密码、密钥、签名或登录令牌。当前本地 /api/health 经 5176 返回 200/ok。

### 15.5 后续正式上线

本次完成测试环境业务闭环，未执行正式账套切换。继续按第 9.1 节核对正式账套专属应用、唯一指定用户和业务权限。待用户提供专用账号后替换 I0001，先只读验证，再安排正式样本和上线窗口。一个指定用户符合当前“最多占用一个账号许可位置”的目标；真实许可占用仍按该金蝶环境的角色授权与许可规则核查，不宣称应用签名自动免许可。
