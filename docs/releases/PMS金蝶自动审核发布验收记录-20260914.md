# PMS 金蝶自动审核发布与验收记录

日期：2026-09-14。当前状态：程序已部署；测试账套与生产补审均通过；已恢复只读。

## 已实现行为

保存后依据状态自动提交、审核，回查目标内码、编号、名称及已审核 C 后才将 PMS 同步标记为成功。Save 返回内码与后续查询绑定；任何内码漂移、重复数据、未知状态立即停止。已审核资料正常更新，不自动反审核；业务拒绝记录失败，网络结果不确定保留待核对状态，不自动重试。

## 验证

- 自动审核契约 15 项通过，包含 A→B→C、已审核不重复审核、只读不联网、业务拒绝、超时不重试、内码漂移和仅保存不能标记成功。
- 应用认证 13 项、查询范围 4 项、档案生命周期、并发及操作日志契约通过。服务器重新执行自动审核、认证、查询和并发检查，全部退出 0。
- 测试账套 test20260910 / 6aa25ec7d9f511，I0001：既有样本 A→C；已审核样本更新内码不变并保持 C；新建自动审核样本 001、002 均为 C。最终版本 002 内码为 6aa78557db9f17。
- 首次创建 002 时与 001 同名，被金蝶明确拒绝且未创建；改用唯一名称并只读查重后成功。没有对不确定结果自动重试。

## 正式部署

- 服务器 10.10.1.228 / C:\PMS，生产账套 2023121101 / 65771a661f427f，应用指定 I0001。
- 程序包 SHA256：7c1fdf3953f6084ff5bd1a8207c4c43f836c5cd14d33ce4d911df1a5cf0bff98。
- 仅替换 backend/app/services/kingdee.py、backend/tests/kingdee_auto_audit_contract.py、backend/tests/project_archive_lifecycle_concurrency_contract.py。
- 备份 C:\backup\PMS-auto-audit-20260914-133359；旧 PID 260 → 新程序普通 PID 5348 → 限定样本 PID 4856 → 首轮恢复普通只读 PID 6444。
- 持久配置始终 K3_READ_ONLY=true，部署及两次切换均比较配置哈希未变。未改 OA、RBAC、应用用户或许可证配置。

## 生产样本结果与阻塞

样本 PMS-ACCEPT-20260914-001，PMS ID 4，金蝶内码 6aa7719bdb9bfc，名称 PMS对接验收样本-更新验证。

13:36 经正式 PMS 已登录用户正常“同步”入口执行，金蝶 Save 明确返回：I0001 对“辅助资料-PMS-ACCEPT-20260914-001-修改”业务操作冲突。受控窗口计数 Save=1、Submit=0、Audit=0。13:37 只读回查：仍为 A，名称及内码未变，待处理同步数量为 0。新增同步日志 33 update/failed、统一操作日志 74 sync/failed，失败未误标成功。

已正常退出临时窗口并恢复普通只读服务。再次核对实际端口/PID、普通 uvicorn 入口、临时进程已停止、nginx 健康；独立探针证明 Save、Submit、Audit 及审核流程均在发送请求前被只读开关拒绝。

用户随后确认已退出占用。重新只读预检样本，保留首轮失败证据，在新窗口 PID 3208 中通过 PMS 正常同步入口执行 Save/Submit/Audit 各一次。14:00:16 回查状态 C，名称和内码不变，PMS success；新增同步日志 34 update/success、操作日志 75 sync/success。验收脚本与第二轮 before 比较，确认新增记录，未将历史成功日志误作本轮成功。

结束后恢复普通只读 PID 6576，窗口 PID 3208 已停止；配置哈希未变、nginx 健康，保存/提交/审核联网前拒绝验证通过。全程未强制解锁、反审核或处理其他生产记录。

## 回退及交接

如需程序回退，停止同步并保持只读，从上述备份恢复已有文件，移除备份清单中原先不存在的新增测试文件，以普通入口启动并核对 PID/健康。代码回退不会撤销金蝶已有保存或审核行为，不应删除或反审核样本来模拟代码回退。

SOP 4.0 已更新最终补审成功结果，同时保留首轮占用失败的过程。新增占用提示优化方案待用户确认，不列为已完成。代码位于 codex/pms-release-readiness，仍未提交本批改动、未推送、未创建 PR、未合并；服务器 HEAD 8108a7e 加本批文件补丁。今日合并及本地标准启动来源收口另行执行。

脱敏证据：.runtime/kingdee-validation/auto-audit/production-evidence.json；测试证据位于同目录 *idguard.json。服务器目录 C:\PMS-kingdee-auto-audit-20260914 保存部署、只读回查及窗口计数。
