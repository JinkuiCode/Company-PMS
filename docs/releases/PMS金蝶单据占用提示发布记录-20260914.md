# PMS 金蝶单据占用提示发布记录

日期：2026-09-14；用户批准明确提示“在金蝶端退出当前单据后再次点击同步”。

## 实际行为

金蝶明确返回单据占用，或本现场已验证的“使用业务单据／业务操作／冲突／请稍候”错误结构时，PMS 提示：

> 金蝶当前单据被占用。请在金蝶端退出当前单据后，再次点击 PMS 的“同步”。如由其他用户占用，请联系该用户退出。

提示附带已脱敏的金蝶原始业务原因，因此保留返回中的占用人；未返回占用人时不编造。保存、提交、审核均适用，提交/审核失败仍明确说明保存已经完成。名称重复、权限不足、一般字段或单据状态冲突保留原错误，不误导为占用。

单条同步及保存并同步沿用已有错误展示；批量同步在原计数摘要后补充占用记录的指引，保留原 API 成功标记、计数及 errors 契约。资料的同步异常字段、同步日志及统一操作日志继续保存失败原因。不会自动重试、强制解锁或反审核。

## 验证与部署

- 本地占用契约先失败后通过，最终 5 项，覆盖保存/提交/审核、占用人、无占用人、脱敏、非占用错误及批量提示；审核 15 项与认证回归通过。独立静态复核通过。
- 正式服务器运行占用 5 项、自动审核 15 项、认证 13 项、查询 4 项及并发契约，均通过；仅模拟失败响应验证提示，不再制造生产占用或重复业务写入。
- 部署文件：backend/app/services/kingdee.py、backend/tests/kingdee_occupancy_contract.py。现有前端已展示后端 message，本次不修改前端文件。
- 正式包 SHA256：9bcca7edf893a5d550b9bd663448322004ab73e8b04869b87398670130f6347f。
- 备份 C:\backup\PMS-occupancy-20260914-140712；后端 PID 6576 → 4796。持久配置哈希未变，K3_READ_ONLY=true；新 PID 普通 uvicorn 入口及 nginx 健康通过，Save/Submit/Audit 联网前只读拒绝通过。
- 第一次发布尝试在文件路径预检处中止，未替换程序或停止服务；修正发布脚本日期路径、重新打包校验后成功。

若回退，保持只读并恢复上述备份的金蝶服务文件，删除原先不存在的新增占用测试文件，正常重启并检查健康。回退提示不会撤销金蝶资料的已审核状态。

生产样本 PMS-ACCEPT-20260914-001 已于本次提示发布前完成补审，内码 6aa7719bdb9bfc、状态 C，成功日志 34/75。本次提示发布不修改业务数据。

代码在 codex/pms-release-readiness，未提交本批、未推送、未创建 PR、未合并。SOP 更新为 4.1。证据：.runtime/kingdee-validation/occupancy-release/production-evidence.json。
