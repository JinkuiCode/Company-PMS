# PMS 金蝶生产样本验收记录

日期：2026-09-14（UTC+8）。用户明确批准仅进行一个样本的新增和更新，验收后恢复只读并保留样本。

### 9.1 生产样本新增与更新验收（2026-09-14）

| 项目 | 实际结果 |
|---|---|
| 目标账套及用户 | 2023121101 / 65771a661f427f；I0001 |
| 样本编号 / PMS ID | PMS-ACCEPT-20260914-001 / 4 |
| PMS 辅助标识 | 客户：内部验收（非客户）；产品类别：Bench；其他非必填项未编造 |
| 初次名称 | PMS对接验收样本 |
| 更新后名称 | PMS对接验收样本-更新验证 |
| 金蝶类别 / 内码 | xsxm / 6aa7719bdb9bfc；新增与更新后的内码相同 |
| 首次核对 | 12:01:22，新建成功、记录唯一 |
| 更新核对 | 12:02:23，名称一致、内码不变、记录唯一 |
| PMS 同步日志 | 31 create/success；32 update/success |
| 统一操作日志 | 70 create、71 sync、72 update、73 sync，全部 success |
| 验收结束 | 样本保留；普通只读服务 PID 260；临时验收 PID 2136 已停止 |

本次没有将持久配置的只读开关改为 false。短时验收使用受控临时运行入口：只允许上述编号、xsxm 类别和批准的两个名称按顺序执行，最多两次 Save；每次保存前核对实际样本内码，并在十分钟期限前后校验，超时不再发送保存请求。其他编号始终拒绝写入。业务保存仍由正常 PMS 登录、权限、字段校验、同步服务和日志流程处理。

结束后退出临时入口，重新以普通 uvicorn 入口启动 C:\PMS\backend，核对进程占用、配置哈希未变及 nginx 健康。独立检查证实 Save 在联网前被只读开关拒绝；没有为验证只读再写一次生产数据。两次 Save 均已消耗，不能通过重新启动临时脚本自动重试。该临时脚本只属于本次验收材料，后续验收须重新查重并明确样本范围。

生产样本写入验收已完成，不代表长期开放所有项目同步；目前保持只读，后续常态写入启用和专用账号替换单独安排。

执行前 PMS 与金蝶均不存在该样本，pending 同步数量为 0。使用正式 PMS 正常界面创建、同步、修改名称并同步，独立只读程序查询 SQL Server 与金蝶验证结果。所有验收断言通过，正常服务恢复后配置未变、监听归属与 nginx 健康均通过。

业务数据实际变更：PMS 新增一个档案，客户标记“内部验收（非客户）”、类别 Bench，名称修改一次；金蝶销售项目辅助资料新增一条并更新名称一次。未删除样本，未操作其他项目的同步。

没有推送或合并。使用分支 codex/pms-release-readiness 的已验收金蝶补丁；服务器 HEAD 仍为 8108a7e 加发布补丁。临时文件传输和证据接收服务已结束。

证据路径：服务器 C:\PMS-kingdee-20260914\sample-acceptance\acceptance-result.json；本地 .runtime/kingdee-validation/production-package/sample-acceptance/production-sample-acceptance-20260914.json。
