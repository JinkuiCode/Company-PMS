# PMS 主分支合并与运行版本交接

日期：2026-09-14。用户在推送、合并范围明确后确认继续。

后续执行结果：用户进一步确认统一 Mac、服务器和 GitHub，运行切换已完成，见[三端版本统一记录](PMS三端版本统一记录-20260914.md)。本文保留合并时的历史快照。

## 已完成的代码交付

- 发布分支：`codex/pms-release-readiness`，已推送。
- 主分支：`master` 从 `ce0ef8f` 快进至 `309d760e3c4154885d0dd033d2cb5c8c7f9e174c`，已推送，无冲突；直接合并，没有创建 PR。
- 程序版本：`327c2d8`；`309d760` 为交付文档提交。本次收口文档另作提交，不改变程序或候选包。
- 合并前根目录的 10 个文档已逐个确认与发布分支内容一致，另存 `backup/pre-release-merge-docs-20260914` stash；旧 stash 保留。没有覆盖独有改动。
- 验证：合并后文件树与发布分支完全相同，Git 远端主分支与发布分支均回查为 `309d760`。程序未发生新变化，复用该版本已有业务契约、前端契约、构建和候选包校验证据。

候选包及 SHA256 见[候选包验收记录](PMS发布工具修复与候选包验收-20260914.md)。本轮没有重启或部署生产服务，也没有执行金蝶业务写入。

## 运行状态与剩余工作

2026-09-14 15:23:50 本地启动器状态检查：

| 项目 | 实际结果 |
|---|---|
| 根目录源码 | master / 309d760 |
| 8000 后端 | PID 4688，来源为临时工作区 `.worktrees/pms-release-readiness/backend`；启动器报告来源不匹配或未接管 |
| 5174 前端 | PID 2176，来源为根目录 frontend；启动器报告状态不匹配或未接管 |
| 启动器默认数据库 | `.runtime/data/pms-dev.db`，不能据此推断临时后端正在使用的数据库 |

临时工作区仍被后端使用，候选包也位于其中，因此保留工作区和分支。源码已合并不等于运行进程已经切换。

生产沿用此前已验收的部署：生产账套 `2023121101 / 65771a661f427f`，用户 `I0001`，日常同步已开启。14:35 启用记录中的普通后端为 PID 3100；这是此前现场证据，本轮没有重新检查生产进程。旧服务器 Git HEAD 8108a7e 加已发布补丁的状态，不能作为整包版本一致的证明。

## 下一阶段的具体范围

1. 本地：核实临时后端实际数据库和受保护配置来源，备份并保留现有数据；在短暂停机窗口退出对应本地进程，从根目录标准启动器启动，确认端口、源目录、数据库、健康及只读金蝶连接。
2. 生产：先只读核实运行文件、配置、数据库及备份，再确定候选包替换范围和回滚步骤；正式部署、重启按明确的切换方案确认后执行。保留已批准的生产账套、I0001 和日常同步开关，不因发布工具默认值改回只读。
3. 运行来源统一且确认没有任务再使用后，再清理临时工作区。专用金蝶账号在用户提供用户名后另行替换，凭据不进入聊天或文档。

本轮仅完成代码推送、合并和交接记录，未把上述运行切换列为已完成。
