> 历史报告：仅描述 2026-09-12 的 ce0ef8f 候选包，不含后续新增功能，不得用作当前最终发布包。当前生产已开放日常同步，后续发布以新的确定提交、校验清单和 SOP 为准。

# PMS 开发机发布候选检查报告

## 结论

- 发布基线：GitHub `master` 提交 `ce0ef8fd0e6a5751cca99c93835051816a1f3890`。
- 开发机后端 18 项既有契约、前端 22 项契约和生产构建通过。
- 新增服务器发布包契约，验证发布内容完整并排除敏感配置、数据库、测试和开发依赖。
- 已生成可校验发布候选包；尚未连接、修改或重启 `10.10.1.228`。
- 金蝶仍要求 `K3_READ_ONLY=true`，本轮没有执行真实连接或业务保存。

## 发布文件

- ZIP：`/Users/jin/Code/PMS/.runtime/releases/pms-server-ce0ef8fd-20260912.zip`
- SHA-256 文件：`/Users/jin/Code/PMS/.runtime/releases/pms-server-ce0ef8fd-20260912.zip.sha256`
- ZIP SHA-256：`cea19cc4e94337b88723a87513d8f6ecbf390e58e3284328eb8ee4fb8127e9f0`
- ZIP 大小：约 5.5 MB。

压缩包包含后端程序、固定依赖清单、后端维护脚本、金蝶官方 SDK、前端生产文件、内部文件校验清单和服务器发布/回退说明。

压缩包不包含 `.env.local`、数据库、日志、测试文件、`node_modules`、`.git`、缓存或本地运行目录。

## 验证证据

1. 后端：使用项目 Python 3.11 虚拟环境并设置后端模块路径，`backend/tests/*.py` 共 18 项全部通过。
2. 前端：`frontend/tests/*.test.mjs` 共 22 项全部通过。
3. 生产构建：`npm run build` 通过，`vue-tsc` 类型检查完成，Vite 转换 2322 个模块。
4. 发布包：`backend/tests/server_release_contract.py` 先因脚本缺失按预期失败；实现后通过，并再次验证包内发布说明。
5. ZIP 完整性：外部 SHA-256 文件与压缩包一致，包内禁止路径扫描无结果。

前端构建仍有既有的大分块提示，不影响本次构建成功；RBAC 测试仍输出既有 `passlib/bcrypt` 版本探测提示，测试结果通过。

## 根目录备份审查

保留备份：`stash@{0}: backup/pre-consolidation-root-20260912`。

- 备份共涉及 26 个路径。
- 其中 13 个文件与当前 `master` 内容完全一致。
- 其余程序文件是统一表单、正式 OA 地址或金蝶安全补强之前的旧版本，没有发现需要恢复到当前 `master` 的独有程序实现。
- `项目档案需修改.png` 的删除状态仍只保存在备份中，未擅自恢复或永久删除。
- 备份不包含被 Git 忽略的真实 `.env.local`。

该备份暂不删除，用作最后一道人工恢复保障；不得整体应用到当前 `master`，否则会覆盖已验证的新实现。

## 服务器后续动作

恢复连接后，先按 `docs/PMS服务器发布与回退说明.md` 执行只读核对，确认服务器实际运行目录、版本、进程、Python 环境、Nginx 目录、数据库和受保护配置，再制定现场部署步骤。当前发布候选包可作为同步来源，但“开发机验证通过”不等于“服务器部署完成”。
