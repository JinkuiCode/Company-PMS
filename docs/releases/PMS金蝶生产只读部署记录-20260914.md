# PMS 金蝶生产只读部署记录

日期：2026-09-14，验证时间 11:29（UTC+8）。

- 服务器：10.10.1.228，程序 C:\PMS；Git HEAD 8108a7e，未提交运行补丁。
- 目标：2023121101 / 65771a661f427f，正式应用登记 360016，仅 I0001。
- 配置：app 请求签名；K3_READ_ONLY=true；DB、OA、PMS 登录配置保留。
- 后端：旧 PID 5488 停止，新 PID 4104 监听 127.0.0.1:8000；后端与 nginx /api/health 均 status=ok。
- 备份：C:\backup\PMS-kingdee-20260914-112714，含本批覆盖文件与受保护配置。
- 验证：真实生产认证 success=true；查询回归 4 项及认证回归 13 项通过；全部部署文件哈希一致；正式 /login 正常呈现，用户已成功进入仪表盘；当前登录身份调用 POST /api/erp/test 返回 HTTP 200、success=true、“金蝶连接测试成功”。
- 依赖：官方 SDK 3.0.0；新增 requests 2.34.2、urllib3 2.7.0、charset-normalizer 3.5.1。服务器 PyPI DNS 失败，改用已校验的内网离线安装，未升级已有 idna/certifi。
- 传输结束后关闭两个临时文件服务，删除开发机临时生产凭据 JSON。服务器正式凭据保留在受保护配置，不在文档中记录。
- 验收边界：本次正式应用认证、后端部署、PMS 登录后只读连接验收完成；正式 OA 域名入口免密进入 PMS 已通过，页面显示 OA 用户 A000645；初始部署保持只读；后续获批单样本新增/更新已完成，现已恢复只读，详见 [生产样本验收记录](PMS金蝶生产样本验收记录-20260914.md)。临时验收 HTML 在检查后删除。
- Git：未推送、未合并；待用户确认今日开发完成后统一处理。

程序包 SHA256：`fec90a1a057a0a8c92f536578ca43ea7321e7efa7ec10854bd9784ba237b5c7c`。
部署脚本 SHA256：`c1f413e123699be7f95f723285942390907e3ea14082f8eddf6d47bd6c93cbd8`。

回退必须先停止本次实际进程并确认端口释放，恢复备份程序和原配置，重新启动并核对新进程、端口及健康结果。不得仅凭启动命令已执行宣称恢复完成；回退代码也不能撤销金蝶业务数据。

后续状态：生产样本验收结束，当前普通后端 PID 260，临时验收 PID 2136 已停止；上述 PID 4104 为初次部署时快照。
