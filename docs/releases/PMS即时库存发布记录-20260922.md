# PMS即时库存发布记录

## 发布准备

- 用户批准继续部署，并确认已备份230上的PMS数据库。
- 目标服务器：10.10.1.228，运行目录 `C:\PMS`；数据库 `10.10.1.230 / PMS`。
- 现场基线：源码 `8db129908ea0d08efa8dba41be4b2ec837f44357`，工作区干净；数据库版本 `2026-09-21-02`。
- 本批目标提交：`9465b2f7`，分支 `codex/pms-inventory-report`；已从固定提交构建，类型检查和生产构建通过。未推送GitHub或合并master。
- 部署文件：`C:\PMS\.runtime\releases\inventory-20260922`。组合包SHA256：`1bc652d0a65223edac038e8d4f7e9db7e3291a3a75ab3c32bbe0a4c7d0bcc9ff`，服务器下载后校验通过。
- 程序和配置备份：`C:\PMS\.runtime\release-history\inventory-20260922-180441`。目录限制访问，受保护配置保留原ACL。
- 本次只更新PMS，不重启SQL Server、OA或服务器；授权只新增库存菜单及首次管理员菜单权限，不修改角色组织范围或业务记录。

## 发布结果

用户输入独立升级凭据后，服务器返回 `DATABASE_BACKUP_VERIFIED`、`VERIFY_OK`、`DEPLOYMENT_OK`、`DEPLOY_EXIT=0`。凭据未记录。

- 已验证数据库备份：`E:\PMS-bkup\PMS20260922.bak`，完成时间2026-09-22 18:02:45。
- 实际部署源码：`9465b2f7eb9389d52fb586d8f7dd8d0c080b9ee5`；数据库版本升级至 `2026-09-22-01`。
- 原业务数据摘要一致，项目档案保持940条，现有角色组织范围未变。新增库存菜单及首次管理员权限按计划安装。
- 服务器发布工具核对：源码/静态文件哈希、受保护配置、健康接口、匿名访问保护、金蝶专用只读查询均通过。只重启PMS自身。
- Mac外部核对：健康接口返回ok；首页与库存页面JS逐字节匹配发布包；库存查询、元数据及导出接口匿名请求均返回403。公网路径 `/openapi.json` 由前端兜底，API目录已由服务器内网8000端口验证，未修改代理配置。
- 用户验收地址：`http://10.10.1.228/reports/inventory`。业务数量口径、实际登录角色及完整页面交互仍由用户验收，不把部署检查等同业务验收。
- GitHub尚未推送、master尚未合并；开发机实际数据库仍待单独对齐，不宣称三端版本一致。
