# PMS 登录修复发布记录

时间：2026-09-14（UTC+8）。范围经用户明确批准：本地账号密码与服务器一致，补上登录失败提示并更新正式环境。

## 已完成

1. 本地稳定数据库用户 id=1 从 admin 调整为 ael，与服务器启用的 ael 使用同一密码校验值；本地用户 ID、角色关系、业务数据保留，旧记住登录令牌已撤销。未修改服务器账号或密码。
2. 前端补上 /auth/login 401 提示；/auth/me 401、服务器异常仍由统一拦截器处理。接口、OA 与权限不变。
3. 将同一测试通过的构建部署至 C:\PMS\frontend\dist；源 Login.vue 和新增测试同步到服务器。部署前校验服务器 HEAD=8108a7e 和原 Login.vue（Windows CRLF）内容，完整备份旧 dist，逐文件校验发布包。旧哈希资源保留，首页以文件替换方式切换。

## 验收证据

- 登录反馈、登录安全、样式、标准列表、系统 UI 契约与 npm run build 全部通过；独立代码复查通过。
- 本地 5176 与正式 10.10.1.228 浏览器使用不存在的测试账号，均显示一次“用户名或密码错误”，未进入业务页；测试输入已清除。
- 正式和本地 /api/health 均 HTTP 200、status=ok。
- 本地 credential_matches_server=true、roles_preserved=true；用户已于 2026-09-14 自行输入服务器密码成功登录本地 PMS，并核实已登录项目档案页和 /api/auth/me 200。
- 发布包 SHA-256：6041a10ec6271e3b5233ec0e839fa51440f8e5db507b583bbfbf1996cf9182cd。
- 正式首页 SHA-256：29cbb21f1d704868af758959704d1ec9e6ca2980c40bf5b7fb634443286b70ff；主要静态资源通过 HTTP 取回与构建一致。
- 本地证据：.runtime/login-fix/http-verification.json；发布清单位于发布包 release-manifest.json。证据和本文均不包含密码或密码校验值。

## 备份与回退

正式备份：C:\backup\PMS-login-feedback-20260914-084550。恢复其中 dist/index.html 到 C:\PMS\frontend\dist\index.html 即可恢复旧前端入口（旧资源仍保留）；恢复 Login.vue 到原源文件位置。新增回归测试可以保留；如要求源码完全回退，移除本次新增的 login-feedback-contract.test.mjs。回退后检查首页、登录、/api/health，无需后端重启。

本地数据库备份：.runtime/login-fix/pms-dev-before-account-sync-20260914-084153.db，权限 600。若需回退，应先停止本地后端并备份当前数据库，再恢复；整库恢复会覆盖备份之后本地新增数据，应先确认变更窗口。不能将本地库或凭据复制到正式环境。

## 版本与待办

正式代码 HEAD 仍为 8108a7e，工作区为本次登录修复补丁。构建基于开发基线 ce0ef8f，其 frontend 与 8108a7e 无差异，再叠加本次修复。未发布 ce0ef8f 的后端或金蝶配置。

开发修复在 codex/pms-release-readiness，尚未合并 master 或推送 GitHub。默认本地 5174 保持 master 页面，修复预览在 5176，代理同一 8000 后端与稳定 SQLite。用户可使用服务器 ael 密码登录；合并 master 后再将默认本地页面更新到正式修复版本。

金蝶仍处于测试账套、应用签名、指定 I0001 与只读模式。PMS 登录成功后的受权连接测试已返回 HTTP 200、success=true（金蝶连接测试成功）；正式专用账号替换与业务样本写入继续按金蝶 SOP 分阶段验收。
