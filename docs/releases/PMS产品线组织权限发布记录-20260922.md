# PMS 产品线组织权限发布记录

## 已完成

- 日期：2026-09-22；目标：`10.10.1.228` 的 `C:\PMS`，PMS 数据库 `10.10.1.230 / PMS`。
- 源码提交：`8db129908ea0d08efa8dba41be4b2ec837f44357`，分支 `codex/pms-db-permission-split`，未推送或合并主分支。
- 数据库版本：`2026-09-21-01` → `2026-09-21-02`。
- 用户已确认备份、升级和发布；管理员凭据仅在服务器交互输入，未入命令、日志或 Git。
- 数据库备份验证通过：`E:\PMS-bkup\PMS20260921.bak`，完成于2026-09-21 17:13:52。
- 程序与配置备份：`C:\PMS\.runtime\release-history\product-line-20260922-142812`。
- 发布资料及执行状态：`C:\PMS\.runtime\releases\product-line-20260922`。

## 验证证据

服务器工具返回 `VERIFY_OK`、`DEPLOYMENT_OK`、`DEPLOY_EXIT=0`：原业务摘要不变，档案940条；数据库结构、新菜单及一次性配置权限、源码和静态资源、受保护配置、健康接口、组织只读连接、匿名访问保护通过。

Mac 独立查询：健康接口返回 `{"status":"ok"}`；`/api/product-lines` 未登录返回403；服务器首页及产品线页面脚本与本批ZIP内文件逐字节一致。

只停止并启动PMS8000端口对应进程；未重启数据库、OA、服务器、门牌系统。传输服务使用服务器来源地址限制及文件哈希校验，完成后关闭，未传输任何凭据。

## 历史归属执行结果

- 用户已自行维护6个启用组织选项及角色授权；迁移前后角色、授权逐项一致。
- 实际数据中旧 `product_line_id` 全部为空；4条明确分类来自 `product_category`。按用户确认，档案1（PMS-2026-001）、2（OA-2026-001）归属Single组织4146325，对应产品线4；档案3（newtest）、4（PMS-ACCEPT-20260914-001）归属8吋半导体组织200292，对应产品线1。
- 2026-09-22 15:38服务器执行独立事务成功，角色迁移清单为空；15:40最终复核通过。总数940，已归属4、未归属936，仅4条归属及更新时间变化，ERP同步状态不变，成功审计日志1条。未调用ERP同步，未重启服务。
- 金蝶期初视图只有分类、编码、名称、备注、日期及内码，没有组织字段；936条不推测归属，按用户要求等待后续统一整理。未归属档案不向普通业务列表放开，不代表删除。
- 迁移计划与结果：`C:\PMS\.runtime\releases\product-line-20260922\approved-four-plan.json`、`four-assignment-result.json`。使用既有 `apply_plan` 服务，保留快照校验、事务锁、幂等检查和统一日志。

## 尚未执行

- 实际用户交互、完整采购/退料链路业务验收仍未完成；不能把部署和4条历史归属完成等同全部业务验收通过。
- 既有1条项目进度未关联档案，本次未按编码自动补关联。
- 开发机数据库已于2026-09-22独立升级到 `2026-09-21-02`。SQLite在线备份为 `.runtime/backups/pms-dev-pre-align-20260922-160235.db`，备份及升级后完整性检查通过。2条档案、1条进度、6条任务、1个用户、3个角色的原字段逐项一致；没有复制服务器数据或授权。

## 合并前验证

- 17组后端数据库升级、RBAC、产品线、采购契约入口全部退出0；7项前端标准契约及 `npm run build` 通过。构建仅有既有大资源包警告。
- 服务器只读服务层验证：单线各2条、两线4条、空授权0条，跨线详情404；新建候选随权限变化；真实只读报表查询返回0条。使用临时收窄的验证上下文，不修改任何用户或角色授权，不等同多账号浏览器验收。
- 本次Git收尾不再次部署服务器。后续文档提交会使Git提交号不同，但本批业务程序仍为已部署的 `8db1299` 内容。

本版本收紧数据权限，不能在结构或迁移失败时自动切回旧宽权限版本继续使用；若回退需单独核对数据库状态和权限数据。
