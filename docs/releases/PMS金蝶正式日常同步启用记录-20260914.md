# PMS 金蝶正式日常同步启用记录

日期：2026-09-14；用户明确确认开放生产日常同步。

## 启用范围

生产金蝶账套 2023121101 / 65771a661f427f，应用指定用户 I0001。只将 C:\PMS\backend\.env.local 中 K3_READ_ONLY 从 true 调整为 false，允许通过 PMS 正常权限校验的项目辅助资料新增、更新、提交和审核。目标仍为销售项目类别 xsxm；未扩展字段、用户、角色、数据范围或产品类别权限。

已部署的自动审核及占用提醒继续生效。用户点击“同步”或“保存并同步 ERP”后执行；本次开关切换没有主动批量同步或写入现有生产项目。PMS 新增档案本身与点击同步是不同操作，不能将开放同步理解为全部档案自动推送。

## 执行与验证

- 开放前：普通进程 PID 4796、受控版本校验一致、生产目标及 I0001 正确、无在途 pending 同步；样本 PMS-ACCEPT-20260914-001 为已审核 C。
- 初次预检因配置值带引号而停止，未修改配置和重启；兼容实际引号格式后重新预检通过。
- 配置备份：C:\ProgramData\PMS-config-backup-20260914-143519\env-before-enable。备份目录取消继承，仅运行管理员、SYSTEM、Administrators 可访问；不传回本地，不进入 Git 或普通证据目录。
- 14:35 停止旧服务，修改开关，比较备份及生效配置确认仅 K3_READ_ONLY 一项改变；配置文件原有 ACL 不变，无该开关环境变量覆盖。
- 普通 uvicorn 服务以新 PID 3100 启动，监听端口归属、新进程健康及 nginx 健康通过。启动前后重新加载配置均为 read_only=false，正式金蝶签名查询成功，样本仍为 C、内码 6aa7719bdb9bfc。
- 重启后正式 PMS 已登录页面正常读取 4 条项目档案，指定样本和同步入口可见。
- 本批不改应用代码。复用上一发布版本已通过的自动审核 15 项、占用 5 项、认证 13 项、查询 4 项及并发契约；当前仅验证配置差异和运行切换，不再次写入样本。

## 日常操作与异常处理

有权限的用户维护档案后，点击“同步”或“保存并同步 ERP”。成功标准为金蝶记录唯一、内码及名称一致、状态已审核，PMS 同步成功。占用失败时，先在金蝶端退出当前单据，再次点击 PMS 的“同步”；若由其他用户占用，联系其正常退出。网络超时等结果不确定时，先核对金蝶实际资料和状态，不能当作占用错误直接重试。

## 暂停与回退

需要暂停金蝶写入时，只将实际生产配置的 K3_READ_ONLY 改回 true，重启普通后端，核对生效值、端口及健康。PMS 自身项目维护仍可继续。保留其他配置，不用旧备份覆盖后续新凭据或其他合法变更。原始备份仅用于本次配置未发生其他变化时的受控恢复。开关回退不会撤销金蝶已有保存或审核结果。

当前为正式日常同步开启状态，不在本次结束时再恢复只读。专用账号替换仍待后续安排；继续仅使用 I0001。代码未推送、未合并；本地测试账套配置未改，服务器重启后继续读取生产受保护配置中的 false。

证据：.runtime/kingdee-validation/enable-production/production-evidence.json；服务器 C:\PMS-enable-production-20260914\enable-result.json。SOP 更新至 5.0。
