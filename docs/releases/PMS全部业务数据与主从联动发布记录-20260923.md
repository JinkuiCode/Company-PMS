# PMS 全部业务数据与主从联动发布记录

## 发布范围

- 用户已批准本次独立数据库升级及 PMS 部署。
- 原提交：`11c35665c60e6258f09247468ddddfa2bf64d062`。
- 目标提交：`f0bd1af99f3b613ea9f1e58ebd81202ba4417229`，本地分支 `codex/pms-inventory-report`；未推送 GitHub，未合并 master。
- 数据库从 `2026-09-22-02` 升级到 `2026-09-23-01`，不新增表列；只新增 `business:data:all` 权限并首次授予 admin，后续撤权不回补。
- 项目档案、项目进度详情随列表行选择切换，草稿保护和异步防错配遵循统一规范。
- 只允许重启 PMS，不重启 SQL Server、OA 或服务器。

## 已完成准备

- 远程核对 `C:\PMS` 当前提交为原提交，分支 `codex/pms-db-permission-split`，工作区干净，健康接口 ok。
- 固定目标提交重新构建发布包通过，发布工具 6 项校验测试通过。
- 发布目录：`C:\PMS\.runtime\releases\all-data-20260923`。
- 传输包 SHA256：`ecd44addd283038a357b04c1af153d7c5ebe06628d5033daf9d80ce3167aec7d`。
- 服务器校验并解包成功，返回 `ALL_DATA_PACKAGE_READY`；仅允许 PMS 服务器下载的临时传输服务已关闭。
- 程序及受保护配置备份：`C:\PMS\.runtime\release-history\all-data-20260923-102227`。

## 当前状态

部署完成。用户输入独立升级身份后，发布工具返回 `VERIFY_OK`、`DEPLOYMENT_OK`、`DEPLOY_EXIT=0`。随后单独执行只读 `--verify`，再次返回 `VERIFY_OK`、`VERIFY_EXIT=0`。

- 已核验完整备份：`E:\PMS-bkup\PMS20260922.bak`，时间 `2026-09-22 18:02:45`，通过 RESTORE VERIFYONLY。
- 当前运行提交：`f0bd1af99f3b613ea9f1e58ebd81202ba4417229`；数据库版本：`2026-09-23-01`。
- 档案940条，升级前后业务基线相同；表列不变，旧权限和角色关系不变；仅新增本批权限节点和 admin 一次性授权。
- 运行源码、前端产物逐文件核验通过，受保护配置未变，服务端首页与固定发布包一致，匿名报表请求仍被拒绝。
- 服务器本机以 `http://10.10.1.228/api/health` 返回 `{"status":"ok"}`，`http://10.10.1.228/login` 返回 HTTP 200。
- 仅重启 PMS；未重启 SQL Server、OA 或服务器。未推送 GitHub，未合并 master。

### 验收限制

发布时开发机直连 curl 返回 `Empty reply from server`，内置浏览器返回 `ERR_EMPTY_RESPONSE`。用户随后通过 TUN 开关对比定位到 Clash Verge，并确认回退旧版本后访问恢复；PMS 未为此修改配置。访问阻塞已解除，具体新版 Clash 内核原因未确定。全部业务数据可见性与两个详情抽屉联动仍待用户业务验收。

远程输入遇到剪贴板超时及普通回车不生效，已使用编码命令和数字键盘回车恢复；未因此执行任何异常写入。

## 开发机数据库对齐

2026-09-23，用户批准继续收尾。本地无8000端口后端监听，先通过SQLite备份生成 `.runtime/backups/pms-dev-pre-align-20260923.db`，再通过独立升级脚本将 `.runtime/data/pms-dev.db` 从 `2026-09-21-02` 升至 `2026-09-23-01`。

原有23张表逐项比对：除预期版本与权限节点变化外，原有业务数据不变、旧菜单与授权保留。完整性检查ok；应用加载及数据库就绪检查通过，进程内健康接口200，匿名报表/导出接口拒绝访问。未复制生产数据、未修改服务器。本次业务代码与已验证部署版本相同，复用对应测试和生产构建证据。

## 主分支收尾

已推送功能分支 `codex/pms-inventory-report`；本地master快进合并至 `a844856`，合并后53项权限测试、6项前端检查通过。业务代码与服务器部署的 `f0bd1af9` 相同，后续提交仅为发布和对齐文档。不为文档提交重启或重新部署服务器。此处上文“未推送、未合并”描述的是当时部署阶段，当前以本节及Git远端引用核对为准。用户业务验收、安全阻塞和历史归属整理并未因Git收尾自动完成。
