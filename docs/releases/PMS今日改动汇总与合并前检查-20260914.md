> 后续结果：本报告记录修复前检查。三项发布问题随后已修复，程序已本地提交为 327c2d8，并完成真实候选包验证；见[发布工具修复与候选包验收](PMS发布工具修复与候选包验收-20260914.md)。未推送、未合并、未操作生产。

# PMS 今日改动汇总与合并前检查

检查日期：2026-09-14 15:04—15:08。范围：本地文件、另一任务交付记录、GitHub 主分支只读检查与本地测试；未操作生产服务，未提交、推送、创建 PR 或合并。

## 结论

业务代码回归通过，但尚不宜直接制作最终正式发布包。发布工具有三项已复现问题需要修复，根目录与发布工作区的两份历史文档需要按段整合。当前金蝶生产日常同步已在此前获批操作中开放，本轮不会把它改回只读。

## 分支与任务归属

- 本地 master 与 GitHub origin/master 实时核对均为 ce0ef8fd0e6a5751cca99c93835051816a1f3890。
- 发布工作区 codex/pms-release-readiness：HEAD b04919386dbf2d3c0391558da82a4109e5db1bec；在 master 上仅多一个已提交的登录反馈修复，其他本批内容尚未提交。
- 另一任务“PMS系统开发”目前未运行。读取其完成记录确认，先前 UI/统一表单等已纳入 master；9 月 12 日创建的发布工具和发布文档继续留在本工作区。不重复引入旧 UI 分支或整体恢复历史 stash。
- 仍保留 stash@{0} 的 backup/pre-consolidation-root-20260912，属于历史备份，不在本轮合并清单中，也不删除。

## 本次拟纳入内容

| 组别 | 内容与状态 |
|---|---|
| 登录错误反馈 | Login.vue 与 login-feedback-contract.test.mjs，已提交 b049193；此前已单独部署正式前端 |
| 金蝶业务同步 | kingdee.py：类别与编号双条件查询、自动提交审核与内码核对、占用指引；源码尚未提交，已按此前发布证据部署 |
| 金蝶测试 | query_scope、auto_audit、occupancy 三个新增契约，以及现有并发契约替身更新 |
| 发布工具 | .gitignore、build-server-release.command、server_release_contract.py；另一任务完成的工作，存在本次复现的发布问题 |
| 交付文档 | SOP 5.0、登录/金蝶各阶段发布记录、正式日常同步启用记录、设计计划、发布回退说明及 change.md |

生产目标配置只保存在服务器受保护文件中，不纳入提交；不要用测试配置或仓库默认值覆盖已启用的生产配置。凭据、数据库、.runtime、node_modules、前端构建目录和旧发布 ZIP 不进入代码合并清单。

## 当前版本验证

- 后端 22 个契约脚本：21 通过，1 个发布包契约未通过。
- 唯一未通过原因：发布脚本检测到 backend/app/services/kingdee.py 尚未提交，主动拒绝打包。此保护符合设计，不能作为业务回归失败，也不能将该项算通过。
- 前端 23 个契约脚本全部通过，含样式、标准列表、系统 UI、档案筛选、统一表单、登录及权限契约。
- npm run build 通过（类型检查与生产构建），无前端源码变更。
- Git diff --check 通过。代码审查覆盖发布工具；本线程此前已审查自动审核与占用提示，当前业务契约重新全部通过。
- 证据目录 .runtime/premerge-20260914 保存每个命令、工作目录、退出码及日志。临时包装复现仓库自动删除，没有改变当前分支或生产文件。

## 发布工具必须处理的问题

| 等级 | 文件位置 | 证据与影响 | 建议处理 |
|---|---|---|---|
| P1 | build-server-release.command:73 | --skip-build 只检查 dist/index.html；独立复现用 A 版前端产物、B 版 HEAD，生成的 ZIP 清单声明 B，但仍含 A 前端。内外哈希只能证明包内文件一致，不能证明来源正确 | 取消跳过构建，或要求产物与目标提交及源码摘要匹配 |
| P2 | build-server-release.command:48 | 脏文件清单漏掉 frontend/index.html 和 tsconfig*.json。实际复现入口有未提交改动仍通过保护 | 覆盖完整构建输入，或固定提交的独立目录构建 |
| P2 | build-server-release.command:120 | 相对 --output-dir 在临时压缩目录中被重新解释；独立复现退出码 15 | 创建后解析为绝对路径再打包 |

发布测试需要加入上述反例；现有成功契约使用 --skip-build，无法识别前端来源错配。本轮仅审查与复现，没有修改发布工具。

## 文档整合与版本一致性

- 根目录与工作区 SOP 5.0 及五份金蝶发布记录内容一致。
- change.md 不同：根目录保留 9 月 14 日早期启动记录，工作区保留 9 月 12 日发布候选收口记录。合并时两段都应保留，不整文件互相覆盖。
- docs/records/金蝶对接实施过程记录-20260910至20260914.md 有日期和旧阶段分支状态差异，应作为带时间的历史材料核对，不能当当前状态覆盖 SOP。
- 旧自动审核计划结尾仍写“占用提示待确认”，已过时；SOP 5.0 和实际发布记录已经完成。旧 9 月 12 日发布报告及通用发布指南也仍写未部署/只读阶段。合并前须标明历史边界，并让后续发布明确恢复当前已批准的日常写入状态。
- 9 月 12 日 ce0ef8f 的旧候选 ZIP 不含后续登录、查询、审核和占用改动，不能作为本次最终发布包。

## 运行来源与服务器版本

本轮只读启动状态检查：标准启动器指向根目录 master@ce0ef8f；本地后端 PID 4688、8000 实际来自发布工作区，显示 mismatched or unmanaged；前端 PID 2176、5174 来自根目录。这里只报告来源混用，不重启或切换。最终版本确定后，再用标准启动方式统一源码与配置来源。

根据本线程 14:35 的已验证部署记录，生产为 C:\PMS，普通后端 PID 3100，K3_READ_ONLY=false，账套 2023121101 / 65771a661f427f、I0001；服务器 Git HEAD 仍为 8108a7e，加已发布补丁。此处复用当时证据，本轮未重新连接生产。后续任何部署均应重新核对现场进程和文件，避免覆盖已正式启用的配置。

## 建议执行顺序

1. 修复发布工具三项问题并补反例测试；整合两份历史文档的独有段落，更新过时的计划状态。
2. 提交发布工具与金蝶改动，重新运行发布包契约，从确定版本生成新包并核对前后端来源、内容排除和校验清单。
3. 经用户对推送/合并动作明确确认后，按约定汇入 master。当前无已创建 PR。
4. 最终合并完成后，另按确认的运行切换安排统一本地启动来源，并对齐正式服务器版本记录；保留生产日常同步开启状态。

本轮交付止于汇总与检查。专用金蝶账号替换不阻碍当前 I0001 日常同步，待新账号准备好再单独办理。

## 审阅快照中的文件清单

- `.gitignore`
- `backend/app/services/kingdee.py`
- `backend/tests/kingdee_auto_audit_contract.py`
- `backend/tests/kingdee_occupancy_contract.py`
- `backend/tests/kingdee_query_scope_contract.py`
- `backend/tests/project_archive_lifecycle_concurrency_contract.py`
- `backend/tests/server_release_contract.py`
- `build-server-release.command`
- `change.md`
- `docs/PMS服务器发布与回退说明.md`
- `docs/records/金蝶对接实施过程记录-20260910至20260914.md`
- `docs/releases/2026-09-12-PMS开发机发布候选检查报告.md`
- `docs/releases/PMS登录修复发布记录-20260914.md`
- `docs/releases/PMS金蝶单据占用提示发布记录-20260914.md`
- `docs/releases/PMS金蝶正式日常同步启用记录-20260914.md`
- `docs/releases/PMS金蝶生产只读部署记录-20260914.md`
- `docs/releases/PMS金蝶生产样本验收记录-20260914.md`
- `docs/releases/PMS金蝶自动审核发布验收记录-20260914.md`
- `docs/superpowers/plans/2026-09-14-kingdee-auto-audit.md`
- `docs/superpowers/specs/2026-09-14-kingdee-auto-audit-design.md`
- `docs/金蝶第三方应用对接SOP.md`
- `frontend/src/views/Login.vue`
- `frontend/tests/login-feedback-contract.test.mjs`
