# PMS 三端版本统一记录

日期：2026-09-14。授权范围：用户明确要求将 Mac 开发机、正式服务器和 GitHub 统一版本，包含已说明的服务切换、备份及验证。

## 交付版本

程序来源为 master / `e752c139729aa4a62f4b588dc5aaa610d1a42cda`。后续记录提交仅涉及文档，与该程序版本无代码差异；交付记录随主分支同步。服务器不再使用旧分支加现场补丁作为当前运行来源。

发布包：`pms-server-e752c139-20260914.zip`。

SHA256：`90e8ae6f44a479b14055f304e0a414d78e1c80393a2766ca79f091d966be9783`。

从确定提交重建前端，包内 221 项校验通过；服务器实际后端及前端 219 个文件逐项校验通过。真实配置、数据库未进入发布包。此前适用于同一程序内容的业务及前端契约验证复用，本次另完成实际运行验收。

## Mac

- 标准源目录：`/Users/jin/Code/PMS`；前后端端口 5174 / 8000。临时验收后端已退出。
- 数据库：保留 `.runtime/data/pms-dev.db`，使用 SQLite 在线备份并通过 integrity_check，备份 `.runtime/version-unification-20260914/mac-before.db`。
- 测试配置保持 test20260910 / 6aa25ec7d9f511、I0001、只读。真实金蝶连接返回 success=true，未执行业务保存。
- 用户重新登录后仪表盘和项目档案读取正常；标准启动器确认前后端均由根目录管理。
- 普通执行环境无权管理既有进程，改用获授权的服务会话完成启动。当前会话保持服务运行；以后使用根目录 start-pms.command 启动，不再使用临时验收脚本。

## 服务器

- 目录 `C:\PMS`，切换至 master。旧分支 `codex/pms-server-sync-20260910` 和原补丁 stash 均保留。
- 原程序、前端、nginx 配置、运行信息及 Git 配置备份：`C:\ProgramData\PMS-release-backup-20260914-155931`，备份目录仅授权当前管理员、SYSTEM 和 Administrators。
- 原 SQL Server 数据库 PMS 保留，备份由 SQL Server 执行 COPY_ONLY / CHECKSUM，并通过 RESTORE VERIFYONLY WITH CHECKSUM。备份位于 SQL Server 主机的 `D:\Program Files\Microsoft SQL Server\MSSQL14.MSSQLSERVER\MSSQL\DATA\PMS-unify-20260914-155249.bak`。校验为备份可读性验证，没有执行恢复演练或覆盖数据库。
- 正式配置字节校验不变，生产账套 2023121101 / 65771a661f427f、I0001、K3_READ_ONLY=false；未迁移数据库、替换凭据或调整 OA 配置。
- 普通后端 PID 4964（本次验收快照），127.0.0.1:8000；nginx 80 端口继续提供服务，静态目录为 C:/PMS/frontend/dist。
- 后端和代理健康返回 ok；实际首页引用新包资源；金蝶查询成功且样本 PMS-ACCEPT-20260914-001 保持 C；没有待处理同步任务。
- 正式浏览器档案 4 条、项目进度 1 条正常；正式 OA 域名入口重新进入 PMS，显示 A000645。页面只读验收未触发同步、编辑或保存。

## 切换中处理的问题

1. 预检发现服务器使用旧功能分支，先保留该分支再建立 master，没有覆盖旧历史。
2. Git 导入触发自动清理时遇到文件占用，保留被占用文件，后续命令关闭本次自动清理，不强制删除 Git 对象。
3. Windows 的自动换行转换使文件和索引报告差异。初次最终检查失败，自动回退也被这些差异阻止；随后恢复已通过业务查询验证的新服务，再确认所有差异仅涉及行尾。备份 Git 配置，将本仓库 core.autocrlf 设置为 false，按 HEAD 重建工作区与索引，最终工作区干净且运行文件哈希一致。没有为换行差异创建代码提交。
4. 自动审批拒绝将服务器版本、进程等元数据回传 Mac；停止该回传，最终核验结果保存在服务器本地并通过控制台查看。

## 证据与回退

服务器最终证据：`C:\PMS-unify-20260914\verified-result.json`；服务日志为同目录 resume-out.log / resume-error.log。不要把较早的失败输出当作最终状态。Mac 构建与备份材料在 `.runtime/version-unification-20260914/`；本次只读连接及运行状态另经现场验证。

如需回退，应先确认当前没有在途同步，停止明确识别的 PMS 进程，恢复上述备份程序与前端并按原方式启动。恢复 Git 旧分支前先保存当前差异，不直接在有差异时切换；数据库和配置只有确实需要恢复时才单独处理，不能把程序回退等同于整库恢复。

保留临时开发工作区和备份以供追溯。未来专用金蝶账号替换另行安排。本次没有扩大账号或业务权限。
