# PMS 发布工具修复与候选包验收

日期：2026-09-14。结论：合并前发现的三项发布问题已修复，已完成本地程序提交、真实构建与候选包检查。本轮没有推送、合并或操作生产服务。

## 版本与文件

- 分支：codex/pms-release-readiness。
- 程序提交：327c2d83bfdb2e629a9e998b007d665c299ba605。该提交包含此前已单独发布的金蝶增强，以及本轮发布工具修复；前一个提交 b049193 为登录反馈修复。
- 候选包：/Users/jin/Code/PMS/.worktrees/pms-release-readiness/release/pms-server-327c2d83-20260914.zip。
- SHA256：c9c181eced3dac36c099094477ae9bf63ffcf41bb843dbe341ffe5eb90c36d1a。
- 大小：5754512 字节；包内校验文件共 221 个。
- 本文及后续证据日志作为文档提交另行保存，不改变候选包对应的程序提交；候选包不因交付记录更新而重命名。

## 已解决问题

1. 取消 --skip-build；前端从固定提交的临时源码目录构建，后端和发布说明同样来自该提交。旧 dist 不再参与打包。
2. 完整 frontend、发布脚本和部署说明纳入未提交检查，覆盖入口、编译配置及未跟踪输入。
3. 相对输出目录转成绝对路径，已用实际 --output-dir release 成功生成候选包。

## 验证证据

- 6 项发布契约在独立 Git 测试目录中先失败后通过，覆盖上述反例、后端脏改动、配置/数据库排除、Git 提交及内外校验。
- 此前完整验证的 21 项后端业务契约输入和相关代码未变化，证据继续有效；新增发布契约通过后，22 个后端契约脚本均有适用通过证据。
- 前端 23 项契约全部通过。候选包再次执行真实类型检查和生产构建，退出 0；保留既有大分块提示。
- ZIP 完整性通过，外部 SHA256 正确，221 个内部文件哈希逐项通过。全部后端文件及 SERVER-DEPLOYMENT.md 与程序提交的 Git 内容逐字节一致。
- 发布清单明确前端源码来自同一程序提交；包内含自动审核和占用指引，不含真实 .env.local、数据库、测试、node_modules、.git 或日志。
- 前端使用本机已安装依赖。源码固定不等于依赖环境完全可重复；没有在本轮重新安装或升级依赖。
- 独立只读代码复核确认原三项问题解决，无新阻断项。

## 文档与范围

根目录与工作区独有的两段 change.md 已合并保留；历史记录保留时间边界，自动审核计划中过时的待确认描述已修正。通用发布步骤明确保留生产已获批的日常同步状态，不再将历史只读默认当成永久关闭指令。

当前生产继续使用此前已启用的 I0001 日常同步，本轮未连接服务器。master 仍为 ce0ef8f，发布分支本地提交不等于已更新 GitHub 或已合并。根目录保留文档副本，待获批合并时核对，不覆盖源代码或恢复旧 stash。

后续结果：用户确认后，已推送发布分支并快进合并至 master（309d760），详见[主分支合并与运行版本交接](PMS主分支合并与运行版本交接-20260914.md)。本地标准启动来源和正式服务器运行版本尚待统一；该候选包尚未作为整包部署到服务器。

本地证据：.runtime/premerge-20260914/release-verification.json、real-release-build.log、backend-results.json、frontend-results.json；前一轮失败的发布测试属于修复前证据，不能当作当前结果。
