# PMS 变更记录

## 2026-09-22 GitHub主分支收尾完成

- 已推送 `codex/pms-db-permission-split`，将master从 `1aabe43` 快进至 `38ba00d`，推送后通过远端引用核对完整提交一致。直接合并，未创建PR；本地当前分支为master。
- 合并前独立审查未发现本批新增的阻塞问题；合并结果与通过测试的开发分支完全一致。另用升级后真实本地数据库验证应用启动、健康接口200及匿名产品线接口403。
- 本次记录提交只补充文档，业务程序与服务器 `8db1299` 无差异，不重新部署服务器。不删除其他任务可能仍引用的功能分支。

## 2026-09-22 开发机数据库对齐与合并准备

- 用户批准先升级本机数据库，再推送GitHub并合并master。通过独立升级脚本将 `.runtime/data/pms-dev.db` 从 `2026-09-20-02` 升至 `2026-09-21-02`，本地服务升级前均已停止，无服务器操作。
- SQLite备份 `.runtime/backups/pms-dev-pre-align-20260922-160235.db` 和升级后完整性检查均通过；逐项比对档案、进度、任务、用户和角色的全部原字段一致。未复制服务器数据、未生成产品线授权、未删除测试记录。
- 涉及文档：产品线迁移操作说明、采购报表说明、产品线发布记录；整理既有尚未提交的部署记录，不修改业务代码。
- 验证：17组后端数据库升级/RBAC/产品线/采购契约退出0；7项前端样式/列表/系统UI/筛选/字典/枚举/产品线契约及构建通过；`git diff --check` 通过。Git合并结果以最终提交及远端核对为准。

## 2026-09-22 四条历史档案产品线归属完成

- 原因：用户完成组织选项及角色授权，批准明确的 Bench/Single 归属；无匹配的数据暂时空置，后续统一整理。
- 服务器实际分类字段为 `product_category`，旧 `product_line_id` 全部为空。核对后通过已部署的 `product_line_assignments.apply_plan` 独立事务更新4条：档案1、2归属 Single（组织4146325，产品线4），档案3、4归属8吋半导体（组织200292，产品线1）。没有修改分类、项目编码或名称。
- 其余936条保持未归属；金蝶期初视图没有组织字段，不推测归属。角色配置、角色授权、项目进度引用保持原样，不调用ERP同步。
- 验证：服务器返回 applied、VERIFY_OK、FINAL_VERIFIED；940条总数不变，仅4条的归属和更新时间变化，同步状态不变，迁移成功日志恰好1条；Mac检查健康接口返回ok。无须重启任何服务。
- 涉及记录：`docs/releases/PMS产品线组织权限发布记录-20260922.md`；服务器清单及结果保存在 `C:\PMS\.runtime\releases\product-line-20260922\approved-four-plan.json`、`four-assignment-result.json`。未推送或合并主分支。

## 2026-09-22 产品线权限服务器部署完成

- 用户输入独立升级身份后，服务器部署工具验证数据库备份 `E:\PMS-bkup\PMS20260921.bak`（完成时间 2026-09-21 17:13:52），升级并发布成功，退出码0。
- 服务器程序：`8db129908ea0d08efa8dba41be4b2ec837f44357`；数据库版本：`2026-09-21-02`。发布工具核对原有业务摘要不变，档案940条，新产品线/角色产品线关联表为空，不代用户分配归属或授权。
- 服务器验证：源码及运行文件哈希、配置不变、结构及新增菜单权限、组织只读读取、前后端健康、匿名访问保护均通过（VERIFY_OK、DEPLOYMENT_OK）。Mac 另行访问 `/api/health` 得到 ok，产品线接口匿名返回403，实际首页及产品线页面脚本与发布包逐字节一致。
- 仅停止并启动 PMS，未重启 SQL Server、OA、服务器或其他业务进程。程序备份：`C:\PMS\.runtime\release-history\product-line-20260922-142812`。
- 待用户在页面创建产品线和配置角色授权，再执行已确认的 Bench/Single 历史归属迁移与实际业务验收。未推送 GitHub、未合并 master；本机源码同提交，但本机开发数据库未在本次升级。

## 2026-09-22 产品线发布包已传服务器，等待升级身份

- 远程连接恢复后确认 PMS 监听8000；仅针对 PMS 进程部署，其他门牌与 Nginx 进程不停止。
- 使用既有受控部署流程准备本批脚本 `release/product-line-deploy.py`、定向传输工具 `release/serve-product-line.py`；发布包及源码 bundle 已发送到 `C:\PMS\.runtime\releases\product-line-20260922` 并通过哈希、当前分支/版本、待执行同步任务等前置检查。
- 旧程序及受保护配置备份已建立：`C:\PMS\.runtime\release-history\product-line-20260922-142812`。临时传输服务已关闭，不传输任何账号密码。
- 当前停在服务器本机的独立 SQL 升级账号输入处，尚未停止 PMS、执行结构升级或切换新版。后续仍由用户输入凭据后校验数据库备份；产品线及角色关系不自动生成。

## 2026-09-22 产品线权限发布包准备

- 用户确认本批数据库备份、结构升级和部署，仍禁止重启数据库、OA 或服务器。
- 本批部署源码固定为本地分支 `codex/pms-db-permission-split` 的 `8db1299`，从确定提交重新构建发布包 `.runtime/releases/product-line-20260922/pms-server-8db12990-20260922.zip`，不包含凭据、数据库和运行数据；其他待办文档保持原工作区状态。
- 验证：数据库升级契约、6 项发布工具契约、确定提交前端构建、ZIP 完整性及 SHA256 校验通过。服务器 `/api/health` 返回 ok，228 远程桌面端口和230数据库端口可达。
- 执行边界：Windows App 原会话已断开，重连等待用户处理 RDP 证书提示；尚未覆盖服务器文件、执行数据库升级或历史归属迁移，未推送 GitHub、未合并主分支。

## 2026-09-22 历史产品线档案清单生成

- 原因：用户自行在页面配置产品线和角色后，需要按已确认历史归属生成迁移清单，避免手工拼接主键及误改角色授权。
- 调整：迁移工具增加只读 `--prepare-mapping`，输入经核对的旧枚举编号与组织内码，读取真实产品线编号及当前快照；仅列入未归属且匹配的档案，未知值另列。目标缺失、禁用、重复来源、版本或编码冲突时不生成可执行清单；角色固定为空，已有归属不覆盖。
- 涉及文件：`backend/app/services/product_line_assignments.py`、`backend/scripts/migrate_product_line_assignments.py`、`backend/tests/product_line_assignment_migration_contract.py`、`docs/产品线迁移操作说明.md`、产品线实施计划。
- 验证：新增测试先因缺少函数及命令选项失败，实现后 16 项迁移契约通过，包含只读文件生成及实际迁移保留原角色授权。只使用隔离 SQLite 和临时文件，未连接或修改服务器业务数据。

## 2026-09-22 产品线配置与迁移操作说明更新

- 原因：用户明确由其在页面维护产品线和角色授权，并确认两条历史归属。
- 调整：补充产品线管理、角色授权的实际入口和操作顺序；记录 Bench 对应组织 200292、Single 对应组织 4146325；迁移说明明确本批 `roles: []`，不代用户分配角色范围，其余归属不推断。
- 涉及文件：`docs/产品线迁移操作说明.md`、`docs/产品线组织字段映射.md`、`docs/superpowers/plans/2026-09-21-product-line-organization.md`。
- 验证：核对前端路由、页面控件及后端菜单权限；产品线 UI 契约、12 项迁移契约通过。本批仅更新项目文档，未写业务归属或授权，未部署服务器。

## 2026-09-22 角色产品线加载失败保护（开发机）

- 原因：产品线候选请求失败时，角色页面存在未处理异常，编辑入口缺少加载保护。
- 调整：候选读取失败不视为空授权、不打开不完整编辑页；恢复后可重新打开。打开期间阻止重复点击，权限树或角色权限读取失败同样中止打开。
- 涉及文件：`frontend/src/views/system/RoleList.vue`、`frontend/tests/product-line-browser.mjs`。
- 验证：新增浏览器反例先复现 503 未处理异常，修复后完整隔离浏览器回归通过；样式、标准列表、系统 UI、产品线契约及 `npm run build` 通过。构建保留已有大包警告。本次启动的本地测试服务曾未更新编译缓存，重启该测试服务后核对返回模块包含新逻辑，再完成浏览器验证。
- 边界：浏览器使用模拟接口，无正式业务写入；未升级数据库、部署服务器、提交、推送或合并。组织选用、历史归属、角色授权清单仍待确认。

## 2026-09-21 产品线组织权限：同步、报表、页面与迁移工具（开发机验证阶段）

- 原因：继续已批准方案，补齐业务授权消费端及历史归属的安全迁移路径；仪表盘保持排除。
- 同步：队列首条历史记录保存项目/组织目标；发送、人工回查前后验证一致性，交互重试/回查重新读取授权。组织缺失或变化不发送、不把旧目标回查结果认作当前档案成功；ERP 必填规则读取新归属字段。
- 报表：授权按项目编码与组织配对、分页前生效；大范围使用受限会话临时表。申请/订单/收料/入库/退料组织不符进入待核对；合并来源订单的供应商也从候选及筛选中隔离，避免隐藏明细侧漏。未改数量计算公式。
- 页面：新增产品线管理标准列表及用户 B 抽屉，组织绑定只读、显示名可改，支持并发冲突保留输入；角色改为明确产品线勾选，停用的已有授权可取消。档案新增重新读取授权候选，单个自动带出、多个主动选、零个阻止创建。旧产品线枚举隐藏，字段目录区分旧枚举与新组织来源。
- 并发：组织目录校验之后重新读取并锁定产品线状态，防止保存期间已禁用仍被新增选入。MSSQL 使用更新/范围锁；真实 MSSQL 并发效果仍待服务器验收。
- 迁移：新增默认只读清单工具与中文标题分类；应用需明确清单、当前版本及快照指纹、有效迁移权限。整批事务记录统一日志，相同批次重跑不恢复后续撤权，不覆盖已有组织归属。支持旧结构只读盘点；不会自动将采购活动推断为档案归属。
- 文件：`backend/app/services/erp_queue.py`、`kingdee.py`、`purchase_reader.py`、`product_line_scope.py`、`project.py`、`product_line_assignments.py`、`enum_registry.py`、`field_catalog.py`；`backend/app/api/product_lines.py`、`purchase_reports.py`；`backend/scripts/provision_purchase_reader.py`、`migrate_product_line_assignments.py`；`frontend/src/api/productLine.ts`、`api/auth.ts`、`composables/useProductLineOptions.ts`、`views/system/ProductLineList.vue`、`RoleList.vue`、`views/project/ProjectArchive.vue`、`ProjectList.vue`、路由与图标注册；对应契约及浏览器测试、组织字段映射、迁移说明和实施计划。
- 验证：新增迁移 11 项、项目范围 14 项、同步范围 7 项、报表组织范围 7 项通过；主数据 13 项、授权 10 项、API 4 项、源读取 4 项、发现 4 项、只读授权 2 项通过。原有采购 connection/progress/reader-account/api/reader、ERP 队列 22 项、档案业务字段 11 项、档案语义/生命周期/并发、进度总表列表/详情/统一保存/更新、字段规则/目录、枚举、RBAC、角色首页、数据库升级、操作日志与档案分页契约通过。旧夹具补充明确组织归属和角色授权，未放松生产检查。
- 前端：七个规定/新增产品线契约与 `npm run build` 通过（保留既有大 chunk 提示）；产品线新增编辑/冲突保留草稿/1366、1600、390 三种尺寸，以及角色停用授权撤销、档案候选数量分支和原地址日期行为通过浏览器测试。删除失败未处理异常已补保护，最终浏览器复验另记同批结果。
- 真实数据边界：只读抽查 6 条已有订单及入库的申请，2 个组织，6 条订单及 6 条入库，未发现组织不一致；没有退料样本。未写金蝶或 PMS 正式业务数据。迁移只在本地隔离库应用测试，本地开发库仅生成只读清单。
- 补验：同 JWT 档案列表/修改接口撤权立即生效、重新授权恢复，API 契约现为 5 项通过；档案产品线来源失败拒绝新增、恢复后重试通过。删除已引用产品线显示拒绝提示且页面无未处理异常；组织候选失败重试、旧响应迟到不覆盖新查询通过，产品线浏览器最终复验通过，最终前端构建通过。独立复核补充旧角色范围快照字段修复，迁移契约现为 12 项通过；只读工具重跑输出已包含旧角色范围中文列。
- 剩余：真实组织选用、显示名、历史档案及角色映射待用户确认；迁移候选冲突核对、真实退料链路、目标数据多用户验收、MSSQL 实际升级/锁行为待完成。未部署、未推送、未合并，不代表整批任务完成。

## 2026-09-21 档案与进度产品线范围接入（开发中）

- 原因：继续已批准的组织产品线权限改造；按用户最新要求，本批排除仪表盘，保留其原有实现，不纳入本批覆盖结论。
- 调整：档案列表、总数、候选及进度对象查询按新组织产品线归属与人员/部门范围取交集，空授权不再放行。档案创建校验授权、启用状态及金蝶组织有效性，新归属写入独立外键，不覆盖历史枚举；变更同时校验目标范围，已同步或期初档案禁止直接换组织。产品类别不再用于档案及进度的人员分配授权判断；产品线字段必填、显示、可编辑规则锁定，不能用字段规则绕过安全必填。
- 文件：`backend/app/services/project.py`、`list_query.py`、`field_policy.py`、`backend/tests/product_line_project_scope_contract.py`、组织产品线实施计划。
- 验证：对象范围及档案写路径 12 项、角色授权 10 项通过，字段目录及枚举契约通过，`git diff --check` 通过；新增行为先取得预期失败再修复。
- 边界：同步队列、报表组织链路、前端、完整回归和历史归属迁移尚未完成，本批不是最终交付。未部署、未推送、未合并，未修改生产业务数据。未来启用仪表盘前须另行检查其产品线数据范围。

## 2026-09-21 组织产品线基础模块与最小读取权限（分阶段实施中）

- 原因：已批准以金蝶组织作为产品线主数据来源，角色明确选择产品线，取消未来业务路径的空值不限约定。
- 本地调整：新增产品线主数据、角色关联表、档案独立归属外键及升级逻辑，版本提升为 `2026-09-21-02`；旧枚举归属保留，不自动猜测迁移。实现主数据增改删、引用删除保护、并发版本检查、中文差异日志、组织候选查询及受控 API；增加角色关联保存与实时产品线并集、强制改密清空范围。新增菜单只在创建时授权，不回补人工撤销的权限。
- 现场调整：经独立明确批准，现有 `pms_purchase_reader` 追加两张组织表共七列，以及收料、退料各一列的 SELECT。未修改任何金蝶/PMS 业务数据、未重建账号、未授予整表或全库读取；密码交互输入后释放。没有重启数据库、服务器或部署 PMS。
- 涉及文件：`backend/app/models/product_line.py`、`project.py`、`init_db.py`；`backend/app/schemas/product_line.py`、`rbac.py`、`user.py`；`backend/app/services/product_line*.py`、`authorization.py`、`auth.py`、`rbac.py`、`field_catalog.py`、`operation_log.py`、`database_revision.py`；`backend/app/api/product_lines.py`、`backend/main.py`；`backend/scripts/inspect_product_line_organizations.py`、`grant_product_line_reader.py`；六个 `backend/tests/product_line*_contract.py`；实施计划和 `docs/产品线组织字段映射.md`。
- 验证：新测试均先失败再通过；发现工具 4 项、主数据 12 项、角色授权 10 项、来源查询 4 项、API 3 项、最小授权 2 项通过；字段目录、枚举、RBAC、角色首页、操作日志及独立数据库升级契约通过。真实只读账号重新连接核验新列可读、无写权限；组织目录 20 条，分页首批 5 条，模糊搜索“事业部”7 条。
- 未完成边界：这是基础模块，不是整套权限改造完成。现有档案、进度、报表仍待统一切换新授权；旧产品类别范围在切换前仍保留。前端产品线菜单/表单、历史映射工具及正式迁移未交付；不得将当前开发中代码直接发布。未推送 GitHub、未合并 master、未升级服务器 PMS 数据库。

## 2026-09-21 采购进度查询服务器发布

- 原因：用户批准部署采购报表，并完成新备份和独立数据库升级凭据输入。
- 发布：`10.10.1.228 / C:\PMS` 从 `e753619a` 更新至 `d1600e55`；PMS 数据库版本从 `2026-09-20-02` 升级到 `2026-09-21-01`，新增报表菜单及管理员首次查看/导出授权。运行时独立只读配置通过 Windows 启动器接入，未改原 PMS、OA 和金蝶同步凭据。
- 备份：原 24 小时校验完整保留；用户新备份 `E:\PMS-bkup\PMS20260921.bak`（17:13:52）通过校验。程序回退副本位于 `C:\PMS\.runtime\release-history\purchase-report-20260921-152420`。此前放宽时限的尝试被拦截，没有在服务器执行，部署使用原始校验器。
- 验证：服务器 `VERIFY_OK / DEPLOYMENT_OK / DEPLOY_EXIT=0`；940 条档案及原有业务数据哈希一致，数据库表结构不变，新增菜单/权限检查通过，报表独立只读账号查询通过。实际网址健康接口正常、匿名报表 API 返回 403；页面 HTML 和五个入口资源与固定版本发布包一致。未重启 SQL Server、OA 或服务器，仅更新 PMS。
- 文件：本记录、`docs/采购进度查询报表说明.md`、实施计划；发布器与状态保留在服务器 `C:\PMS\.runtime\releases\purchase-report-20260921`，本机发布辅助文件在 `release/`。私密配置通过加密通道单独传输，不进入发布 ZIP 或 Git；临时传输通道已关闭。
- 边界：浏览器当前未登录，正式页面业务交互、金蝶数量人工核对与 OA 嵌入仍待验收。功能分支固定提交已部署，未推送 GitHub、未合并 master。

## 2026-09-21 采购报表 Windows 启动配置接入

- 原因：用户批准将采购报表部署服务器，需要独立只读连接，不覆盖既有 PMS、OA 或金蝶同步凭据。
- 调整：Windows 启动器支持可选 `purchaseConfig` 私密配置路径，仅在启动 PMS 子进程时设置连接配置；配置文件不存在时拒绝启动，并恢复调用者原有环境。
- 文件：`ops/windows/Start-Pms.ps1`、`backend/tests/windows_operations_contract.py`、`docs/采购报表只读账号运维.md`。
- 验证：Windows 运维契约先失败后通过；本条仅记录启动接入完成，服务器发布结果另行记录。

## 2026-09-21 采购报表正式读取器、接口与页面（开发机）

- 原因：用户确认 V2 样稿、正式只读报表方案及持续实施，追加仅查询申请日期 2026-01-01 起的数据（包含当天）。
- 后端：固定参数化 SQL、独立只读连接、申请明细粒度分页，订单/入库/退料分层核对及汇总；列表、明细、候选、导出统一日期和项目权限范围。分批数据不重复累加，单位先乘后除，分类基于未取整基本数量；悬空、NULL 或多申请来源不能证实的数量标待核对，原始合并单据不向其他项目泄漏。
- 页面：接入标准列表、50 条分页、远程模糊候选、申请日期与进度过滤、列布局及个人查询方案；760px 关联明细平铺两张表，鼠标/键盘换行同步切换，取消旧请求且拒绝过时响应。日期起点页面可见。CSV 最多 500 行，按当前权限与查询导出，中文状态、防公式注入和操作日志齐备。
- 权限与目录：新增报表中心及查看/导出权限，菜单自动分配编号避免占用既有菜单；仅首次授权 admin，重启不补回撤权。字段注册表统一供元数据、字段目录和导出使用。数据库版本提升为 `2026-09-21-01`，服务器尚未升级。
- 文件：`backend/app/services/purchase_{reader,fields,migration,progress}.py`、`backend/app/api/purchase_reports.py`、初始化/数据库版本/字段目录/角色首页注册与 `backend/main.py`；`frontend/src/views/reports/`、`src/api/purchaseReport.ts`、路由、列设置操作列锁定、请求取消处理；采购契约及浏览器测试、实施计划和报表说明。
- 验证：报表后端 48 项通过；RBAC、独立数据库升级、字段目录、枚举及操作日志契约通过；前端样式/标准列表/系统 UI/数据字典/枚举/报表联动契约通过，`npm run build` 通过（保留既有大包提示）。Edge 隔离接口验证主从联动、键盘选行、乱序、失败重试、查询方案实际行数及列设置、导出权限；1366/1600 桌面截图检查，390px 抽屉不越界。窄屏主表固定列拥挤测试先失败后修复：表格保持可读宽度并允许横向滚动，页面纵向滚动至分页，不改全局导航。
- 真实只读验证：2026 年范围当次 158092 条，首批 50 条约 0.80 秒；`CGSQ030665` 第 7 行下单 18、入库 8；`CGSQ029837` 第 302 行三次入库 1/3/2 合计 6；`CGSQ029378` 第 43 行入库 3、退料 1、净值 2。新版 SQL 在真实库执行通过，待入库和已完成筛选返回分类一致。实际不同单位换算与异常链路仍需业务人工样本复核。
- 边界：未部署 PMS 服务器、未迁移正式 PMS 菜单、未修改金蝶业务数据、未重启 SQL Server/OA/服务器，未推送或合并。服务器部署等待本批备份校验和独立升级批准；测试通过不代替 OA 嵌入与业务正式验收。

## 2026-09-21 采购报表只读账号现场创建及独立连接

- 原因：用户已批准创建并完成交互输入管理员凭据，继续报表数据接入。
- 现场结果：脱敏执行记录确认 `10.10.1.248/AIS20231211221516` 的 `pms_purchase_reader` 已提交、权限审核及独立登录通过，23 张表指定列只读；没有业务数据写入或服务重启。运行凭据仅在 Git 忽略的本机私密配置中，管理员凭据未保存。
- 程序：新增 `backend/app/services/purchase_connection.py`，独立配置、密码脱敏、限定报表账号、连接/语句超时及目标身份检查，会话结束回滚关闭，默认不自动连接金蝶；将已在本机使用的 `pymssql==2.3.13` 固定到后端依赖清单，服务器仍需发布时检查依赖。
- 验证：新增连接契约 5 项先失败后通过；新模块使用新账号实查采购申请成功。只读补样核对数量 6 的订单分三次入库 1/3/2，发现空单号 Z 状态记录需排除重复累计；退料来源已抽查，单位不同案例仍缺真实样本。数量内核 16 项回归通过。
- 文件：连接模块、`backend/tests/purchase_connection_contract.py`、`backend/requirements.txt`、报表说明、账号运维文档及实施计划。正式读取器、API、菜单、页面和服务器部署仍未完成；未推送或合并。

## 2026-09-21 采购报表最小只读账号创建工具（尚未现场执行）

- 原因：用户批准由本任务创建专用只读账号，管理员凭据由用户本机输入。
- 调整：新增固定目标 `10.10.1.248/AIS20231211221516` 的交互式创建工具，账号 `pms_purchase_reader`；限定 23 张表必要字段的 SELECT，拒绝业务增删改和执行权限；遇到既有账号/配置不覆盖。事务内权限核验通过后提交，再以新身份独立验证。新运行密码随机生成并存入 Git 忽略的 600 权限文件，管理员凭据不保存，异常结果脱敏。
- 文件：`backend/scripts/provision_purchase_reader.py`、`backend/tests/purchase_reader_account_contract.py`、`docs/采购报表只读账号运维.md`。
- 验证：5 项防护测试先失败后通过；授权字段与已取得的实际表结构离线比对无缺失。未实际创建账号，未改变服务器权限，未部署 PMS；现场结果待用户交互输入后核验。

## 2026-09-21 采购报表数量内核与明细联动基础（开发机，实施中）

- 原因：采购报表样稿、正式数量与权限方案获批；用户追加逐字段说明、明细随左侧行选择变化、标题沿用金蝶命名，确认复用现分支。
- 已实现：独立数量汇总内核，使用当前来源量，分别计算下单、实收、入库后退料及差额；对不完整来源、合并申请归属不清、重复、单位不符和未知状态输出异常/空值，不猜分、不掩盖负差额。尚未接入实时 SQL 读取器。
- 已实现：采购明细状态控制器，选行清空旧明细/订单筛选、取消旧请求、抑制过时成功或失败响应、关闭后不重开，支持重试及翻页后清除失效选中行。尚未接入正式页面。
- 文档：新增逐字段报表说明草稿及分阶段实施计划，区分真实抽样证据、模拟测试与尚未验收的分批入库/退料案例。
- 文件：`backend/app/services/purchase_progress.py`、`backend/tests/purchase_progress_contract.py`、`frontend/src/views/reports/purchaseDetailState.ts`、`frontend/tests/purchase-detail-state.test.mjs`、`docs/采购进度查询报表说明.md`、`docs/superpowers/plans/2026-09-21-purchase-progress.md`。
- 验证：测试先失败后实现，后端数量 16 项、前端异步状态 6 项通过；既有档案分页、样式、标准列表、系统 UI 契约与前端构建通过，构建保留既有大包提示。
- 边界：正式 API、菜单/权限迁移、数据读取器和列表页面未完成；没有部署、升级数据库、修改金蝶数据、推送 GitHub 或合并 master。数量测试通过不代表真实单据全场景已验收。

## 2026-09-21 项目档案扩展与条件同步服务器发布

- 原因：用户开发机验收通过，批准部署，并确认数据库已备份、执行独立升级。
- 发布：`10.10.1.228 / C:\PMS` 从 `3938f7c` 更新至 `e753619a`；`10.10.1.230/PMS` 数据库版本从 `2026-09-20-01` 升级至 `2026-09-20-02`。包括产品线、合同及联系字段、地址逐级展示和按编码/名称变化登记同步任务。
- 备份：数据库备份 `E:\PMS-bakup\PMS20260920.bak` 通过 SQL 校验；程序与受保护配置副本位于 `C:\PMS\.runtime\release-history\archive-fields-20260921-112638`。凭据只在服务器交互输入，未写入项目或日志。
- 验证：独立验收 `VERIFY_EXIT=0`，最终 `DEPLOYMENT_COMPLETE / FINAL_EXIT=0`；940 条档案及原有业务数据哈希不变，新增字段未回填历史数据，合同必填生效时间与产品线枚举初始化正确。服务器后端、前端发布文件逐项哈希一致；新接口及 Schema 存在、匿名访问受限；实际网址首页及档案页面资源哈希与开发机匹配，健康接口正常。
- 收尾：发布器停留在启动命令等待阶段，但 PMS 已运行；独立完整验收通过后，仅终止已识别的发布器进程，再次验收并登记完成，未再次重启 PMS。未重启服务器、SQL Server 或 OA，未修改受保护配置，未测试写入真实金蝶。
- 文件：本记录、`docs/项目档案合同与联系信息.md`、`docs/档案自动同步与同步管理.md`；发布工具位于本机 `release/archive-fields-deploy.py`、`release/package-archive-fields.py`，服务器发布状态位于 `C:\PMS\.runtime\releases\archive-fields-20260921\deployment-state.json`。
- Git：已在功能分支固定程序提交，未推送 GitHub、未合并主分支；本次发布记录后补留在开发机。

## 2026-09-21 档案地址逐级展示与按映射字段同步（开发机）

- 原因：用户确认省、市、详细地址逐级展示，编辑仅在项目编码或名称实际变化时同步金蝶。
- 调整：新增和编辑分别根据自身表单值决定地址字段展示；换省清空市及详细地址，换市清空详细地址；取消恢复联动前值，统一保存包含清空字段。保留地址整体可空、填写时必须完整的校验。
- 同步：新建仍登记同步任务；编辑后端比较规范化后的编码、名称实际变化，只有变化才登记任务。仅改其他字段保留原状态、失败原因和排队任务，不自动重试；管理员重试及启停同步保持不变。编辑接口新增 `sync_queued`，前端据此显示不同保存提示。
- 文件：`backend/app/services/project.py`、`backend/tests/archive_business_fields_contract.py`、`frontend/src/views/project/ProjectArchive.vue`、`frontend/tests/archive-field-expansion.test.mjs`、`frontend/tests/archive-field-expansion-browser.mjs`、`docs/项目档案合同与联系信息.md`、`docs/档案自动同步与同步管理.md`。
- 验证：先补测试复现旧行为（本地字段更新仍新增任务、详细地址未清空），实现后后端档案业务 11 项、同步队列 22 项、金蝶查询范围 6 项及生命周期/并发/操作日志回归通过；前端行为及样式/标准列表/系统 UI/抽屉/自动同步 18 项通过。Edge 模拟接口验收新增逐级展开、已有地址展示、Esc 恢复、完整性校验、联动保存及仅保存提示，含桌面/窄屏检查；构建通过，仅既有大包提示。
- 边界：未新增数据库结构、未修改金蝶映射/目标匹配/权限；仅开发机验证并启动本地服务，未部署服务器、未向真实金蝶写入、未推送或合并。

## 2026-09-20 项目档案产品线、合同与联系信息（开发机）

- 原因：用户批准扩展项目档案资料，新建默认当前用户为负责人，新增产品线、合同/出货/质保日期及项目联系地址信息。
- 后端：新增 10 个业务字段；产品线使用独立 `product_line_id` 和数字流水枚举 `product_line`，初始 Bench、Single，不影响产品类别权限。合同签订/出货日期按首次升级时间约束新记录，存量可继续维护；规则重置保留截止时间。地址完整性、省市关联及枚举值在服务端校验，失败不部分保存；沿用原保存与后台同步同事务流程，不改金蝶映射。修复只读日期原值回传因字符串/date 类型不一致被误拒绝的问题。
- 前端：新增档案复用用户 B 统一侧边表单；详情增加合同与交付、项目联系信息分组，使用统一日期/选择/长文本控件，切换省份清空城市、Esc 恢复联动字段；新字段加入列设置和筛选但默认隐藏。保存期间锁定输入并保留失败草稿，取消新增保留未保存确认。省市由同一受控接口提供并显示名称。
- 数据与升级：数据库修订 `2026-09-20-02`，SQLite/MSSQL 兼容可空列迁移；旧语义迁移识别新产品线标记，避免重复升级删错枚举。字段目录、中文日志标签和字段规则同步更新。地区数据为固定提交的 2023 年大陆 31 省级地区离线快照，含来源与许可证，不声称最新或覆盖境外。
- 文件：`backend/app/models/{project,init_db}.py`、`schemas/project.py`、`api/projects.py`、`services/{archive_business_migration,archive_regions,project,enum_registry,field_policy,field_catalog,list_query,database_revision,project_archive_semantic_migration}.py`、`app/data/regions/`；`frontend/src/views/project/ProjectArchive.vue`、`src/api/project.ts`；新增档案业务/地区/前端行为/浏览器测试，更新枚举和抽屉契约；`docs/项目档案合同与联系信息.md`。
- 验证：新增后端 9 项业务与 12 项地区测试、前端 11 项行为测试通过；数据库升级/旧结构/SQLite 初始化、字段目录/枚举/规则、档案语义/生命周期/并发/分页、RBAC/操作日志、金蝶期初/查询范围与 22 项同步队列回归通过。前端样式、标准列表、系统 UI、表单布局/采用、档案筛选/抽屉/生命周期冲突/自动同步、数据字典/枚举契约通过。Edge 模拟接口验证新建与存量必填差异、默认负责人、省市取消与保存、失败草稿、列设置持久化与编码筛选；1366×768、390×844 截图检查通过。`npm run build` 通过，仅保留既有大包提示；`git diff --check` 通过。
- 开发机：升级前 SQLite 备份 `.runtime/backups/archive-business-fields-20260920-before.db` 完整性 ok；升级后档案数量 2→2、版本 `2026-09-20-02`。本机服务重新加载，省市接口拒绝未登录访问；本机后台 ERP 执行器未开启。浏览器测试均使用模拟接口，不写入业务资料。
- 边界：未部署服务器、未连接实际 MSSQL 执行本批迁移、未向真实金蝶写入，未推送 GitHub/合并 master。服务器部署须单独批准升级流程；不可仅回退旧程序并执行旧初始化，详见本批文档。采购报表继续等待业务反馈，未改动样稿。

## 2026-09-20 采购报表样稿独立 HTML 交付

- 原因：用户需将样稿发给业务同事确认，明确暂缓正式报表开发。
- 调整：新增独立导出脚本，将V2样稿、Vue/组件运行代码、字体及样式全部嵌入单个HTML；产物为 `release/采购进度查询-V2-业务确认样稿.html`，无需开发服务。未修改正式业务功能。
- 文件：`frontend/prototypes/purchase-progress/export.mjs`、`check.mjs`、样稿说明及本记录。
- 验证：独立打包完成；Edge在断网模式下从file地址打开实际交付文件，筛选、分批明细及订单联动、空状态、方案持久化、列设置、桌面/窄屏交互测试通过；HTTP/HTTPS及业务API请求数为0。报表任务等待业务反馈，未部署服务器。

## 2026-09-20 采购报表样稿 V2：分批案例与平铺明细

- 原因：用户确认增加分批下单/分批入库案例，并要求简化详情展示。
- 调整：气动隔膜阀样例改为三批订单40/35/25与三筆入库20/15/10；加热器增加部分下单且分批入库案例。详情抽屉约760px，只保留申请身份及订单/入库两张表，支持订单行筛选入库、查看全部和切换申请后清除筛选，去掉数量卡片及嵌套订单卡片。
- 文件：`frontend/prototypes/purchase-progress/App.vue`、`check.mjs`、`docs/prototypes/采购进度查询样稿说明.md`、本记录。
- 验证：Edge 样稿交互检查通过，覆盖3批订单/3笔入库、数量合计、订单筛选/空状态/恢复全部、申请切换、原有筛选与保存方案，以及1600/1366/390宽度；无业务API请求。已比对新版明细截图；本轮未改正式应用入口及共享组件，沿用前批正式应用构建和契约证据。
- 边界：仅更新本机样稿，未连接数据库、金蝶或部署服务器，等待用户确认效果。

## 2026-09-20 采购进度查询交互样稿

- 原因：用户批准先制作首张采购进度报表效果，确认后再实施正式功能。
- 调整：新增独立 Vue 样稿，复用现有主题、字体、标准列表和表单控件；以申请明细为粒度，分组展示请购/订单/入库汇总，提供右侧订单与分批入库追踪、筛选、列设置、本机查询方案和模拟导出。保留正式系统代码及数据不变。
- 文件：`frontend/prototypes/purchase-progress.html`、`frontend/prototypes/purchase-progress/{main.js,App.vue,check.mjs}`、`docs/prototypes/采购进度查询样稿说明.md`、本记录。
- 验证：Edge 交互测试通过，覆盖拆单、分批入库、空状态、模糊搜索、方案保存恢复和列设置；检查 1600×900、1366×768、390×844 布局，无业务 API 请求。样式/标准列表/系统 UI 契约和 `npm run build` 通过，保留既有大包提示；样稿未进入正式构建入口。
- 边界：仅模拟数据效果稿，不含实际金蝶查询、权限、采购业务计算口径或 Metabase 接入；未部署服务器、未推送 GitHub、未合并主分支，等待用户确认。

## 2026-09-20 档案自动同步服务器部署完成

- 原因：用户批准直接部署到已对接金蝶的服务器验收，并明确确认已完成数据库备份、不再重复检查备份。
- 发布：服务器 `10.10.1.228` 的 `C:\PMS` 从 `8a8defc4` 更新到 `3938f7c2b565e238dead774e82accc4d934cbb67`；使用独立升级身份将 `10.10.1.230/PMS` 升级到 `2026-09-20-01`，新增任务表和管理员同步菜单权限。受保护配置开启 `ERP_SYNC_WORKER_ENABLED=true`，原 `K3_READ_ONLY=false` 保持不变；仅重启 PMS 后端，未重启 SQL Server、OA 或服务器。
- 备份：接受用户本次手工备份确认，未重新执行备份或 RESTORE VERIFYONLY；程序和受保护配置备份位于 `C:\PMS\.runtime\release-history\auto-sync-20260920-151350`，私人配置备份目录限制访问。升级凭据仅存在于本次进程内，未写入文件或日志。
- 验证：真实运行身份通过队列表插入/更新后回滚、SQL Server 跨连接锁互斥及释放检查；独立最终核验输出 `VERIFY_OK queued_tasks=0 business_unchanged=true revision=2026-09-20-01`、`DEPLOYMENT_OK`、`VERIFY_EXIT 0`。核对源码版本、发布文件哈希、前端首页、菜单授权、原业务数据、匿名接口拒绝；Mac 独立访问健康接口 HTTP 200/status ok，服务器首页资源与本地发布产物一致。
- 发布收尾：PMS 启动进程已退出且新后端正常运行，辅助部署进程仍等待输出；核对进程身份后只结束辅助部署进程，独立执行完整验证并记录完成状态，未重复升级或重启后端。
- 文件：服务器发布回执 `C:\PMS\.runtime\releases\auto-sync-3938f7c2b565e238dead774e82accc4d934cbb67\deployment-state.json`；本地 `release/auto-sync-deploy.py`、`docs/档案自动同步与同步管理.md`、本记录。
- 边界：任务数为 0，不会自动批量推送历史档案；本次未擅自修改真实项目触发金蝶写入，保存入队、实际金蝶成功和失败重试仍待用户服务器业务验收。未推送 GitHub、未合并 master；已有 OA 登录风险待办未纳入本次修改。

## 2026-09-20 自动同步服务器发布准备与单项目保护

- 原因：用户批准直接部署服务器，强调每次仅操作指定项目，不能影响其他编号。
- 调整：金蝶查询在精确类别/编号、重复记录拒绝的基础上，再校验返回编号与目标相同、内码有效；异常结果不得作为其他项目的更新目标，也不得退化为新增。
- 文件：backend/app/services/kingdee.py、backend/tests/kingdee_query_scope_contract.py、本记录。
- 验证：新增两项测试先失败后通过，6 项查询范围、13 项认证、15 项审核、22 项队列测试通过；未向真实金蝶写入。已只读核实服务器 C:\PMS、提交 8a8defc452fc41ec6c7e1ff3e0292cac9e7b63a1、干净工作区及健康状态，部署尚未执行。

## 2026-09-20 档案保存自动入队与管理员同步管理

- 原因：用户批准保存后后台独立同步，取消维护人员手动同步，管理员单独查询和处理异常。
- 后端：新增持久化任务表、独立修订号 2026-09-20-01、一次性菜单授权、筛选分页与范围受控日志接口；档案保存和入队同事务，旧等待任务被新版本替代。后台条件领取防重复执行；明确连接失败有限重试，不确定结果先回查；重试/核查写操作日志。已写入金蝶及期初档案锁定编号，实际同步中/待核查继续占用保护。
- 前端：取消单条同步、批量同步、保存并同步；新增系统管理/同步管理标准列表及档案只读日志抽屉，权限分别控制查看、重试/核查。状态自动刷新避开编辑、未保存草稿与选择操作。无自定义字段的筛选栏不显示无效“添加筛选”按钮；空同步状态不再误筛为同步中。
- 文件：backend/app/models/erp_task.py、services/erp_queue.py、services/erp_queue_migration.py、api/sync_tasks.py；project/kingdee/list_query/生命周期/字段目录/角色模板/数据库修订与初始化及 main/config；frontend/src/views/system/SyncTaskList.vue、components/SyncLogDrawer.vue、config/syncUi.ts、ProjectArchive.vue、PmsListFilters.vue、菜单图标和路由；对应测试及 docs/档案自动同步与同步管理.md、实施计划。
- 验证：22 项队列行为测试、14 个后端契约脚本及档案分页回归通过；10 个前端契约脚本、Edge 模拟接口浏览器测试通过；类型检查和构建通过，保留既有大包体积提示。浏览器覆盖筛选回首页、50 条分页、日志、重试成功/失败、权限与两种分辨率；没有调用真实金蝶写接口。
- 安全复核补强：新增 erp_execution_lock.py 跨进程档案执行锁，原进程持锁时拒绝恢复/核查；旧超时任务不覆盖最新版本状态。回查发现金蝶资料存在即锁定项目编号；结果不一致默认保持待核查，管理员须明确确认金蝶端请求已结束后才可解锁重试，确认写入审计。SQL Server 会话锁仍需服务器运行身份实测。
- 复核结果：针对三项安全问题复核未发现新增严重问题；22 项队列测试通过，模拟 MSSQL 连接检查了锁获取/释放路径，未替代真实 MSSQL 断连恢复验收。
- 开发机：活动 SQLite 库已先备份校验后独立升级，档案数量 2→2，任务数 0，完整性 ok；PMS 本地后端重启加载，健康接口 ok，新接口拒绝未登录访问。备份位于 .runtime/backups/20260920-auto-sync/。执行器默认关闭，待页面验收及获准的金蝶联调后启用。
- 边界：未部署服务器、未重启数据库/OA/服务器、未推送 GitHub或合并 master；服务器升级和真实金蝶验收尚未执行。

## 2026-09-20 第三批统一表单：角色新增与编辑

- 原因：用户确认部门/枚举值开发机验收通过，继续已批准的系统表单分批迁移。
- 调整：角色表单改用 PmsFormDrawer，字段采用单列标签/控件布局，权限树与字段共用内容滚动区，保存按钮固定底部；保存期间阻止再次提交及修改，失败保留草稿。未改权限勾选规则、角色 API、数据范围或登录首页计算。
- 文件：frontend/src/views/system/RoleList.vue、frontend/tests/role-drawer-browser.mjs、docs/superpowers/plans/2026-09-18-user-parameters-security.md、本记录。
- 验证：新增浏览器测试先在旧弹窗上失败、迁移后通过，覆盖新增/编辑、必填、失败重试、权限与首页选项联动、原字段保留及 1366×768 / 1600×900 布局；既有 role-home-browser.mjs 回归通过，8 个风格/表单/RBAC/首页契约脚本通过，vue-tsc 和构建通过。浏览器接口均为本机模拟，未写真实角色权限；构建保留既有包体积提示。
- 状态：开发机实施完成，等待第三批用户验收；未部署服务器、未推送 GitHub、未合并主分支。

## 2026-09-20 第二批统一表单：部门与枚举值

- 原因：用户验收用户 B / 参数 A 后要求继续，按已批准的分批迁移方式统一其他系统表单。
- 调整：部门、枚举值新增和编辑复用 PmsFormDrawer，固定头部和保存区，使用既有统一控件；保存期间禁止重复提交和修改，失败保留输入。枚举保存绑定打开时的分类；顶级部门不再显示内部值 0。
- 共享适配：统一抽屉中的 Element 表单校验容器行距，保留错误提示空间，未修改列表行内编辑或业务接口、权限、数据库结构。
- 文件：frontend/src/views/system/UserList.vue、EnumList.vue；frontend/src/form-system/components/PmsFormDrawer.vue；frontend/tests/system-form-drawer-browser.mjs、user-parameters-browser.mjs；实施计划及本记录。
- 验证：8 个前端契约脚本通过；部门/枚举 mock 浏览器测试通过，检查 1366×768 与 1600×900 的排版、必填、失败重试和保存请求；原用户/参数浏览器回归通过（测试定位改为用户字段所在抽屉，避免匹配隐藏的部门抽屉）；vue-tsc 与构建通过，仍有既有大包体积提示。仅开发机完成，未部署服务器、未推送或合并 GitHub，等待本批验收。

## 2026-09-20 OA 员工视图只读授权完成

- 原因：按用户明确批准，解除“引用 OA”的 SQL Server 916 访问阻塞。
- 调整：在 10.10.1.230 的 ecology 库为现有登录 pms_app_runtime 创建同名用户映射，授予 CONNECT 与 dbo.Jinky_Employee 对象 SELECT；未授予全库 SELECT、CONTROL 或视图 INSERT/UPDATE/DELETE，未修改 OA 登录流程。
- 验证：授权事务及权限检查通过，GRANT_EXIT 0；提交后使用真实 PMS 运行连接查询成功，过滤已有账号后有 447 个候选，抽查 20 条，工号和姓名模糊搜索检查通过。服务健康返回 ok。未新建 PMS 用户、未写 OA 业务数据、未重启任何服务。
- 回执：C:\PMS\.runtime\permission-changes\oa-reference-20260920\grant-receipt.json，committed=true、user_created=true；授权前记录为同目录 before.json。页面选择回填及用户新增流程仍待用户实际验收。
- 文件：docs/用户与参数设置.md、docs/superpowers/plans/2026-09-18-user-parameters-security.md、本记录；授权辅助脚本位于忽略目录 release/。

## 2026-09-20 修正 OA 授权检查的身份恢复顺序

- 原因：授权事务中切换至 PMS 查询后直接 REVERT，SQL Server 返回 15199，要求先返回 EXECUTE AS 所在数据库。
- 调整：检查收尾先 USE ecology，再 REVERT；新增顺序回归测试。涉及忽略目录 release/grant-oa-reference.py、release/test_grant_oa_reference.py 及本记录。
- 验证：新增测试先失败后通过，4 项本地测试通过。服务器确认 HAS_DBACCESS('ecology') 仍为 0、无提交回执；修正版哈希验证通过，重新等待管理员输入，未扩大权限或重启服务。

## 2026-09-20 准备 OA 视图最小权限授权

- 原因：用户批准为 PMS 运行身份增加指定 OA 员工视图的只读访问。
- 调整：准备事务化授权脚本，仅建立必要用户映射、CONNECT 和 dbo.Jinky_Employee 的 SELECT；检查无全库 SELECT/CONTROL 和视图写权限，验证业务查询后提交。脚本传至 C:\PMS\.runtime\permission-changes\oa-reference-20260920，未改应用代码或重启服务。
- 文件：忽略目录 release/grant-oa-reference.py、release/test_grant_oa_reference.py、传输辅助脚本及本记录。
- 验证：本地 3 项辅助安全测试通过；服务器只读核对账号 pms_app_runtime、PMS 库及 OA 不可访问状态；脚本哈希匹配，当前等待管理员凭据，尚未执行数据库授权。临时传输服务已关闭。

## 2026-09-20 同步用户与参数实施计划状态

- 原因：实施计划仍保留部署前状态，与已完成的服务器测试部署记录不一致。
- 调整：补充 2026-09-18 服务器部署完成项，明确 OA 只读权限待批准、真实用户验收待完成，以及 OA 登录安全风险仍阻塞正式上线。
- 文件：docs/superpowers/plans/2026-09-18-user-parameters-security.md、本记录。
- 验证：与已有发布回执、使用说明核对；未修改业务代码、数据库权限或服务器配置。

## 2026-09-18 用户与参数第一批服务器部署完成

- 原因：按用户授权将已验证的用户 B、参数 A 和密码管理第一批部署到 10.10.1.228 供测试。
- 调整：服务器代码更新为 8a8defc452fc41ec6c7e1ff3e0292cac9e7b63a1；通过独立身份和 upgrade_database.py 将 10.10.1.230/PMS 升级到 2026-09-18-03，发布前端并启动 PMS。未重启 SQL Server、OA 或整机；未推送 GitHub或合并 master。
- 备份：程序备份 C:\PMS\.runtime\release-history\user-security-20260918-165509；数据库备份 E:\PMS-bakup\PMS-preimport-20260917-173045.bak 已通过 RESTORE VERIFYONLY。
- 验证：服务器 deploy.py --verify 退出码 0；运行源码及前端资源哈希、实际首页、数据库结构与版本、新接口、匿名参数访问拒绝、密码组件验证通过，既有业务数据哈希未变。启动子进程退出后部署监控未结束，仅清理已识别的部署 Python 进程；独立再次验证成功后记录 complete，RECOVERY_EXIT 0。Mac 到服务器健康接口返回 ok。
- 待办：运行身份读取 ecology.dbo.Jinky_Employee 返回 SQLSTATE 08004 / SQL Server 916，无法访问 OA 数据库，引用 OA 尚不可验收；没有自动授权。用户初始密码参数仍未设置，须管理员在页面维护。既有 OA 身份凭证校验风险仍按用户决定暂缓，未修复。真实用户业务验收待用户执行。
- 文件：本记录、docs/用户与参数设置.md；服务器发布包及忽略目录 release 下部署辅助文件。

## 2026-09-18 服务器密码组件改用离线安装

- 原因：升级身份及 SQL 备份校验通过后，服务器 pip 外网下载长时间未完成，部署仍停留在 prepared 阶段。
- 调整：从包注册源下载 Windows Python 3.13 适用组件，校验传输包 SHA256 后安装 argon2-cffi 23.1.0 及 bindings 26.1.0；保留服务器已有 cffi/pycparser。仅停止本次部署的 pip 子进程，未停止 PMS、SQL、OA 或服务器。恢复命令设置 PIP_NO_INDEX 和本地 wheels 路径，避免再次外网下载。
- 文件：忽略目录 release/prepare-user-security-wheels.py、release/serve-user-security.py、离线 wheel 包；本记录。
- 验证：服务器安装退出码 0，加密哈希生成与验证及 PMS 健康检查通过，恢复前检查通过。数据库备份为 2026-09-17 17:30 的 PMS-preimport 文件，本次 RESTORE VERIFYONLY 已通过。尚未执行结构升级和程序切换；重新等待独立升级凭据，临时传输服务已关闭。

## 2026-09-18 修复发布脚本备份查询并恢复升级前流程

- 原因：输入升级凭据后在 prepared 阶段出现 ProgrammingError；只读检查确认 SQL Server 备份元数据字段为 media_family_count，原脚本错误引用 family_count。
- 调整：修正备份查询字段并保留结果别名；新增仅允许 prepared 阶段的恢复入口，重新校验版本、业务数据、配置、暂存源码和发布包，不跳过备份校验、不删除部署状态。涉及忽略目录 release/user-security-deploy.py、release/test_user_security_deploy.py 和本记录。
- 验证：16 项离线测试通过；服务器健康正常，数据库仍为 2026-09-18-02。修正版包校验及恢复前检查通过，远程重新等待独立升级凭据；未停 PMS、未执行数据库升级或发布。临时文件传输服务已关闭。

## 2026-09-18 用户与参数服务器包就绪，等待升级凭据

- 调整：在既有功能分支固定提交 `8a8defc452fc41ec6c7e1ff3e0292cac9e7b63a1`，重新构建生产包并传至服务器 `C:\PMS\.runtime\releases\user-security-8a8defc452fc41ec6c7e1ff3e0292cac9e7b63a1`。未推送 GitHub、未合并 master。
- 文件：忽略目录 `release/user-security-deploy.py`、部署脚本测试、固定 manifest、离线 Git bundle、生产 ZIP 及临时传包脚本；本条记录。生产包 SHA256 为 `a775f24f1c2ccaf991bf7df863a16f39a2ab5809eb949ec1548ee668ba09e323`。
- 验证：固定提交的 vue-tsc/Vite 构建通过；发布脚本 14 项测试通过，包内 248 项文件哈希通过。服务器版本/工作区、生产库目标与 revision02、运行健康、无待执行 ERP 同步及包校验已通过；程序备份完成于 `C:\PMS\.runtime\release-history\user-security-20260918-165509`。
- 当前状态：远程终端停在 `SQL upgrade username`，等待用户输入独立升级账号及隐藏密码。数据库备份尚待此身份查询及 RESTORE VERIFYONLY 校验；没有停服务、升级数据库或切换正式程序。临时传输服务已关闭。OA 遗留风险继续保留，不作为安全验收通过。

## 2026-09-18 用户与参数服务器发布准备

- 原因：用户知悉遗留 OA 身份校验风险后，明确要求服务器部署测试，并批准数据库升级；不包含 SQL Server、OA 或整机重启。
- 调整：更新本批实施计划和使用说明的发布边界，保留 R1 未解决记录；准备从当前已验证源码制作确定提交的发布包。部署成功后另记实际回执。
- 涉及文件：`docs/superpowers/plans/2026-09-18-user-parameters-security.md`、`docs/用户与参数设置.md`、本条记录。
- 验证：远程只读核对服务器基线 `ddc405cfa33b166521efe159a045ba8c2aff5e62`、工作区干净。尚未切换程序或升级数据库；数据库备份须在独立升级身份下校验。

## 2026-09-18 用户 B、参数 A 与本地密码管理第一批

- 原因：按用户批准的参数 A / 用户 B 样稿统一新增编辑布局，取消创建用户时人工输入密码，提供 OA 员工引用和独立密码重置。
- 调整：新增可复用 `PmsFormDrawer`；用户表单改为 492px 右侧抽屉，账号（工号）、员工姓名、邮箱等统一排版；OA 模糊查询过滤已有账号后分页；参数设置维护注册参数，初始密码只存哈希且不回显。新增邮箱、首次改密、凭据版本、独立重置权限和失败次数限制；本批不迁移其他系统表单。
- 文件：`frontend/src/form-system/`、用户/参数/改密页面、认证 API/路由/状态；`backend/app/` 下用户模型与 Schema、参数服务、认证授权、OA 人员查询、迁移和字段目录；相关契约及浏览器测试；`docs/用户与参数设置.md`、本批实施计划、UI 标准及 `AGENTS.md`。
- 验证：前端 28 契约脚本（32 断言）、mock 浏览器交互与 1366/1600 布局、角色首页回归、类型检查及构建通过。后端 29 契约脚本通过；最后定点修正后专项 36 项及 RBAC 回归通过，覆盖旧会话并发改密、双向旧状态禁用、日志失败回滚、SQLite 升级中断重试、禁用角色保留，以及改密 UPDATE 的 MSSQL 离线编译。既有大包提示和 Python UTC 弃用警告仍存在。
- 本机：已备份实际 SQLite 库并升级为 `2026-09-18-03`；用户、档案、项目数量保持 1/2/1，初始密码参数未设置。真实 OA 视图与 MSSQL 尚未联调，不能用 mock 测试替代生产验收。
- 运行：重启本机 PMS 后，5174/8000 前后端源码目录及持续存活检查通过，代理健康接口 200/ok，运行 OpenAPI 确认新接口和无密码的创建请求。启动输出提交号仍为未提交改动前基线，不等于三端版本一致。
- 复审：前端三项问题及后端 R2–R6 已关闭；R1 为用户明确暂缓的既有 OA 身份校验问题，未修改其接口行为。
- 安全边界：复核发现既有 OA 回调身份绑定缺陷，用户明确暂不修改、单列发布安全阻塞；本批不宣称整个认证链路安全通过。未部署服务器、未重启共享数据库或服务器、未提交推送或合并。

## 2026-09-18 用户与参数 Batch 1 前端审查修复

- 原因：用户草稿缺少路由离开保护，表单输入缺少错误/必填关联及首错焦点，参数 A 缺少独立分组筛选和新密码显示切换。
- 调整：三个页面复用统一控件 scoped slots 绑定可访问语义，nextTick 后聚焦错误；用户抽屉共享关闭/路由草稿确认；参数新增分组选择与仅新输入值的显示/隐藏按钮，已存密码不回显。
- 涉及文件：`frontend/src/views/system/UserFormDrawer.vue`、`frontend/src/views/system/ParameterList.vue`、`frontend/src/views/ChangePassword.vue`、`frontend/tests/user-parameters-browser.mjs`、`frontend/tests/user-parameters-contract.test.mjs`、`release/user-settings-design-20260918/frontend-fix-report.md`、`change.md`。
- 验证：TDD 新增 5 项契约先失败后通过；全 API mock 浏览器验证草稿后退确认、输入语义/焦点、分组搜索、密码显示边界，以及重置专用权限和重置不暗中保存资料；style/list-standard/system-ui-consistency 契约与 npm run build 均退出 0。详细命令和边界见修复报告。
- 范围：未修改后端、表单容器或 UserList，未接触生产、提交、推送、合并或部署；保留现有 Vite 5174 运行。

## 2026-09-18 合并列设置服务器部署完成

- 发布：服务器 `C:\PMS` 在原功能分支从 `aa66737` 快进到 `ddc405cfa33b166521efe159a045ba8c2aff5e62`；仅替换前端静态资源，保留旧哈希资源供已打开页面使用，入口文件最后切换。
- 备份：`C:\PMS\.runtime\release-history\column-settings-20260918-140106`，发布包位于 `.runtime\releases\column-settings-ddc405cf`。
- 验证：服务器回执 `DEPLOYMENT_OK`、工作区干净、静态文件哈希一致；Mac 独立读取服务器入口及 JS/CSS 共 61 项与发布包一致，健康接口返回 ok。数据库未修改，所有服务均未重启，临时传输服务已关闭。
- 预检处置：Git 中文路径转义导致范围检查首次误判，尚未切换程序；仅在重试进程设置 `core.quotepath=false` 后原范围校验通过，未修改全局 Git 配置。
- 边界：没有推送 GitHub或合并 master；服务器真实业务数据未做写入，页面由用户刷新后验收。

## 2026-09-18 合并列设置与显式保存

- 原因：显示字段与布局、列宽分散在两个页签，操作缺少统一保存入口。
- 调整：项目档案与项目进度共用单一列设置面板，合并显示勾选、冻结区域、列宽和排序；未显示字段按业务分组收起。底部保留恢复默认、取消、保存；面板编辑只改草稿，保存统一应用，取消或关闭丢弃草稿，表头直接拖动继续自动保存。
- 修复：恢复默认使用页面原始列定义，避免 AG Grid 当前列状态污染默认值；项目档案身份列的隐藏状态可正确恢复；动态总表列先创建再应用布局。
- 文件：两列设置组件、项目档案/进度页面、列布局/分页/标准列表测试、UI 标准、AGENTS.md 与服务器发布说明。
- 验证：27 项前端契约通过，vue-tsc 与生产构建通过；Edge 浏览器验证保存前不生效、取消、字段显示/隐藏、动态字段、冻结、排序、列宽、刷新持久化、分页筛选及左右 1px 分隔线。构建仍有既有大包体积提示。
- 发布边界：仅前端更新，不修改数据库、不重启任何服务；服务器部署结果完成后另记。未推送 GitHub、未合并 master。

## 2026-09-18 列布局与角色首页正式部署完成

- 发布：服务器 `C:\PMS` 同一功能分支由 `6c36d58` 快进至 `aa6673723c99d71124b629b643647031b7af47c5`，数据库经独立升级身份升级至 `2026-09-18-02`，仅重启 PMS 后端；未重启 SQL Server、OA 或 Nginx，未推送 GitHub 或合并 master。
- 备份：已验证 `E:\PMS-bakup\PMS-preimport-20260917-173045.bak` 可读取；程序备份及发布回执在 `C:\PMS\.runtime\release-history\role-home-20260918-131445`，初次停机前业务基线保留于同级 `role-home-20260918-130823`。
- 验证：独立核验返回 `DEPLOYMENT_VERIFIED`，服务器工作区干净，后端/前端/运维文件 SHA256 与发布包一致，数据库新增列可查询，真实运行的角色接口包含首页两字段，健康接口正常；项目档案、项目进度和角色菜单授权数据哈希与原停机前基线一致。运行配置校验通过且发布过程中未变更。
- 故障处置：首次升级被旧迁移的期初名称校验阻断，修复后执行完整升级成功；启动脚本结束后部署包装进程仍等待输出管道，使用独立核验形成回执后仅关闭该包装进程，运行服务不受影响。临时传输服务已关闭。
- 边界：本次未修改真实角色首页配置、未执行金蝶业务同步；页面交互由用户刷新后验收。

## 2026-09-18 修复期初导入后再次升级的兼容性

- 原因：生产发布在旧语义迁移校验处失败；只读复现确认第 5 条档案的空名称触发旧规则，角色新增列尚未执行，数据库处于 upgrading，PMS 后端已停止。
- 调整：旧迁移允许 `kingdee_initial` 来源保持空名称，取消名称重复判定，仍校验项目编码和序列号唯一、普通档案名称非空；数据库版本提升为 `2026-09-18-02`。
- 文件：`backend/app/services/project_archive_semantic_migration.py`、`backend/app/services/database_revision.py`、`backend/tests/legacy_schema_upgrade_contract.py`。
- 验证：新增期初空名称及同名档案重复升级测试先失败后通过；旧库迁移、数据库升级、期初导入、角色首页、项目档案语义五项契约通过，diff 检查通过。生产恢复结果待后续实际执行记录。

## 2026-09-18 角色首页与列布局服务器发布准备

- 原因：用户批准将本批开发结果部署服务器，并确认数据库备份与当前 PMS 维护窗口。
- 调整：本地提交 `f179c5e8`，按该提交重新构建生产包；传至 `C:\PMS\.runtime\releases\role-home-f179c5e8`，程序备份位于 `C:\PMS\.runtime\release-history\role-home-20260918-121535`。部署器仅使用交互输入的独立数据库升级身份，不保存密码。
- 涉及文件：忽略目录 `release/role-home-deploy.py`、`release/serve-role-home.py`、发布 ZIP/Git bundle，以及服务器上述准备目录；本条记录。
- 验证：发布包构建、SHA256、ZIP 完整性通过；服务器基线 `6c36d58`、工作区干净、生产目标 `10.10.1.230/PMS`、当前数据库版本 `2026-09-17-02` 及服务健康预检通过；程序备份已完成，临时传输服务已关闭。
- 当前边界：停在 `SQL upgrade username` 输入处，等待用户在远程终端输入。尚未停服务、升级数据库或覆盖正式程序；完成后另记实际部署结果。未推送 GitHub、未合并 master。

## 2026-09-18 清除冻结分隔线重复边框

- 原因：冻结数据容器与边界单元格同时绘制边框，操作列还额外绘制不同颜色的左边框，导致左右分隔观感不一致。
- 调整：表头由冻结表头容器绘制分隔；数据行仅由 AG Grid 标记的边界单元格绘制。移除数据容器边框及操作列额外边框，两侧统一 1px `--pms-frozen-divider`，保留内部普通列边框及选区样式。
- 文件：`frontend/src/styles/pms-theme.css`、`frontend/tests/grid-layout-browser-helper.mjs`、`docs/PMS-UI-STANDARD.md`。
- 验证：新增测试先捕获数据容器重复边框失败，修复后通过。两项目页面左右表头/数据边界宽度及颜色一致；列宽拖动、冻结排序保存、分页筛选回归通过，27 项前端契约及类型检查/构建通过，已检查 1600px 截图。
- 范围：仅开发机，服务器和数据库均未改动，未推送 GitHub。

## 2026-09-18 分隔线细化与数字控件按钮重叠修复

- 原因：三段冻结区域 2px 分隔偏重；统一表单将数字输入框内边距覆盖为普通文本内边距，未预留加减按钮位置，首页优先级数字与减号重叠。
- 调整：分隔线降至 1px，颜色不变；统一数字控件新增按钮宽度令牌与按模式区分的内边距，覆盖紧凑/标准、有按钮/无按钮/右侧按钮、禁用和只读，不增加角色页面局部补丁。
- 文件：`frontend/src/styles/pms-theme.css`、`frontend/src/form-system/form-tokens.css`、角色首页/列布局浏览器测试、新增 `frontend/tests/number-control-browser.mjs`、`docs/PMS-UI-STANDARD.md`。
- 验证：角色页面布局测试先重现输入区两侧各重叠约 23px，再通过修复；十种控件尺寸/状态几何测试通过，0/999 边界、直接输入、加减操作和保存刷新回显通过。列表浏览器回归、27 项前端契约、类型检查及构建通过（仅原有大包提示）。已查看数字控件状态截图。
- 范围：仅开发机更新，无服务器部署、数据库改动或 GitHub 操作。

## 2026-09-18 列冻结排序列宽与角色登录首页（开发机）

- 原因：列设置只能控制显隐，操作列填充与用户认可的方案 A 不符；登录入口统一进入仪表盘，不能适应角色工作场景。
- 调整：共享列设置新增“布局与列宽”，支持左侧冻结/中间滚动/右侧冻结、拖动/上下排序和数字列宽；表头拖动与面板共用原账号/浏览器偏好，恢复默认同时重置布局。操作列取消独立填充，维持 112px 标准；三段之间采用 2px 浅灰分隔。勾选列、操作列固定，不允许移出各自边缘。
- 调整：角色新增登录首页菜单与优先级（0-999）。后端只接受该角色有查看权限的有效页面；多角色按优先级降序、角色 ID 升序解析，失效时回退可访问页面，否则 403。普通、OA、自动登录共用首页解析，有效业务深链接优先；不改变 SSO 凭据验证和 RBAC 授权。角色保存沿用统一日志，字段目录自动收录新字段。
- 文件：`PmsGridLayoutEditor.vue`、`PmsListColumnPicker.vue`、两项目列表、`pms-theme.css`、`RoleList.vue`、路由及四个登录页面、`loginDestination.ts`；后端角色模型/Schema、`role_home.py`、角色/认证服务、初始化和数据库版本；相关测试及 `docs/PMS-UI-STANDARD.md`。
- 验证：28 项前端契约/登录目标测试、`vue-tsc`/生产构建通过（保留原有大包提示）；Edge 浏览器验证列宽输入/鼠标拖动、冻结、拖放、排序、刷新恢复、恢复默认及分页筛选回归；普通/JWT/OA/免密入口、深链接、角色保存、无权首页回退通过。后端首页、数据库升级、RBAC、日志、字段目录、枚举、SQLite 初始化七项契约通过；补充角色保存、非法首页回滚、撤权清空和日志差异检查通过。
- 本机：升级前备份 `.runtime/data/pms-dev-before-role-home-20260918.db`；开发 SQLite 升级到 `2026-09-18-01`，仅重启本机后端。健康接口正常，运行中 OpenAPI 已包含角色首页字段。
- 复查：修正首页自动回退顺序，按菜单祖先与同级顺序选择页面，避免不同目录的页面排序号互相干扰；新增跨目录回退测试，先重现失败后通过。
- 发布边界：服务器未部署、未升级数据库；新模型发布前必须单独确认维护窗口和数据库备份。未推送 GitHub、未合并 master。

## 2026-09-18 列布局与操作区交互样稿

- 按批准的样稿阶段新增 `docs/prototypes/list-layout-v2.html`，独立展示轻分隔、淡中性色、轻按钮三种方案，以及正常/悬停/选中/只读状态；提供左侧冻结、中间滚动、右侧冻结、字段显隐、拖动/上下调整和恢复默认。
- 样稿偏好使用独立 localStorage 键，不读取真实业务数据、不修改正式页面或服务器；角色登录首页与优先级仅作交互示意，不写入角色配置。
- 验证：本机 Edge 自动检查冻结位置、上下排序、刷新恢复、恢复默认、风格切换、角色面板均通过，并查看 1366px 截图；700px 截图已生成。内置浏览器拒绝打开 file URL，因此交付本地 HTML 文件，未绕过浏览器安全限制。样稿等待用户选择视觉方案后再进入正式实现。

## 2026-09-18 统一项目操作列尺寸与编辑入口

- 发布收尾：服务器同分支快进到 `6c36d583cc03f7a7713b845e40d1195d9a446262`，备份 `C:\PMS\.runtime\release-history\action-columns-20260918-105654`；发布器报告成功并校验前端清单，Mac 独立检查首页与入口资源一致、健康接口正常。未改数据库、未重启服务、未推送 GitHub 或合并 master；临时传输服务已关闭。
- 原因：上一批只统一颜色与按钮样式，档案仍为 112px、进度为 88/60px，且进度残留 4px 内边距，造成按钮位置不一致。
- 调整：两页复用 `PMS_ACTION_COLUMN` 固定 112px；共享令牌统一左右内边距 8px、按钮 40×24px、间距 8px、按钮组左对齐。清除进度局部间距覆盖，恢复列偏好时约束宽度和固定位置。进度“详情”改为“编辑”，无编辑权限为“查看”，不改变抽屉和权限逻辑。
- 文件：`frontend/src/config/listUi.ts`、`styles/pms-theme.css`、`views/project/ProjectArchive.vue`、`views/project/ProjectList.vue`、列表浏览器与导航契约测试、`docs/PMS-UI-STANDARD.md`。
- 验证：浏览器测试先重现档案内边距为 10px（预期 8px）失败；修复后确认两页列宽/内边距/按钮宽高/水平位置完全一致，包含无编辑及删除权限的只读状态，分页和筛选保存回归通过。26 项前端契约及生产构建通过，构建仅有原有大包体积提示。

## 2026-09-18 列表自适应版本服务器发布

- 发布：服务器同一功能分支由 `9a770fcd` 快进到 `d7d0e57599c4cff773fc2212f1d8b783f2b676d4`，部署前后工作区干净。仅更新前端源文件、静态资源和相关文档/测试，未推送 GitHub 或合并 master。
- 备份：`C:\PMS\.runtime\release-history\list-density-20260918-104446`；发布包在 `C:\PMS\.runtime\releases\density-d7d0e575`。保留旧散列资源供已打开页面使用，新资源先发布、首页最后替换。
- 验证：部署脚本报告 `DEPLOYMENT_OK`，清单内前端文件校验通过；Mac 独立获取服务器首页及入口 JS/CSS，与本机字节一致，健康接口返回 `ok`。没有数据库变更，没有重启后端、Nginx 或 SQL 服务；临时传输服务已关闭。真实业务页面视觉效果待用户刷新验收。
- 涉及文件：忽略目录下的一次性 `release/density-deploy.py`、打包文件和传输脚本，以及本条发布记录。

## 2026-09-18 列表自适应高度、统一分页与操作列

- 原因：列表固定高度造成底部空白，分页默认值不一致，首次全文提示延迟过长，档案行内操作过密。
- 调整：共享列表改为剩余高度布局与内部滚动；档案、进度、用户、数据字典、操作日志默认 50 条；角色、枚举、字段规则、任务列表同步适配高度，不新增分页。查询、排序、保存和分页联动保持原实现。
- 调整：统一 AG Grid/Element Plus 表格全文提示为 200ms；档案行仅保留编辑/查看和同步，顶部批量操作保留；档案与进度操作列统一按钮尺寸、间距和冷灰背景。
- 文件：`frontend/src/config/listUi.ts`、`App.vue`、`main.ts`、`components/PmsDataList.vue`、`styles/pms-theme.css`、对应项目/系统列表页面；`frontend/tests/list-density-contract.test.mjs`、分页浏览器测试及相关既有契约；`docs/PMS-UI-STANDARD.md`。
- 验证：26 项前端契约通过；浏览器以 940 条档案和 2040 条进度模拟数据验证分页、筛选、排序、保存、过期响应、错误重试和长文本边界；1366×768、1600×900 截图与六个系统页面高度检查通过。生产构建通过；仅有原有大包体积提示。本批无后端或数据库修改，部署结果另记。

## 2026-09-18 列表修复部署与远程输入诊断

- 诊断：Mac Finder 自动键盘输入正常，Windows App 远程鼠标正常；切换连接键盘模式为 Unicode 后可输入普通文字，但部分标点和功能键仍丢失。采用 UTF-16LE 编码的 PowerShell 命令及文字换行后恢复可验证的远程命令执行；这确定了本次可用的输入方式，不将更底层工具原因描述为已证实。
- 发布：按用户批准，从服务器干净的 `d300b413` 在同一功能分支快进到 `9a770fcd36074d28af6bffeb39b13bcb52f28204`。备份目录 `C:\PMS\.runtime\release-history\pagination-20260918-100805`，包位于 `C:\PMS\.runtime\releases\pagination-9a770fcd`。未修改数据库结构、业务数据、受保护配置或 SQL 服务，未推送 GitHub 或合并 master。
- 收尾：部署器启动子进程后等待输出管道结束，PMS 已正常运行；停止确认身份的部署器进程，不停止新版 PMS，改为独立执行校验。后续本地一次性部署脚本改用临时文件接收子进程输出，避免长期服务持有管道造成等待。
- 验证：服务器只读验证退出 0，启用档案 940 条，两页各 15 条且 ID 不重复，筛选通过；首个服务内查询约 137ms（非浏览器端到端耗时）。服务器 Git 工作区干净，新 API 含 filters/sort/archive_id；健康接口 HTTP 200，前端首页 SHA256 与本地构建一致，服务器登录页正常显示。业务页面人工视觉验收仍待用户登录后完成。
- 涉及文件：本次源码版本沿用上一批列表修复；本地 `release/pagination-deploy/deploy.py`、`verify.py`、`Deploy.cmd`、`release/serve-pagination.py` 为一次性运维产物，临时传输服务已关闭。本次没有新增 SMB 共享或更改共享权限。

## 2026-09-18 列表修复发布包准备（服务器部署待继续）

- 后续准备：用户手动执行只读命令后，远程画面确认服务器仍为 `d300b413c69a4c3f22c2f5b6c4e68dffde1e2977` 且工作区干净。新增本地 `release/PMS-pagination-deploy-9a770fcd.zip` 一次性部署工具，包含版本/哈希/ERP 占用校验、项目内程序备份、同分支快进、前端替换、配置保留、健康与真实档案分页只读验证，以及失败时程序回退。Python 语法、发布 ZIP 和源码 bundle 哈希、发布清单全部文件哈希校验通过；Windows 现场执行与验收仍待用户运行，不宣称已部署或回退演练通过。
- 原因：用户要求使用服务器完整数据验收，已批准部署上一批列表修复。
- 完成：在现有 `codex/pms-db-permission-split` 分支提交 `9a770fcd`；使用标准发布工具从该提交重建前端并生成发布包及源码增量 bundle，未推送或合并主分支。
- 文件：`release/pms-server-9a770fcd-20260918.zip`、同名 `.sha256`、`release/pms-source-9a770fcd.bundle`（本地发布产物）。ZIP SHA256 为 `3507f8092d4d2708a2de4ea95e7113d9ad7b9ba8486f2502c560e5d76156c68b`。
- 验证：发布构建退出 0；服务器 `/api/health` 返回 `ok`。Windows App 已连上 PMS，但远程键盘输入未送达，尚未完成运行路径核对，未停止服务或覆盖服务器文件；已请用户恢复远程输入后继续。此记录不代表部署完成。

## 2026-09-18 列表分页、查询联动及长文本修复（开发机）

- 原因：档案固定请求 1000 条且未分页，逐行查询人员导致大量 SQL；长名称挤出 AG Grid 列宽。用户要求查询、布局保存与分页整体联动，并检查其他列表。
- 调整：档案使用真实服务端分页、白名单筛选/排序、人员批量读取、按 ID 刷新详情；恢复个人列排序后查询。查询变化清空勾选，保存保留条件，末页回退，旧响应失效，加载失败可重试。项目进度一次读取权限范围内完整结果并由 AG Grid 先排序再分页，避免跨批次记录移动造成漏项；操作日志自定义条件移到 SQL 分页前；用户和数据字典补请求竞态及 loading 收尾；共享表格值容器限制宽度并省略长文本。
- 涉及文件：`backend/app/services/list_query.py`、`project.py`、`operation_log.py`，对应项目/日志 API；`ProjectArchive.vue`、`ProjectList.vue`、`OperationLogList.vue`、`UserList.vue`、`DataDictionaryList.vue`、`pms-theme.css`；新分页后端/前端/浏览器测试，已有抽屉初始化契约，`docs/PMS-UI-STANDARD.md`、`docs/records/PMS-列表分页验收-20260918.md`。
- 验证：全部后端和前端契约通过，`npm run build` 通过。1040 条 SQLite 测试集查询每页 15 条时为 6 次 SQL（修复前无人员样本即 50 次）；约 4ms 仅代表本地测试。Edge 模拟浏览器验收 940 条档案及 2040 条项目进度、排序恢复、保存联动、过期响应、失败重试与末页回退；1366×768/1600×900 长名称列内省略通过。
- 边界：开发机一键启动检查确认后端 8000、前端 5174 来源正确，使用本地 SQLite；自动化临时服务结束后需用户双击启动器进行人工验收。未部署服务器、未修改正式数据、未提交推送或合并。项目进度保留全数据客户端动态筛选，不宣称其全量加载成本已消除。

## 2026-07-20 - 一键启动器版本一致性修复

- 重构根目录 `start-pms.command` 为稳定启动入口，通过 `.runtime/active-source` 明确选择前后端代码源；启动时打印代码路径、分支、提交号和数据库路径，配置失效时明确失败，不再静默回退旧版本。
- 启动器会检查 `8000/5174` 监听进程的工作目录和受管 PID；仅复用来源一致的进程，旧版本、手工进程或过期 PID 会先被停止再从当前代码源启动。
- 进程归属校验同时核对代码目录、实际监听 PID 和启动提交号；同一工作树切换提交后也会自动重启，避免继续复用旧代码进程。
- PID 文件改为记录实际监听进程，新增无副作用的 `PMS_STATUS_ONLY=1` 状态模式，日志统一写入根目录 `.runtime/logs`。
- 增加原子启动锁，重复双击会串行复用同一组服务；健康检查失败或启动被中断时，只清理本次拉起的临时进程和标记，成功后在终端等待关闭前主动释放锁。
- 本地 SQLite 数据迁移至工作区无关的 `.runtime/data/pms-dev.db`；首次建立时使用 SQLite 在线备份写入进程级临时文件，成功后原子替换，避免备份中断留下半成品数据库，也避免删除临时 worktree 时丢失测试数据。
- 已使用项目档案最新代码完成真实双击启动验证，前后端均保持运行，重复启动会复用同一组受管进程；合并后可直接切换到 `master` 并继续执行同样的来源校验。
- 新增 `frontend/tests/startup-launcher-contract.test.mjs` 并更新启动样式契约；契约会实际执行隔离状态检查，验证有效源码、失效指针和非法覆盖路径；启动器契约、样式契约、标准列表契约、系统 UI 一致性契约和根目录/当前代码源前端构建均通过。
- 浏览器验证管理员登录、项目档案启用/禁用操作和“项目档案编辑”抽屉均来自最新版本，页面控制台无错误。

## 2026-07-20 - 项目档案启停生命周期与删除保护 UI

- 原因：项目档案列表需要与后端启停、删除保护和批量事务接口保持一致，同时修复受保护删除原因无法被键盘/辅助技术访问，以及并发禁用或同步占用后抽屉仍可重复提交的问题。
- 调整内容：列表默认仅查询启用档案并显式发送 `true / false / all` 三态参数，移除旧档案状态筛选、列和编辑器；按启用状态与 RBAC 展示编辑、查看、同步、启用、禁用和删除动作；批量删除与批量启停分别使用一次原子接口并发送全部选中 ID；禁用档案抽屉只读并保留启用状态列、禁用行弱化和固定操作列恢复。
- 调整内容：受保护删除改为 Element Plus tooltip，使用可聚焦且带 `aria-label`、`aria-disabled` 的 owner 包裹实际原生禁用按钮，使鼠标、键盘和辅助技术均可获得后端 blocker 说明，删除动作继续不可点击。
- 调整内容：抽屉保存或同步遇到 `ARCHIVE_DISABLED`、`ARCHIVE_OPERATION_PENDING`、`ARCHIVE_LIFECYCLE_CONFLICT` 等生命周期 `409` 时立即锁定编辑，明确提示失败并保留未保存草稿；随后以 `enabled=all` 刷新同 ID 档案并替换 `selectedArchive` 生命周期状态，关闭时仍需用户明确决定是否放弃草稿。
- 涉及文件：`frontend/src/views/project/ProjectArchive.vue`、`frontend/tests/archive-lifecycle-contract.test.mjs`、`frontend/tests/archive-lifecycle-conflict-contract.test.mjs`、`frontend/tests/archive-filter-contract.test.mjs`、`.superpowers/sdd/task-5-report.md`、`change.md`。
- 验证结果：`node tests/archive-lifecycle-contract.test.mjs`、`node tests/archive-lifecycle-conflict-contract.test.mjs`、`node tests/archive-edit-drawer-contract.test.mjs`、`node tests/archive-filter-contract.test.mjs`、`node tests/list-standard-contract.test.mjs`、`node tests/system-ui-consistency-contract.test.mjs`、`node tests/list-navigation-polish-contract.test.mjs`、`node tests/style-contract.test.mjs` 均退出 `0`；`npm run build`（`vue-tsc -b && vite build`）退出 `0`，Vite 转换 2293 个模块。浏览器全套验收留待 Task 6 单独记录。

## 2026-07-15 - 项目档案语义化字段与数字枚举迁移

- 项目档案新增客户、序列号字段；项目编号改为可编辑，并对项目编号、项目名称、序列号执行去除首尾空格后的唯一性校验，其中项目编号和序列号不区分大小写。
- 后端及前端统一将“产品线”语义改为“产品类别”、将“产品类型”改为“设备系列”；项目档案、项目进度、角色产品范围、字段目录、字段规则和操作日志同步使用新字段编码。
- 项目进度创建必须关联项目档案，项目编号、项目名称、客户、产品类别、设备系列和序列号由档案自动带出，并在项目进度详情中保持引用只读；档案修改编号、名称或产品类别时同步更新已关联项目。
- 可配置及固定流程业务枚举统一改为按枚举独立分配的数字流水存储值；存储值创建后不可修改，删除后不复用，ERP 同步状态等外部协议值继续保留字符串语义。
- 枚举管理新增值时不再要求人工录入存储值，页面明确提示由系统分配；角色产品类别范围改为保存数字 ID，未选择仍表示全部产品类别。
- 新增可重复执行的旧数据库语义迁移：自动补齐新列、迁移旧产品线/产品类型及项目总表快照、生成规范化唯一键与索引，并保留历史枚举显示名及流水上限；系统重启不会恢复已人工删除的可配置枚举默认值。
- 加固旧库迁移边界：数字字符串先按旧枚举映射再写入新流水值，档案编号和名称规范化后同步关联项目，避免升级后出现无效枚举或档案/进度身份不一致。
- 项目级开发规则同步改用“产品类别 / 设备系列”，并明确业务枚举数字流水不可修改、删除后不可复用，避免后续模块重新引入旧字段语义。
- 新增项目档案语义契约与旧库迁移回归，扩展枚举、字段目录、字段规则、RBAC、项目总表和前端抽屉/列表契约；完整后端契约、前端契约和生产构建通过。
- 完成独立 SQLite 浏览器验收：创建、编辑、三项唯一性冲突定位、枚举 `5 -> 删除 -> 6` 不复用、角色产品类别范围，以及项目进度六项档案来源字段只读均符合预期。

## 2026-07-14 - 项目档案字段级编辑抽屉

- 移除项目档案列表的整行双击编辑事件，单击或双击普通数据单元格只保留列表选择行为，避免点击同行任意位置都打开编辑器。
- 项目档案编辑改为右侧独立抽屉，仅操作列“编辑”按钮可以打开；新增档案继续使用集中录入弹窗。
- 抽屉按基础信息、计划信息、ERP 同步和维护信息分组展示，标签、空白区域和只读字段不绑定编辑事件，只有具体字段值按钮可以进入编辑。
- 编辑值先保存在本地草稿，当前输入尚未按 Enter 时底部保存按钮也会实时启用；支持 Enter 暂存、Esc 取消、关闭或切换档案时确认未保存修改。
- 抽屉底部固定“保存修改”和“保存并同步 ERP”，内容区独立滚动，不带动项目档案列表；字段显示、编辑、必填继续执行统一字段规则和 RBAC 权限。
- 保留既有项目档案更新、ERP 同步和操作日志接口，不修改数据库结构；新增抽屉点击范围、滚动边界和草稿保存契约测试。

## 2026-07-14 - 系统页面视觉一致性与日志差异可读性

- 为“字段规则”动态菜单补充 `SetUp` 图标注册，修复菜单只有文字、图标空缺的问题。
- 前端内置可变字重 `Noto Sans SC`，并通过 `--pms-font` 同步给原生控件、Element Plus 与 AG Grid，减少 Windows 与 macOS 的中文字体、字宽和行高差异。
- 扩展全局 PMS 设计令牌和系统页面基础类，统一内容面板、分区标题、双栏布局、密集 Element 表格、状态标签及技术字段编码样式。
- 以项目档案、项目进度为基准，收敛仪表盘、用户管理、角色管理、数据字典、枚举管理、字段规则和操作日志的字体、表格密度、边框、按钮及状态表达；移除角色树中的 emoji 图标。
- 操作日志查询接口新增结构化 `diff_items`，通过字段目录、项目总表注册表和统一字段映射解析中文字段名，并格式化枚举、布尔、空值及嵌套字段。
- 操作日志详情改为中文字段标题和独立的修改前/修改后信息块，原始字段编码降级为辅助信息，原始快照收纳到折叠技术区。
- 新增 `docs/PMS-UI-STANDARD.md`、系统 UI 一致性契约及操作日志中文差异契约，生产构建通过。

## 2026-07-13 - 进度条、角色模板与字段治理

- 修复项目进度详情抽屉使用行内元素时进度轨道和彩色填充失效的问题；列表与抽屉统一按 `<30%` 红色、`30%-79%` 琥珀色、`>=80%` 绿色展示，未维护值继续显示 `-`。
- 新增系统管理员、业务管理员、操作员三套可编辑默认角色模板；模板仅在角色首次创建时授权，系统重启不再补回人工撤销权限，多角色仍按实时 RBAC 并集计算。
- OA 自动开户改为只分配启用的 `operator` 角色；角色缺失或禁用时返回明确错误，不再回退到数据库中的任意角色。
- 新增 `sys_business_field_policy` 与统一字段治理服务，覆盖项目档案和项目进度的业务显示、允许编辑、必填及列表可选范围，并保留引用、计算、系统字段的代码级只读上限。
- 新增“系统管理 / 字段规则”页面及 `/api/field-policies` 查询、批量保存、恢复默认接口；支持模块、字段、分组和来源筛选，规则修改写入操作日志并通过更新时间检测并发覆盖。
- 项目档案表单、筛选和列设置消费同一份字段规则；项目进度列表、筛选、双击编辑、详情抽屉和新增项目表单同步应用规则，浏览器列偏好会自动剔除失效字段。
- 项目新增支持动态 `sheet_values`，项目主表与总表明细在同一事务中创建；规则生效后的新数据严格校验必填，存量数据按规则生效时间豁免，创建失败不会留下半成品项目。
- 项目更新、列表自动保存、详情统一保存、档案写入和 ERP 同步均接入后端字段规则校验；结构化 `422` 在前端转换为字段级中文提示，自动保存失败会恢复原单元格值。
- 将项目创建契约收紧为必须关联项目档案；项目号、项目名称、产品线等来源字段继续只读并回到来源模块维护。
- 新增字段规则、角色模板、OA 默认角色、重启不回补、动态必填事务和进度条样式契约测试；完整前后端契约集及生产构建通过。
- 完成浏览器验收：字段规则页正确展示项目档案 15 个字段和项目进度 131 个字段；抽屉 20%、60%、90% 进度分别呈现红、琥珀、绿色，抽屉独立滚动且底部保存栏保持固定。

## 2026-07-13 - 列表与导航细节优化

- 左侧导航展开宽度由 220px 收窄为 184px，折叠宽度继续保持 64px，为业务列表释放更多横向空间。
- 二级菜单采用纵向导轨方案：子菜单区域增加层级线，当前页面使用靛蓝定位点和柔和背景，一级、二级菜单在全部展开时仍可快速区分。
- 项目档案列表表头统一居中，与项目进度普通表头保持一致；数据单元格既有对齐方式不变。
- 项目档案复用标准 `PmsListColumnPicker`，固定选择、项目编号、项目名称和操作列，其余字段按业务信息、计划信息、维护信息、ERP 同步分组自由显示或隐藏。
- 项目档案列选择、顺序、宽度和固定状态按当前用户自动保存到浏览器，并兼容迁移旧版 `pms_archive_grid_layout_v2` 布局缓存；恢复默认会同时重置所有列偏好。
- 列设置组件增加页面级无障碍名称和“恢复默认”事件，项目档案与项目进度共用组件时可分别表达页面语义。
- 项目进度操作列按权限压缩：有删除权限时为 88px，仅保留详情操作时为 60px，操作按钮在列内居中。
- 新增列表与导航契约测试，并在 1366×768、1600×900 下验证侧栏、表格、横向滚动和分页无页面级溢出；本次不修改后端 API、RBAC、OA 或 ERP 流程。

## 2026-07-13 - 数据字典与枚举管理整改

- 新增只读字段目录服务与 `/api/field-catalog`，自动汇总 SQLAlchemy 模型、Pydantic Schema 和 126 个项目总表注册字段，支持关键词、模块、类型、来源和仅枚举字段筛选。
- 数据字典页面改为标准全宽密集列表，固定字段名称与编码列，提供横向滚动、分页、长说明提示和只读字段元数据展示。
- 将“字段管理”更名为“枚举管理”，迁移到 `/system/enum`，保留 `/system/field` 跳转兼容和原菜单/按钮 ID，避免破坏现有角色授权关系。
- 枚举管理只展示开发注册的五类业务枚举；产品线、产品类型可配置，档案状态、项目状态、任务状态锁定存储值；系统固定枚举不进入管理页。
- 删除五类无运行时用途的旧字段分类；未知历史自建分类保留数据库记录但不在枚举页面展示，并在初始化时输出迁移提示。
- 枚举值新增引用数量与删除保护；禁用值不再用于新建和变更，历史数据继续通过完整标签映射展示。
- 新增统一前端 `useEnumOptions` 能力，项目档案、项目进度、任务、仪表盘和角色产品线改为统一读取枚举，显示名变更后同步刷新共享缓存。
- 项目档案、项目进度、任务和角色产品线写接口接入统一枚举校验；所有枚举维护操作继续写入系统操作日志。
- 项目总表字段元数据增加 `enum_code`，详情抽屉和动态筛选按字段声明自动加载枚举，不再针对单一字段写特殊判断。
- 收紧角色数据范围边界：创建和更新只允许 1-4，全新数据库增加检查约束，历史异常值在运行时按最小权限处理。
- 新增字段目录、枚举管理前后端契约测试，并修复项目总表动态列表测试数据库路径不一致导致的残留数据问题。
- 修正数据字典全高布局，1366×768 与 1600×900 下横向滚动条和分页均完整可见；关键词输入支持 Enter 立即筛选。
- 加固 `start-pms.command` 的后台进程启动与端口回收逻辑，前后端使用 `nohup` 脱离终端输入，并在普通停止失败时强制清理占用端口的旧进程。
- 增加旧数据库升级契约，在任何角色 ORM 查询前补齐 `sys_role.product_lines`，避免已有数据库因 `create_all` 不会新增列而无法启动。
- 字段目录增加 SQLAlchemy 映射表自动兜底，ERP 同步日志、进度变更日志、菜单权限、枚举定义、关联表与登录令牌等模型字段全部纳入只读目录。
- 固定流程枚举初始化会清理未注册且未引用的旧值；仍被历史数据引用的旧值自动停用并保留标签，非数字脏值不再导致枚举页面报错。
- 产品线存储值禁止包含逗号，前后端同时校验，避免与角色产品线范围的序列化格式冲突。
- 项目进度与任务列表对可维护枚举标签统一进行 HTML 转义，避免异常显示名破坏表格单元格内容。
- 路由权限守卫改为返回跳转结果，移除 Vue Router 已弃用的 `next` 回调写法，保持权限导航行为不变并清理运行时警告。

## 2026-07-12 - 角色权限一致性与 RBAC 架构整改

- 新增统一实时授权上下文：每次请求从数据库计算有效用户、有效角色、权限并集、最宽数据范围和产品线并集；JWT 仅保留用户身份，撤权后下一次请求立即生效。
- 建立 `require_permission()` / `require_any_permission()` 统一依赖，将项目进度、档案、任务、ERP、用户、角色、部门、字典、字段、操作日志和仪表盘接入按钮级权限。
- 项目、任务、档案与 ERP 写操作同时校验对象数据范围和产品线；越界对象统一返回 `404`。修复用户未绑定部门时“本部门”范围意外退化为全部数据的问题。
- 已关联项目档案时，产品线权限以档案值为准；项目进度更新接口不再联动修改档案产品线，避免绕过档案编辑权限。
- 扩展 `/api/auth/me` 返回 `role_codes`、`permissions`、`data_scope`、`product_lines`；前端 Pinia、路由守卫、按钮、表格双击编辑和抽屉保存共用同一权限码。
- 角色权限保存自动补齐页面查看权限和祖先菜单，拒绝重复、不存在或已禁用的权限节点；取消查看时前端同步取消对应动作。
- 禁用角色不再参与授权、已关联用户的角色禁止直接删除，并保证至少一名有效用户保留用户管理与角色编辑能力。
- 启动初始化仅在全新数据库首次为默认管理员授权，既有数据库不再自动补回 `admin` 被撤销的权限。
- 新增服务器本地应急脚本 `backend/scripts/recover_role_permissions.py`，可按角色恢复指定权限或全部权限，并写入操作日志。
- 原公开的 SSO 测试链接生成器改为需要 `system:user:edit` 权限，生成行为写入操作日志；正式 OA SSO 登录与回调流程保持不变。
- 新增前后端 RBAC 契约测试，覆盖同一 JWT 即时撤权/恢复、禁用用户/角色、多角色并集、数据越界、初始化不回补权限、前端路由与操作入口。

## 2026-07-12 - 项目进度节点编辑与表头对齐

- 修复项目进度列表“节点”双击编辑时的状态选项类型不一致问题：列表数据和保存接口继续使用状态数值 `1/2/3`，下拉选项通过列格式化展示“进行中 / 已完结 / 暂停”，从而正确匹配当前值。
- 将项目进度列表的普通字段表头、分组表头和动态字段表头统一居中；不改变数据单元格既有对齐方式。
- 新增前端契约测试，防止节点编辑重新出现数值/中文标签错配，以及表头居中规则遗漏。

## 2026-07-12 - 项目进度整格编辑

- 移除项目进度列表编辑状态下由 AG Grid 单元格、选择器和输入框叠加形成的多重边框。
- 编辑器保持铺满原单元格，仅由单元格提供一层浅色焦点边界，避免出现嵌套的小输入框。

## 2026-09-07 - 项目指令的批准复用与交付边界

- 原因：用户批准项目级 AGENTS 审查建议，要求实施 PMS 的建议修改。
- 调整内容：明确复用既有分支、worktree、项目 UI 规范和已批准设计；将架构及 OA/ERP/权限影响放入该批方案；明确现有验证清单与证据复用方式；保留 master 合并确认及安全清理边界。
- 涉及文件：`AGENTS.md`、`change.md`。
- 验证结果：AGENTS 与已批准 PMS 建议稿逐字一致；后端权限、业务日志、字段治理、字体和原验证命令保持不变；文件回读及差异空白检查通过。仅改指令文档，未运行前后端构建或业务测试。
- Git 状态：本地 `codex/agents-rule-clarity` 分支，未暂存、提交、推送、创建 PR 或合并。既有未提交内容保留，另一工作树未修改。

## 2026-09-10 - 项目档案选择字段无内框样例

- 原因：项目档案抽屉进入编辑态后，Element Plus 控件的白底和内阴影与字段行叠加，形成“字段行内再套一个小框”，展示态与编辑态观感不一致。
- 调整内容：仅将“产品类别”作为第一项确认样例，使用统一行内编辑样式；内部选择控件改为透明、无边框和无阴影，整块值区域使用淡色背景提示编辑状态；同时清除可搜索选择器内部真实输入元素继承的全局焦点轮廓，避免 Mac/Windows 浏览器出现第二层紫色框。其他字段暂未改动，待用户确认样例后再统一应用。
- 涉及文件：`frontend/src/views/project/ProjectArchive.vue`、`frontend/src/styles/pms-theme.css`、`frontend/tests/archive-edit-drawer-contract.test.mjs`。
- 验证结果：两轮回归测试分别先因缺少无内框样式、缺少内部输入焦点轮廓规则按预期失败，实施后通过；`archive-edit-drawer-contract`、`style-contract`、`list-standard-contract`、`system-ui-consistency-contract`、`project-sheet-detail-drawer-contract` 和 `npm run build` 均通过。浏览器实测内部控件 `box-shadow: none`、`outline: none`、背景透明，外层编辑区域使用淡色背景。

## 2026-09-10 - 登录默认凭据清理与无凭据启动探活

- 原因：登录页公开并预填固定本地管理员账号，且 macOS 启动器使用同一固定账号密码检查 API，修改管理员密码后会被误判为启动失败。
- 调整内容：登录页移除默认账号提示和预填值，仅保留 OA 门户入口提示；新增无需登录的 `/api/health` 健康接口，启动器改为通过该接口检查前端代理；新数据库不再创建固定 `admin` 账号，只在无用户时读取 `PMS_BOOTSTRAP_ADMIN_USERNAME` 与 `PMS_BOOTSTRAP_ADMIN_PASSWORD` 创建初始管理员，密码至少 12 位且只保存哈希；兼容角色已初始化但用户表为空的恢复场景，确保用户与管理员角色在同一事务内正确关联。
- 涉及文件：`frontend/src/views/Login.vue`、`frontend/tests/login-security-contract.test.mjs`、`backend/app/core/config.py`、`backend/app/models/init_db.py`、`backend/main.py`、`backend/tests/bootstrap_admin_contract.py`、`backend/tests/project_progress_workbench_contract.py`、`start-pms.command`、`change.md`。
- 验证结果：登录安全、启动器、样式、标准列表、系统 UI、项目档案抽屉、配置、SQLite 初始化、RBAC、操作日志及初始管理员契约测试通过；`zsh -n start-pms.command` 与 `npm run build` 通过；浏览器确认登录输入框为空，`/api/health` 经 Vite 代理返回 `{"status":"ok"}`。RBAC 测试仍输出既有 `passlib/bcrypt` 版本探测警告，但测试结果通过。

## 2026-09-11 - PMS 统一表单框架设计样稿

- 原因：项目档案抽屉的文本、选择字段在阅读、悬停和编辑时出现框体尺寸变化、文字移位和重复焦点框，暴露出全系统缺少可独立升级的统一表单基础层。
- 调整内容：新增 PMS 统一表单框架设计规格，明确令牌层、字段原语、标准表单/抽屉行内编辑/AG Grid 三类场景适配器、交互合同、分批迁移、跨平台验收和回滚边界。
- 调整内容：新增不连接 API、不进入生产路由的独立可交互样稿，可切换详情抽屉、新增表单、表格编辑和状态矩阵，用于在正式改造前确认审美和交互。
- 涉及文件：`docs/superpowers/specs/2026-09-11-pms-form-system-design.md`、`docs/prototypes/pms-form-system-v1.html`、`change.md`。
- 验证结果：本机 Edge 人工检查四个样稿场景；使用本机 Edge 内核在 `1366x768` 和 `1600x900` 自动截图，页面无脚本错误；客户字段阅读态和编辑态外容器均为 `240x32px`，位置一致，内部输入元素无 outline、无 box-shadow、无额外 padding。本批未修改正式业务页面，未部署服务器。
- 调整内容：样稿获确认后补充九阶段实施计划，明确公共组件接口、TDD 契约、项目档案试点、项目进度与系统页面迁移顺序、每批验收暂停点及最终跨平台门禁；按当前代码盘点补齐树形选择、开关、单项/多项勾选和分段选择适配器，计划仍不包含服务器部署、GitHub 推送或主分支合并。
- 涉及文件：`docs/superpowers/plans/2026-09-11-pms-form-system.md`、`change.md`。
- 验证结果：实施计划完成模糊占位扫描、文件路径核对和差异空白检查；正式业务代码尚未开始迁移。

## 2026-09-11 - PMS 统一表单框架第一批与项目档案试点

- 原因：已确认的统一表单样稿需要先形成可独立升级的公共模块，并以项目档案作为首个正式页面试点，消除文本、选择和日期字段在显示、悬停、编辑及聚焦状态下的尺寸变化、文字移位和重复内框。
- 调整内容：新增 `frontend/src/form-system/`，建立统一表单类型、尺寸与状态令牌、控件外壳，以及文本、选择、树形选择、日期、数字、长文本、开关、单选、多选和分段选择适配器；补充标准表单字段、详情抽屉行内字段和 AG Grid 单元格编辑适配规则，并由前端入口统一加载。
- 调整内容：项目档案新增弹窗改用 `PmsFormField` 与统一控件；详情抽屉改用 `PmsInlineField`，只有字段值区域触发编辑，标签与空白区域不触发；所有可编辑字段共享 `32px` 编辑表面、相同文字起点、单层焦点反馈和统一只读图标，原有草稿、校验、统一保存、ERP 同步与权限逻辑保持不变。
- 调整内容：独立代码审查后补齐抽屉编辑器自动聚焦、字段可访问名称、字段元数据加载失败时的既有必填规则兜底，以及后端字段错误进入统一控件错误态；文本、日期和选择控件沿用相同交互合同。
- 调整内容：启用状态筛选的 Element Plus 选项值由 `null` 改为后端已支持的 `'all'` 协议值，保留默认只看启用档案和 `true / false / all` 查询语义，清除试点页面的无效选项值警告。
- 调整内容：项目档案首屏不再等待字段规则、权限和枚举辅助请求；枚举标签未就绪时列表暂显 `-`，映射到达后定向刷新对应 AG Grid 列，既避免泄露数字存储值，也避免辅助接口拖慢列表。列表失败与用户选项、列偏好初始化相互隔离。
- 涉及文件：`frontend/src/form-system/`、`frontend/src/main.ts`、`frontend/src/views/project/ProjectArchive.vue`、`frontend/src/styles/pms-theme.css`、`frontend/tests/form-system-contract.test.mjs`、`frontend/tests/form-system-layout-contract.test.mjs`、`frontend/tests/archive-edit-drawer-contract.test.mjs`、`frontend/tests/archive-filter-contract.test.mjs`、`frontend/tests/archive-lifecycle-contract.test.mjs`、`change.md`。
- 验证结果：统一表单、布局、项目档案编辑抽屉、筛选、生命周期、生命周期并发、样式、标准列表和系统 UI 一致性共九项契约均退出 `0`；`npm run build` 退出 `0`，转换 2322 个模块。独立临时数据库在本机 Edge 完成新增、字段值点击、自动聚焦、文本编辑、选择下拉、统一保存、枚举标签刷新和抽屉独立滚动验收；`1366x768` 与 `1600x900` 下抽屉均为 `400px`，字段行 `48px`，显示与编辑表面均为 `254x32px`，保存栏保持固定。AG Grid 旧选择 API 仍输出既有弃用提示，留待标准列表基础模块升级批次处理。
- 范围说明：本批停在项目档案试点验收点，尚未迁移项目进度或系统管理表单，也未部署服务器、推送 GitHub或合并 `master`。

## 2026-09-11 - PMS 统一表单框架第二批项目进度迁移

- 原因：项目档案试点验收通过后，按已批准的分批迁移计划将项目进度详情抽屉和表格内编辑接入同一套表单基础层，避免选择、日期和数字编辑器继续由页面零散覆盖样式。
- 调整内容：项目进度详情抽屉改用 `PmsInlineField` 与统一文本、选择、日期、数字和长文本控件；保留字段来源说明、进度条、快捷加入列表、草稿计数、底部统一保存及原有字段规则，不改变引用字段只读边界。
- 调整内容：为共享行内字段增加标签附属说明和紧凑操作插槽，统一处理快捷操作的悬停、键盘聚焦、定位和内容避让；项目页面不再通过绝对定位自行拼装字段来源与快捷按钮。
- 调整内容：项目进度工作台和任务进度表统一接入 AG Grid 表单适配器；编辑单元格由共享令牌提供唯一焦点边界，内部文本、选择和数字编辑器铺满单元格并移除嵌套边框。列表双击编辑、七阶段字段、自动保存、动态列和权限逻辑保持不变。
- 涉及文件：`frontend/src/form-system/components/PmsInlineField.vue`、`frontend/src/form-system/form-tokens.css`、`frontend/src/views/project/ProjectList.vue`、`frontend/src/views/project/ProjectProgress.vue`、`frontend/tests/form-system-layout-contract.test.mjs`、`frontend/tests/project-progress-workbench-contract.test.mjs`、`frontend/tests/project-sheet-detail-drawer-contract.test.mjs`、`change.md`。
- 验证结果：前端全部 20 项契约测试退出 `0`，`npm run build` 退出 `0` 并转换 2322 个模块；本机 Edge 验证节点、原计划发货和阶段进度编辑均保留原值且仅显示一层编辑边界，详情抽屉独立滚动，底部统一保存固定，未修改时按钮禁用。独立代码审查提出的编辑态快捷按钮遮挡、无编辑权限缺少只读说明、旧 AG Grid 焦点样式竞争和草稿类型放宽问题均已修正并补充回归断言；项目档案试点相关契约同步通过。
- 范围说明：本批仅完成开发机项目进度迁移，尚未迁移系统管理表单，未部署服务器、推送 GitHub 或合并 `master`。

## 2026-09-11 - PMS 统一表单框架第三批共享列表控件迁移

- 原因：项目档案与项目进度已经接入统一表单基础层，但标准列表的自定义筛选和列设置仍直接使用 Element Plus 字段控件，跨页面新增列表时仍可能出现尺寸、焦点和字体状态不一致。
- 调整内容：`PmsListFilters` 的文本、选择、日期和数字筛选统一改用紧凑型 PMS 表单控件，保留原有字段定义、操作符、日期 `YYYY-MM-DD`、区间双值、清空和 `update:filters` 事件协议；各类筛选值宽度保持迁移前数值。
- 调整内容：`PmsListColumnPicker` 的搜索与勾选统一改用 PMS 文本和复选框控件，保留列搜索、选择数量、全选、恢复默认、列表持久化及 `update:modelValue` 协议；移除嵌套标签并为勾选项补充可访问名称。
- 调整内容：独立代码审查后将共享筛选控件的固定宽度容器统一为块级元素，并增加契约约束，避免行内 `span` 包裹控件根 `div` 形成无效 HTML。
- 涉及文件：`frontend/src/components/PmsListFilters.vue`、`frontend/src/components/PmsListColumnPicker.vue`、`frontend/tests/list-standard-contract.test.mjs`、`frontend/tests/archive-filter-contract.test.mjs`、`frontend/tests/form-system-contract.test.mjs`、`change.md`。
- 验证结果：定向契约测试和生产构建通过；本机 Edge 验证项目档案与项目进度的自定义筛选输入、筛选生效、列搜索、列勾选及恢复默认，公共控件尺寸与原布局一致。完整前端契约与最终构建结果见本批提交前验证。
- 范围说明：本批仅完成开发机共享列表控件迁移，尚未迁移系统管理表单，未部署服务器、推送 GitHub 或合并 `master`。

## 2026-09-11 - PMS 统一表单框架第四批系统管理表单迁移

- 原因：用户、角色、旧数据字典、枚举和字段规则页面仍直接使用 Element Plus 字段控件，弹窗字段在公共 flex 容器中还会按内容宽度收缩，无法继承统一表单框架已经确认的尺寸、焦点和跨平台视觉标准。
- 调整内容：用户与部门维护统一改用文本、数字、树形选择、开关和多选适配器；角色维护统一改用文本、选择、开关、复选框组和长文本适配器，同时保留权限树本身的原生树形勾选交互。
- 调整内容：枚举维护统一接入数字流水只读展示、文本、数字和开关适配器；字段规则的模块切换、搜索、来源筛选及四类策略开关统一接入紧凑控件；旧数据字典维护页面同步移除原始字段控件旁路。
- 调整内容：公共弹窗表单将 Element Plus 的字段内容 flex 容器统一改为全宽块级布局，避免自定义字段按内容收缩。浏览器实测用户弹窗字段由 `162px` 恢复为 `448px`；角色双列字段为 `296px`、跨列字段为 `608px`。
- 兼容边界：角色权限、数据范围 `1-4`、产品类别范围、用户和部门数值 ID、状态 `0/1`、枚举存储值、字段规则 `updated_at` 并发检查及服务端结构化错误协议均保持不变。
- 涉及文件：`frontend/src/form-system/form-tokens.css`、`frontend/src/views/system/UserList.vue`、`frontend/src/views/system/RoleList.vue`、`frontend/src/views/system/MenuList.vue`、`frontend/src/views/system/EnumList.vue`、`frontend/src/views/system/FieldPolicyList.vue`、`frontend/tests/form-system-layout-contract.test.mjs`、`frontend/tests/system-ui-consistency-contract.test.mjs`、`frontend/tests/enum-management-contract.test.mjs`、`frontend/tests/field-policy-contract.test.mjs`、`change.md`。
- 验证结果：统一表单、统一表单布局、系统 UI 一致性、枚举管理、字段规则和样式契约均退出 `0`；`npm run build` 退出 `0`，转换 2322 个模块。开发机 Edge 完成用户新增/编辑、角色新增、可配置枚举新增和字段规则模块切换的只读交互验收，所有业务输入均位于统一控件内，未提交或修改业务数据。
- 范围说明：本批仅完成开发机系统管理写表单迁移，尚未迁移认证页面，未部署服务器、推送 GitHub 或合并 `master`。

## 2026-09-11 - PMS 统一表单框架第五批查询与认证页面迁移

- 原因：数据字典、操作日志、旧字段维护页、登录、OA 登录和 SSO 令牌工具仍直接使用 Element Plus 字段控件，尚未完整纳入统一尺寸、焦点、字体和可访问性标准。
- 调整内容：数据字典与操作日志的日期、文本、选择和枚举筛选统一改用紧凑型 PMS 表单控件，保留原查询参数、自定义筛选和日志中文差异展示协议；旧字段维护页的搜索及存量弹窗同步迁移，避免兼容路由重新暴露旧控件。
- 调整内容：本地登录与 OA 登录页面改用常规尺寸 PMS 文本和复选框控件，保留空凭据初始值、回车登录、记住登录和既有认证接口，并将 `name`、`autocomplete` 和可访问名称正确传递至原生输入元素。
- 调整内容：SSO 令牌工具的测试及 HMAC 表单改用统一字段与文本控件，保留测试账号、链接格式、权限边界和 `/sso/generate-url` 接口，不提交或记录任何凭据与令牌。
- 涉及文件：`frontend/src/views/system/DataDictionaryList.vue`、`frontend/src/views/system/OperationLogList.vue`、`frontend/src/views/system/FieldList.vue`、`frontend/src/views/Login.vue`、`frontend/src/views/SsoStart.vue`、`frontend/src/views/TokenGenerator.vue`、`frontend/tests/data-dictionary-contract.test.mjs`、`frontend/tests/login-security-contract.test.mjs`、`frontend/tests/system-ui-consistency-contract.test.mjs`、`change.md`。
- 验证结果：统一表单、统一布局、数据字典、登录安全、系统 UI 一致性和样式契约均退出 `0`；`npm run build` 退出 `0`，转换 2322 个模块。开发机新版本使用独立 `5176` 端口验收：登录字段均为 `36px` 外框且只有单层焦点反馈，用户名与密码原生输入分别带 `username`、`current-password` 自动完成语义且值为空；OA 登录页保留两个空凭据输入、统一记住登录控件及原入口。已登录页面因浏览器会话按端口隔离，未复制敏感会话令牌，查询页由契约与生产构建验证。
- 范围说明：本批仅完成开发工作树内的查询、兼容与认证页面迁移，未部署服务器、推送 GitHub 或合并 `master`。

## 2026-09-11 - PMS 统一表单框架最终收口

- 原因：分批迁移完成后需要建立永久采用门禁，清理全局主题中遗留的 Element Plus 内部控件覆盖，并补齐项目页面中遗漏的基础筛选和新增项目表单，防止后续开发重新产生双框、尺寸变化和跨平台文字移位。
- 调整内容：新增统一表单采用契约，递归检查业务 Vue 页面，禁止在 `form-system` 外直接使用输入、选择、树形选择、日期、数字、开关、复选和分段控件；全局样式契约同时禁止 `pms-theme.css` 覆盖输入控件内部 DOM。
- 调整内容：项目档案、项目进度和任务进度的基础筛选统一改用紧凑型 PMS 控件；新增项目弹窗统一改用 `PmsFormField` 及文本、选择、树形选择、日期、数字和长文本控件，保留字段规则、动态必填、权限和接口协议。
- 调整内容：新增项目的部门和项目经理初始值由数值哨兵 `0` 改为真正空值，避免未选择时在统一选择控件中显示 `0`；移除全局主题对 Element Plus 输入内部边框、悬停和焦点的旧覆盖，第三方兼容规则统一收口到表单系统令牌层。
- 调整内容：更新 UI 标准，明确业务页面的统一表单导入路径、标准表单/抽屉/AG Grid 三类适配边界，以及新增字段必须覆盖的状态验收要求。
- 涉及文件：`frontend/src/views/project/ProjectArchive.vue`、`frontend/src/views/project/ProjectList.vue`、`frontend/src/views/project/ProjectProgress.vue`、`frontend/src/styles/pms-theme.css`、`frontend/tests/form-system-adoption-contract.test.mjs`、`frontend/tests/style-contract.test.mjs`、`frontend/tests/project-progress-workbench-contract.test.mjs`、`docs/PMS-UI-STANDARD.md`、`change.md`。
- 验证结果：统一表单、布局、采用门禁、样式、标准列表、系统 UI、项目档案筛选/抽屉/生命周期、项目进度工作台/详情/列设置、数据字典、枚举、字段规则和登录安全共 17 项前端契约测试全部退出 `0`；`npm run build` 退出 `0`，转换 2322 个模块。本机 Edge 验证项目档案和项目进度基础筛选、新增项目弹窗、部门树形选择和日期选择器，无重复内框、裁切或未选择项显示 `0` 的问题。
- 范围说明：统一表单框架在开发机完成并通过本阶段验收；尚未部署服务器、推送 GitHub 或合并 `master`，Windows 缩放环境仍需在部署前做最终验收。

## 2026-09-11 - OA 嵌入环境表单焦点重影修正

- 原因：PMS 直连页面在 macOS 和 Windows 显示正常，但从泛微 OA 门户嵌入进入时，统一控件原有的 `1px` 聚焦边框与 `3px` 半透明外扩焦点环在门户缩放和图层合成下被强化，表现为选择框双边界；行内字段显示面与编辑面部分盒模型依赖全局继承，也会放大跨容器渲染差异。
- 调整内容：统一表单控件的焦点和错误状态取消外扩阴影，仅由最外层控件绘制单层状态边框；Element Plus 内部包装层与真实输入继续保持无边框、无阴影和无焦点轮廓。行内字段显示面与编辑面显式统一 `border-box`、固定高度、全宽和零容器内边距，避免文本、选择字段在悬停与编辑间改变尺寸或文字起点。
- 调整内容：新增统一表单契约，禁止重新引入向控件外部扩张的焦点环，并锁定行内显示面和编辑面的盒模型规则；业务页面未增加 OA 专用 CSS 分支。
- 涉及文件：`frontend/src/form-system/form-tokens.css`、`frontend/tests/form-system-contract.test.mjs`、`frontend/tests/form-system-layout-contract.test.mjs`、`change.md`。
- 验证结果：两项新增断言先针对旧外扩焦点环按预期失败，实施后通过；前端全部 22 项契约测试通过。`npm run build` 退出 `0`，`vue-tsc` 类型检查通过，Vite 转换 2322 个模块并完成生产构建。开发机 Edge 在独立最新端口检查聚焦控件，外层、Element Plus 包装层与真实输入的 `box-shadow` 均为 `none`，内部输入无边框和轮廓，控件高度保持 `36px`。
- 范围说明：本批仅完成开发工作树修正和开发机验证，尚未部署服务器、推送 GitHub 或合并 `master`；办公 Windows 的 OA 嵌入场景需在部署该构建后进行最终验收。

## 2026-09-11 - OA 表单焦点修复服务器部署

- 原因：开发机修正通过后，需要让 `10.10.1.228` 的运行版本与已验证代码一致，并为办公 Windows 的 OA 嵌入验收提供真实服务器版本。
- 调整内容：将提交 `61dd08c` 快进同步到服务器 `C:\PMS`，部署同一提交构建的前端静态文件；替换前将旧 `dist` 完整备份到 `C:\backup\PMS-dist-20260911-143013`。
- 部署校验：服务器源码 HEAD 为 `61dd08c` 且工作区无未提交修改；发布包 SHA-256 为 `8d5418ad8975aaf156ad09ce3abc665e0feb7a16edf3226dcc7787e0371d5bea`，服务器首页引用 `assets/index-Co2-yT2i.css`，旧焦点样式资源已不再由新入口引用；`/api/health` 返回业务内容 `{"status":"ok"}`。
- 浏览器验证：使用 Edge 新标签访问服务器构建，客户字段显示态与编辑态外框均为 `254x32px`；产品类别聚焦时最外层保留单边框，外层、Element Plus 包装层和真实输入均无焦点阴影，输入内边距为 `0`，未再出现文字位移。办公 Windows 的 OA 门户会话仍需用户刷新后完成最终视觉验收。
- 范围说明：代码已推送到 `codex/pms-server-sync-20260910` 功能分支并部署服务器；本次未合并 `master`。

## 2026-09-11 - OA 正式 PMS 地址统一

- 原因：PMS 正式服务器的表单修复已经生效，但 OA 菜单仍通过 `pms_sso.jsp` 跳转到旧开发地址 `10.10.91.60:5174`，导致 OA 内看到的并非 `10.10.1.228` 当前版本，清除浏览器缓存也无法生效。
- 调整内容：OA JSP 跳转地址改为 `http://10.10.1.228/sso/start`；后端默认 `PMS_FRONTEND_URL` 与 `PMS_CALLBACK_URL` 同步改为正式服务器标准 HTTP 地址。认证签名格式、密钥、用户映射和 OA 接口地址保持不变。
- 涉及文件：`OA对接/pms_sso.jsp`、`backend/app/core/config.py`、`backend/tests/sso_deployment_contract.py`、`change.md`。
- 验证结果：新增 SSO 部署契约退出 `0`，确认 JSP 与后端默认地址均指向 `10.10.1.228` 且目标配置不再包含旧地址；Python 源码语法检查退出 `0`。开发机未安装后端依赖，既有 `config_contract.py` 留待依赖齐全的 PMS 服务器补跑；OA 服务器 JSP 部署后仍需从办公 Windows 完成实际菜单跳转验收。

## 2026-09-11 - 记录 OA 单点登录 JSP 部署路径

- 原因：用户完成 OA 服务器 SSO 文件更新并确认问题解决，需要将实际部署位置纳入项目文档。
- 调整内容：新增 OA 单点登录部署说明，记录 OA Web 服务器、JSP 物理路径、Web 路径、项目源文件、正式 PMS 地址、替换步骤和敏感配置保护要求。
- 涉及文件：`docs/OA单点登录部署说明.md`、`change.md`。
- 验证结果：根据用户提供的 OA 服务器截图核对，`pms_sso.jsp` 位于 `E:\Weaver\ecology\jsp\pms_sso.jsp`；用户已确认更新成功且旧地址问题解决。本次未修改程序代码、服务配置或服务器文件。

## 2026-09-12 - 金蝶应用认证迁入最新开发基线

- 原因：原金蝶应用认证改动所在根目录比分支最新 `master` 落后 17 个提交，并混有多批未提交内容；用户确认改为只在开发机继续，需要在不覆盖统一表单和 SSO 更新的前提下完成本地收口。
- 调整内容：从 `master` 提交 `0d588cc` 建立 `codex/kingdee-app-auth` 隔离工作树，仅迁入金蝶官方 SDK 应用签名认证、只读保护、配置模板、诊断脚本、专项测试、固定 SDK 包和对接文档；保留最新 OA 正式地址，不迁移旧 UI 样例和重复登录改动。受保护的 `.env.local` 未复制。
- 安全补强：`K3_READ_ONLY` 代码默认值由 `false` 改为 `true`，只有在另行批准的写入窗口显式配置 `false` 才开放保存请求；专项契约已覆盖该默认关闭行为。
- 涉及文件：`backend/app/core/config.py`、`backend/app/services/kingdee.py`、`backend/requirements.txt`、`backend/.env.kingdee.example`、`backend/scripts/check_kingdee_connection.py`、`backend/tests/kingdee_app_auth_contract.py`、`backend/tests/project_archive_lifecycle_concurrency_contract.py`、`backend/vendor/`、`docs/金蝶第三方应用对接SOP.md`、`docs/金蝶云星空接口对接方案.md`、金蝶设计与实施计划、`change.md`。
- 验证结果：最新 `master` 基线的前端登录、统一表单、样式、标准列表、系统 UI 和生产构建通过；迁移完成后 `backend/tests` 全部 18 个契约测试通过，其中金蝶应用认证 13 项模拟契约覆盖默认只读和连接构造异常脱敏。未访问金蝶、未发送 Save、未连接或修改服务器。SDK SHA-256 与记录值 `b8e40f96ac143028dbb5fb732ec7e6c513628603070aabf3e53678a34e308056` 一致。

## 2026-09-12 - 开发机发布候选收口

- 原因：开发机与 GitHub 分支完成统一后，需要在不连接服务器的前提下完成完整回归、遗留备份审查，并准备可校验、可回退的服务器发布候选包。
- 调整内容：新增 `build-server-release.command`，只打包已提交的后端程序、固定依赖、维护脚本、金蝶官方 SDK 和前端生产文件；部署相关源码有未提交改动时拒绝打包，运行时配置、数据库、日志、测试、开发依赖和 Git 信息明确排除。
- 调整内容：新增服务器发布包契约测试，实际检查 ZIP 内容、Git commit、只读要求、内外 SHA-256 和禁止路径；发布包内置服务器部署、验收和回退说明。
- 调整内容：修正金蝶 SOP 中已失效的 worktree 和临时分支状态，新增开发机发布候选检查报告；根目录旧改动继续保存在 `backup/pre-consolidation-root-20260912`，未恢复旧代码或处理用户保留的图片删除状态。
- 涉及文件：`.gitignore`、`build-server-release.command`、`backend/tests/server_release_contract.py`、`docs/PMS服务器发布与回退说明.md`、`docs/金蝶第三方应用对接SOP.md`、`docs/releases/2026-09-12-PMS开发机发布候选检查报告.md`、`change.md`。
- 验证结果：后端既有 18 项契约、前端 22 项契约全部通过；新增发布包契约完成 RED/GREEN 验证；`npm run build` 通过并转换 2322 个模块。发布候选包为 `/Users/jin/Code/PMS/.runtime/releases/pms-server-ce0ef8fd-20260912.zip`，SHA-256 为 `cea19cc4e94337b88723a87513d8f6ecbf390e58e3284328eb8ee4fb8127e9f0`。本批未连接、修改或重启服务器，未访问金蝶真实接口。

## 2026-09-14 - 本地登录账号同步与正式登录失败提示修复

- 原因：本地只有 admin，服务器启用账号为 ael；普通登录页未处理统一拦截器留给页面处理的登录 401，导致点击后没有错误提示。
- 调整内容：备份本地 SQLite 后，将本地用户 id=1 登录名改为 ael、同步服务器 ael 的密码校验值，保留用户 ID、角色与业务数据关系，撤销该本地用户旧记住登录令牌，并记录脱敏操作日志。服务器密码未重置；凭据经一次性加密传输，临时文件已移除。
- 调整内容：登录页面仅处理 /auth/login 的 401，显示服务端文字或“用户名或密码错误”；其他错误仍由统一拦截器提示，避免 /auth/me 401 重复提示。新增实际登录处理器回归测试。
- 涉及文件：frontend/src/views/Login.vue、frontend/tests/login-feedback-contract.test.mjs、docs/releases/PMS登录修复发布记录-20260914.md、change.md；本地受保护运行数据库 .runtime/data/pms-dev.db。
- 验证结果：登录反馈用例按 TDD 先失败后通过，登录安全、样式、标准列表、系统 UI 契约及生产构建均退出 0；独立复查通过。本地与正式浏览器实测错误凭据均显示一次“用户名或密码错误”；本地密码校验值与服务器一致、角色关系不变。用户已自行输入服务器密码成功登录本地 PMS，页面与后端登录状态已核实；不记录密码。
- 发布结果：正式服务器 10.10.1.228 前端已部署；旧版备份 C:\backup\PMS-login-feedback-20260914-084550；首页及发布文件 SHA-256 验证通过，/api/health 返回 200/ok。保留旧静态资源供已打开页面使用；未部署后端或配置、未重启后端、未写金蝶业务数据。
- 版本范围：正式服务器 HEAD 保持 8108a7e，登录修复以可回退补丁发布。开发修复位于 codex/pms-release-readiness；尚未合并 master 或推送 GitHub。本地修复预览端口 5176 代理现有 8000 后端；默认 5174 仍为 master 页面。


## 2026-09-14 - 金蝶类别查询修复与测试项目新建更新验收

- 原因：辅助资料查询接收类别参数但未用于过滤，且直接拼接编号、仅取首条，可能选中其他类别同编号资料或漏检重复。
- 调整内容：在开发工作树按类别与编号联合筛选，两参数单引号转义；最多查询 2 条并在重复时失败关闭，保持 API、认证、RBAC 与字段映射不变。新增 4 项查询范围契约测试。
- 实际验收：用户批准后，本地 PMS 创建 PMS-TEST-20260914-001（id=2），通过原界面同步测试账套销售项目，再仅修改名称并更新。金蝶两次回查内码均为 6aa74dc6db7b77，最终名称为 PMS金蝶对接验收-20260914-更新验证；PMS 两条同步日志 create/update 均成功，统一操作日志 49—52 完整。样本保留。
- 运行恢复：真实写入后结束临时写入进程，以只读模式重新启动本地修复后端；受保护配置始终保持只读 true，未复制凭据、未调整正式环境。当前 8000 为工作树临时验收后端，5176 为修复前端，今日合并收口时需统一启动来源。
- 涉及文件：backend/app/services/kingdee.py、backend/tests/kingdee_query_scope_contract.py、docs/金蝶第三方应用对接SOP.md、change.md；本地与金蝶测试账套中的指定样本及日志。
- 验证结果：4 项查询范围测试 RED→GREEN，13 项应用认证、生命周期、并发、操作日志契约退出 0；独立审查通过，真实新建/更新和恢复只读后健康检查通过。证据 .runtime/kingdee-validation/sample-acceptance-20260914.json。
- Git 状态：本批修复在 codex/pms-release-readiness，尚未提交、推送或合并；按用户要求今日开发完毕后统一上传合并。


## 2026-09-14 - 金蝶 SOP 正式整理与部署准备

- 原因：实施记录累积了不同阶段状态，需要形成可直接执行且无过期待办混杂的操作说明。
- 调整内容：SOP 更新为 2.0，统一账号与许可说明、应用创建、配置、字段映射、测试/正式切换、轮换和回退步骤；原始过程另存历史记录。记录用户最新决定：正式部署先使用 I0001，专用账号后续替换；用户后续确认目标为 2023121101 / 65771a661f427f。
- 涉及文件：docs/金蝶第三方应用对接SOP.md、docs/records/金蝶对接实施过程记录-20260910至20260914.md、change.md。
- 验证结果：文档相对链接有效、两工作目录正文一致；历史快照保留。仅完成文档与部署包准备，本条不代表已执行后端正式部署、上传或合并。


## 2026-09-14 - 补充金蝶生产应用配置记录

- 原因：生产账套应用已由管理员保存，需要在 SOP 中区分应用配置完成与正式 PMS 后端切换。
- 调整内容：记录生产应用登记 360016、仅授权 I0001、生产目标 2023121101 / 65771a661f427f；明确开发机只读认证通过，服务器后端尚未切换。
- 涉及文件：`docs/金蝶第三方应用对接SOP.md`、`change.md`。
- 验证结果：核对本次保存与只读认证证据；文档差异检查通过。未写入生产业务数据，未推送或合并。


## 2026-09-14 - 金蝶生产应用只读部署

- 原因：按已批准范围将正式 PMS 从原账套密码认证切换为指定生产账套应用认证。
- 调整内容：发布金蝶配置、服务、只读诊断脚本、官方 SDK 及相关回归文件；只修改 K3 配置，生产账套为 65771a661f427f，I0001，业务写入关闭；补齐缺失依赖并重启后端。更新 SOP 与生产部署记录。
- 涉及文件：`backend/app/core/config.py`、`backend/app/services/kingdee.py`、`backend/requirements.txt`、`backend/.env.kingdee.example`、`backend/scripts/check_kingdee_connection.py`、`backend/vendor/`、金蝶相关测试、`docs/金蝶第三方应用对接SOP.md`、`docs/releases/PMS金蝶生产只读部署记录-20260914.md`、`change.md`；服务器受保护配置未进入 Git。
- 验证结果：生产真实认证 success=true；4 项查询与 13 项认证回归通过；新 PID 4104 接管端口，后端及 nginx 健康均正常，部署哈希一致。正式登录成功，当前登录身份调用受保护金蝶连接接口返回 HTTP 200、success=true；临时验收页检查后删除。未写生产业务数据、未推送或合并。


## 2026-09-14 - 修正 SOP 的 OA 环境说明

- 原因：用户明确 10.10.1.149 为测试 OA、10.10.1.247 为正式 OA。
- 调整内容：补充正式/测试环境表、正式菜单域名入口和 OA 验收当前状态，避免混淆 IP 登录与域名登录。
- 涉及文件：`docs/金蝶第三方应用对接SOP.md`、`change.md`。
- 验证结果：正式菜单链接与域名登录页现场核对；部署前后 15 项 OA/SSO 配置一致；文档差异检查通过。未修改 OA 系统或认证配置。


## 2026-09-14 - 补齐正式 OA 免密验收文档

- 原因：用户完成正式 OA 域名登录，原 PMS 菜单免密跳转已取得实际验证结果。
- 调整内容：将 SOP 与部署记录的 OA 待验收状态更新为通过，说明 IP 与域名登录状态不同；未修改 OA 配置或认证代码。
- 涉及文件：`docs/金蝶第三方应用对接SOP.md`、`docs/releases/PMS金蝶生产只读部署记录-20260914.md`、`change.md`。
- 验证结果：点击正式 OA 的 PMS 菜单，内嵌 PMS 仪表盘显示 OA 用户 A000645；文档差异检查通过。生产金蝶业务写入继续关闭，未推送或合并。


## 2026-09-14 - 生产金蝶单样本新增更新验收完成

- 原因：用户明确批准生产样本写入验收。
- 调整内容：正式 PMS 新建 PMS-ACCEPT-20260914-001（ID 4），同步金蝶后修改名称并再次同步；临时运行入口仅放行该编号的两次 Save，设置十分钟期限和内码核对；结束后恢复普通只读后端 PID 260，持久配置与程序文件未改。更新 SOP 3.0 和验收报告。
- 涉及文件/数据：正式 PMS 样本档案、ERP 同步日志、统一操作日志、金蝶 xsxm 辅助资料；`.runtime/kingdee-validation/production-package/sample-acceptance/` 临时验收材料；`docs/金蝶第三方应用对接SOP.md`、`docs/releases/PMS金蝶生产样本验收记录-20260914.md`、原生产部署记录、`change.md`。
- 验证结果：金蝶内码 6aa7719bdb9bfc 新增/更新前后相同，记录唯一，名称一致；同步日志 31/32 与操作日志 70—73 全成功；临时进程已停止，普通后端健康正常，只读 Save 在联网前被拒绝。样本保留，长期同步未开启，未推送或合并。

## 2026-09-14 金蝶自动提交审核及正式发布

- 原因：保存后的项目辅助资料停留创建状态，用户要求自动审核。
- 调整：保存后按状态执行提交/审核，回查 C 和目标内码、名称一致才标记成功；保留业务失败及结果不确定处理、只读保护，不自动反审核。
- 文件：backend/app/services/kingdee.py、backend/tests/kingdee_auto_audit_contract.py、backend/tests/project_archive_lifecycle_concurrency_contract.py（代码在 pms-release-readiness 工作树）；docs/金蝶第三方应用对接SOP.md、docs/releases/PMS金蝶自动审核发布验收记录-20260914.md。
- 验证：15 项自动审核及认证、查询、生命周期、并发、操作日志回归通过；测试账套新增自动审核及已审核更新通过。正式程序已发布并健康；生产样本因 I0001 修改页占用导致 Save 拒绝，日志 33/74 正确记失败，尚未补审。已恢复普通只读 PID 6444，保存/提交/审核联网前拒绝验证通过。
- 状态：SOP 4.0 已记录当前结果与待处理占用；未推送、未合并。

## 2026-09-14 金蝶生产样本补审收口

- 原因：用户已正常退出造成修改冲突的金蝶资料页面。
- 调整：只读回查后重新开启限定样本窗口，正常 PMS 同步执行 Save、Submit、Audit 各一次；更新 SOP 4.0 与自动审核验收记录最终结果，保留第一次失败证据。
- 涉及文件：docs/金蝶第三方应用对接SOP.md、docs/releases/PMS金蝶自动审核发布验收记录-20260914.md。
- 验证：14:00:16 生产样本为已审核 C、内码和名称一致，新增同步日志 34、操作日志 75 均成功；普通只读 PID 6576 健康，临时进程已停止，联网前只读拒绝通过。未推送、未合并。

## 2026-09-14 金蝶单据占用提示

- 原因：用户要求占用失败时明确说明“金蝶端退出当前单据后再次点击同步”。
- 调整：保存/提交/审核的占用错误返回明确处理指引并保留脱敏占用人/原因，批量摘要也显示；保留失败日志，不自动重试、强制解锁或反审核。SOP 升级 4.1。
- 文件：backend/app/services/kingdee.py、backend/tests/kingdee_occupancy_contract.py（修复工作树），docs/金蝶第三方应用对接SOP.md、docs/releases/PMS金蝶单据占用提示发布记录-20260914.md。
- 验证：占用 5 项、审核 15 项、认证 13 项、查询 4 项及并发服务器契约通过；误报反例及批量提示 RED→GREEN，独立复核通过；正式普通只读 PID 4796 健康、配置哈希不变、联网前只读拒绝通过。
- 发布：备份 C:\backup\PMS-occupancy-20260914-140712；未推送、未合并。

## 2026-09-14 开放金蝶生产日常同步

- 原因：用户明确确认正式开放日常同步。
- 调整：仅生产 C:\PMS\backend\.env.local 的 K3_READ_ONLY 从 true 改为 false，继续生产账套 2023121101 / 65771a661f427f、I0001；保留自动审核、占用提示及现有权限。配置备份留在管理员受限目录。
- 涉及文件：服务器 backend/.env.local（受保护、不入 Git）、docs/金蝶第三方应用对接SOP.md、docs/releases/PMS金蝶正式日常同步启用记录-20260914.md。
- 验证：只有开关发生变化、配置 ACL 保留，无环境覆盖；普通服务 PID 3100 启动、端口及后端/nginx 健康通过；新配置签名查询成功，样本仍 C。此次无生产业务写入。本地测试配置不变，未推送、未合并。

## 2026-09-14 - 本地最新 PMS 运行版本接续金蝶联调

- 原因：用户确认并行 UI 任务阶段完成，要求继续金蝶对接。
- 调整内容：通过现有启动器从项目根目录运行 master@ce0ef8f 的前后端，继续使用已有测试账套应用配置和只读保护；更新 SOP 最新状态与运行记录。没有修改程序代码或凭据配置。
- 涉及文件：docs/金蝶第三方应用对接SOP.md、change.md；运行状态与证据位于 Git 忽略的 .runtime/ 目录。
- 验证结果：启动器确认 8000/5174 源码归属正确；健康接口 HTTP 200；匿名金蝶连接测试 HTTP 403，保留登录校验；真实金蝶只读脚本退出 0，13 项专项契约通过。PMS 受权连接接口等待用户登录后验收。
- 范围：未连接、部署或重启正式服务器，未执行业务保存；本次未提交、推送或合并。

## 2026-09-14 发布工具修复与记录整合

- 原因：合并前检查发现旧前端与新提交错配、构建输入漏检、相对输出目录失败。
- 调整：取消 --skip-build，从确定提交的临时目录重建前端，完整检查 frontend、发布脚本和说明；后端与说明从同一提交读取，输出目录转为绝对路径。
- 调整：发布契约采用独立 Git 测试目录，验证构建来源、未提交输入、相对路径、内容排除与校验；整合两段独有项目日志，归档旧文档并明确当前正式同步已开放。
- 文件：build-server-release.command、backend/tests/server_release_contract.py、docs/PMS服务器发布与回退说明.md、历史记录、自动审核计划与 change.md。
- 验证：6 项发布契约先失败后通过，diff 检查通过；提交后再验收真实候选包。当前只完成本地工具和文档调整，未推送、未合并、未操作生产。

## 2026-09-14 确定提交的候选包验证

- 已完成：程序本地提交 327c2d8，从该提交实际构建生成 pms-server-327c2d83-20260914.zip。
- 验证：真实构建退出 0；ZIP 完整性、外部 SHA256、221 个内部哈希、全部后端和部署说明的 Git 内容一致性通过；真实配置和数据库排除通过。
- 涉及文件：docs/releases/PMS发布工具修复与候选包验收-20260914.md、合并前检查报告、SOP 与 change.md；候选包和原始证据在忽略目录。
- SHA256：c9c181eced3dac36c099094477ae9bf63ffcf41bb843dbe341ffe5eb90c36d1a。未推送、未合并、未操作生产服务。

## 2026-09-14 主分支推送合并与交接记录

- 原因：用户确认继续执行今日已验证改动的统一推送与合并。
- 调整：发布分支推送至 GitHub，根目录 master 从 ce0ef8f 快进至 309d760 并推送；保留文档备份 stash 和在用临时工作区。SOP 更新至 5.1，补充运行来源差异及后续切换范围。
- 涉及文件：docs/金蝶第三方应用对接SOP.md、docs/releases/PMS发布工具修复与候选包验收-20260914.md、docs/releases/PMS主分支合并与运行版本交接-20260914.md、change.md。
- 验证：合并无冲突，文件树与已验证分支一致；远端两分支均为 309d760。15:23 启动器确认本地后端仍使用临时工作区，故未清理；本轮未部署或重启生产。

## 2026-09-14 Mac、服务器和 GitHub 版本统一

- 原因：用户明确要求三端统一版本。
- 调整：Mac 退出临时后端，根目录标准启动器接管；服务器保留旧分支及补丁备份，切换 master 并部署 e752c13 确定版本包；统一服务器源码行尾格式和 Git 索引，保留原数据库、凭据、OA 配置和生产同步开关。SOP 更新至 5.2。
- 涉及：服务器 C:\PMS 源码及 frontend/dist、本地启动状态；docs/金蝶第三方应用对接SOP.md、docs/releases/PMS三端版本统一记录-20260914.md、原主分支交接记录、change.md。
- 验证：Mac 数据库备份 integrity_check、SQL Server COPY_ONLY/CHECKSUM 备份及 VERIFYONLY 通过；包内 221 项和服务器部署文件 219 项校验通过；服务健康、用户登录、项目档案/进度读取、OA 免密入口及两环境金蝶只读查询通过，生产样本保持 C。本次未执行业务保存。

## 2026-09-14 安全配置与 Windows 运维模块

- 原因：源码仍包含数据库、JWT、OA/SSO 等环境敏感默认值，服务器开机启动、健康检查和日志轮转尚无可重复执行的标准模块。
- 调整：移除敏感及公司环境默认值，OA JSP 改为从 Java 运行环境读取共享密钥，两个旧 Windows 批处理改为标准运维入口；新增开发/生产运行模式和启动前配置校验；本地启动器使用项目根目录受保护配置并按需生成本机 JWT 密钥；新增 Windows 启停、进程归属校验、健康检查、日志轮转、计划任务注册及部署前备份模块，默认不自动重启。
- 调整：发布包纳入安全配置模板和 Windows 运维模块；服务器发布说明补充配置检查、计划任务与重启验收边界。
- 涉及文件：`.gitignore`、`backend/app/core/config.py`、`backend/main.py`、`backend/scripts/check_runtime_config.py`、`backend/scripts/prepare_local_config.py`、`backend/.env.example`、`OA对接/pms_sso.jsp`、`start-pms.command`、`pms-auto-start.bat`、`start-services.bat`、`ops/windows/`、`build-server-release.command`、相关契约测试、`docs/PMS服务器发布与回退说明.md`、`docs/PMS安全配置与Windows运维实施说明.md`、`change.md`。
- 验证：全部后端契约脚本、全部前端契约脚本和前端生产构建通过；安全配置检查确认生产模式缺项会返回失败，当前业务源码中已知敏感明文扫描为 0。正式服务器配置迁移、计划任务安装、重启验收和密钥轮换尚未执行，需连接服务器后的独立维护窗口。

## 2026-09-15 Windows 生产配置预检脚本修复

- 原因：服务器部署前使用新版 Windows 运维入口执行生产配置预检时，`check_runtime_config.py` 作为独立脚本运行无法定位同级 `app` 包，导致运维启动流程在停止旧服务前即失败。
- 调整：配置检查脚本根据自身绝对路径加入后端根目录后再加载应用配置；新增独立进程契约，主动清除外部 `PYTHONPATH`，确保测试覆盖 Windows 运维脚本的真实调用方式。
- 涉及文件：`backend/scripts/check_runtime_config.py`、`backend/tests/runtime_security_contract.py`、`change.md`。
- 验证：新增契约先复现 `ModuleNotFoundError`，修复后运行时安全契约和 Windows 运维契约均通过。服务器旧生产服务保持运行，尚未部署本修复或重启服务。

## 2026-09-15 PMS 数据库迁移连接兼容修复

- 原因：PMS 数据库从 `10.10.1.149` 还原到 `10.10.1.230` 后，ODBC Driver 18 使用默认强制加密，在目标 SQL Server 登录前 TLS 握手阶段被断开；同机只读预检使用非加密连接可以正常访问目标库。
- 调整：PMS 的 `pyodbc` 连接串显式设置 `Encrypt=no`，同时保留 `TrustServerCertificate=yes`，消除不同 SQL Server TLS 配置导致的驱动默认行为漂移。
- 涉及文件：`backend/app/core/config.py`、`backend/tests/config_contract.py`。
- 验证：配置契约新增 ODBC 加密参数断言，先失败后修复；运行时安全契约和 Windows 运维契约通过。生产切换工具最初误用 `/health`，而实际健康地址为 `/api/health`，造成目标启动和回退均被误判超时；修正后切换成功。
- 发布：服务器 `10.10.1.228` 已备份原配置并切换到 `10.10.1.230 / PMS`，使用独立运行账号 `pms_app_runtime`；后端新进程 PID 5816。服务器内部直连返回 PMS 数据库和 17 张表，启动日志确认应用完成启动；外部主页与 `/api/health` 均为 HTTP 200，匿名 `/api/auth/me` 为预期 HTTP 403。未执行 ERP 同步或其他业务写入。
- 验收：用户已通过 OA 完成登录，并确认项目档案、项目进度读取正常。启动日志仍有被程序捕获的既存 `bcrypt` 版本兼容警告，但未影响本次登录和业务只读验收，后续可作为独立依赖维护项处理。
- 安全例外：`10.10.1.230` 由信息安全管理员维护，当前无法进入服务器配置 SQL Server TLS。用户已知悉 `Encrypt=no` 会使数据库查询及返回数据不使用 TLS，并明确同意先合并当前内网兼容方案；后续由信息安全管理员为 SQL Server 配置专用证书后，恢复 `Encrypt=yes`、启用证书校验并重新完成连接与业务验收。
- 回退：切换前环境配置备份位于 `C:\ProgramData\PMS-security-rollout-20260915\env-before-db-cutover-20260915-153420.bak`；原数据库 `10.10.1.149` 未改动，可按该备份恢复。

## 2026-09-15 - 调整 PMS 到金蝶销售项目字段映射

- 原因：业务确认金蝶销售项目需要按“PMS 编码到金蝶编码、PMS 编码到金蝶名称、PMS 名称到金蝶备注”维护，旧实现把 PMS 名称写入了金蝶名称。
- 调整内容：金蝶保存时将 `project_code` 同时写入 `FNumber`、`FDataValue`，将 `project_name` 写入 `FDescription`；查询及自动提交审核回查同步校验编码、名称和备注。历史资料仍按 `FNumber` 定位，更新不改变金蝶内码和编码。
- 涉及文件：`backend/app/services/kingdee.py`、`backend/tests/kingdee_app_auth_contract.py`、`backend/tests/kingdee_auto_audit_contract.py`、`backend/tests/kingdee_query_scope_contract.py`、`docs/金蝶第三方应用对接SOP.md`、`docs/金蝶云星空接口对接方案.md`、`change.md`。
- 验证结果：新增映射契约先在旧实现失败，修改后认证/保存、自动审核、查询范围和占用提示专项测试通过；未对生产金蝶执行保存、提交或审核，未部署生产服务器。

## 2026-09-16 Windows 运维备份路径修复

- 原因：正式服务器执行发布前备份时，Nginx 配置使用相对路径 `conf\\nginx.conf`，旧脚本按当前目录查找而失败。
- 调整：相对 Nginx 配置路径统一基于 `nginxRoot` 解析，绝对路径保持原行为。
- 涉及文件：`ops/windows/Export-PmsOperationsBackup.ps1`、`backend/tests/windows_operations_contract.py`、`change.md`。
- 验证：新增相对路径契约先失败后通过；Windows 运维契约和发布包构建需在当前提交上重新验证。

## 2026-09-16 PMS 正式服务器版本更新与运维任务启用

- 原因：将已合并的金蝶项目字段映射和 Windows 运维备份修复部署到正式 PMS 服务器，并补齐开机启动、健康检查和日志轮转任务。
- 调整：服务器 `10.10.1.228` 的 `C:\PMS` 从 `c3b8521` 快进到 `a8daf45`；部署同一提交构建的前端文件；注册 `PMS-Startup`、`PMS-Health`、`PMS-Log-Rotation` 三项计划任务，健康检查仍保持 `autoRestart=false`，不自动重启服务。
- 备份：部署前运行标准运维备份，备份保存在 `C:\ProgramData\PMS-release-20260916\backups`；发布包和离线 Git bundle 保存在 `C:\ProgramData\PMS-release-20260916`。
- 验证：服务器仓库为 `master@a8daf45` 且工作区干净；前端 160 个目录、160 个文件镜像复制成功且无失败；主页和 `/api/health` 返回 HTTP 200，匿名 `/api/auth/me` 返回预期 HTTP 403；三项计划任务均为 `Ready`。
- 安全盘点：只读确认生产运行账号 `pms_app_runtime` 当前属于 `db_owner`，另有显式 `CONNECT`；本次未修改数据库权限。后续需先拆分数据库结构升级账号与日常运行账号，再收紧运行权限，避免影响当前自动建表和字段升级流程。
- 业务边界：本次未执行金蝶保存、提交、审核或其他业务写入，未重启 Windows 或 SQL Server 服务。

## 2026-09-16 数据库升级与日常运行权限拆分（开发机）

- 原因：正式 PMS 运行账号仍有 `db_owner`，因为后端生产启动会自动建表并运行升级；需先拆分升级和日常启动，再安全收紧数据库权限。
- 调整：新增数据库版本表及就绪检查；生产启动只读检查版本，独立 `upgrade_database.py` 在受控发布时运行原初始化和迁移，失败保持未就绪；开发模式仍自动初始化。更新运维说明和项目开发规则，明确备份、升级、运行身份分离及版本提升要求。
- 涉及文件：`backend/app/models/database_revision.py`、`backend/app/services/database_revision.py`、`backend/app/models/init_db.py`、`backend/scripts/upgrade_database.py`、`backend/main.py`、`backend/tests/database_upgrade_contract.py`、`ops/windows/README.md`、`docs/PMS服务器发布与回退说明.md`、`docs/superpowers/plans/2026-09-16-db-permission-split.md`、`AGENTS.md`、`change.md`。
- 验证：Python 3.11 临时环境运行全部 25 项后端契约均通过；新增测试覆盖重复升级、缺失/旧版本、失败升级以及生产启动不调用初始化；`git diff --check` 通过。
- 边界：仅在开发机功能分支实现，尚未推送、合并、部署；未更改正式数据库、账号权限或 PMS 服务。生产迁移及收权须在备份和单独批准的维护窗口完成。

## 2026-09-17 金蝶销售项目档案期初导入准备（开发机）

- 原因：金蝶历史项目档案需要进入 PMS，但历史备注大量为空，不能将金蝶名称或编码冒充 PMS 项目名称；冲突记录也不能静默覆盖。
- 调整：新增只读预检及受控导入脚本；仅将金蝶编码映射为 PMS 项目编号、备注映射为 PMS 项目名称，空备注保留空值。新增只读 `data_origin` 来源标识，期初记录显示“金蝶期初”，并以来源阻止删除，不伪造 PMS 已同步标志、时间或人员。后续编辑、ERP 同步和建立项目进度均要求先补齐名称。新增档案名称仍必填。版本化数据库升级允许期初名称为空，并保持非空名称唯一。
- 涉及文件：`backend/app/models/project.py`、`backend/app/models/init_db.py`、`backend/app/schemas/project.py`、`backend/app/services/database_revision.py`、`backend/app/services/field_policy.py`、`backend/app/services/project.py`、`backend/app/services/project_archive_lifecycle.py`、`backend/app/services/project_archive_initial_migration.py`、`backend/app/services/kingdee_initial_archive.py`、`backend/scripts/import_kingdee_initial_archives.py`、`backend/tests/kingdee_initial_archive_contract.py`、`frontend/src/views/project/ProjectArchive.vue`、`frontend/tests/kingdee-initial-archive-contract.test.mjs`、`docs/superpowers/plans/2026-09-17-kingdee-initial-archive.md`、`change.md`。
- 验证：期初映射、空名称、冲突隔离、来源删除保护和 SQLite 旧库升级专项契约通过；项目档案生命周期、数据库升级、字段目录、操作日志、项目更新等后端契约及前端标准列表/风格/系统 UI/档案筛选契约通过，前端构建通过。
- 边界：仅开发机代码和服务器只读异常预检。异常明细留存 PMS 服务器本地 `C:\ProgramData\PMS-release-20260916\kingdee-initial-archive-anomalies-20260917.txt`，未写入仓库。尚未执行生产备份、数据库结构升级、期初导入、部署、推送或合并；正式操作需备份验证和另行批准。

## 2026-09-17 期初异常清单整理与服务器历史目录归档

- 原因：期初异常原始报告只有英文问题码，不便核对；多批发布临时目录散落在正式服务器 C 盘根目录。
- 调整：在服务器本地生成含“序号、问题分类、关联项目编码或重复值、原始记录详情、处理建议、导入处理”六列的中文 CSV 和说明，保留原始报告副本。将 8 个未被 PMS 服务引用的历史目录移入 `C:\PMS\.runtime\release-history\20260917`。`C:\PMS - 副本` 含未入版本管理的内容；确认文件占用者是闲置 PowerShell 后正常退出该终端，再移动副本，未删除任何文件。
- 涉及文件：`ops/windows/Format-KingdeeInitialAnomalies.ps1`、`ops/windows/Archive-PmsRootArtifacts.ps1`、`ops/windows/README.md`、`change.md`；服务器本地 `C:\PMS\.runtime\initial-import\20260917`、`C:\PMS\.runtime\release-history\20260917`。业务数据未进入仓库。
- 验证：中文 CSV 与原报告均为 133 条，CSV 六列；8 个已归档目录共 18804 个文件，移动后逐项核对文件数和字节数，清单为 8 项 `Archived`、0 项待处理。服务器 `/api/health` 返回 HTTP 200，三个 PMS 计划任务仍存在。未改数据库、服务配置、正式源码或金蝶业务数据；未执行期初导入。

## 2026-09-17 金蝶重复项目编码提示修复（开发机）

- 原因：日常同步在金蝶同类别、同项目编码返回多条时虽会停止保存，但专门的重复原因被通用查询异常覆盖，页面只显示授权或连接错误。
- 调整：重复编码使用独立异常并原样传至同步结果；其他查询异常仍走现有脱敏提示。不改变查询范围、禁用状态处理或金蝶写入流程。
- 涉及文件：`backend/app/services/kingdee.py`、`backend/tests/kingdee_query_scope_contract.py`、`change.md`。
- 验证：重复提示契约先失败后通过；金蝶应用认证、自动审核和期初导入契约通过。服务器只读核验新视图销售项目 937 条、空编码 0 条、重复编码 0 组；`A-202629` 在视图和金蝶只读 API 均返回 1 条。另以 `FForbidStatus=B` 查询到禁用样本，并确认仅按该样本编码查询时 API 仍返回禁用记录；视图过滤不等于日常同步过滤。未执行期初导入或金蝶保存，代码尚未部署。

## 2026-09-17 金蝶期初视图调整后的只读异常复核

- 原因：金蝶来源视图已过滤禁用记录，原异常清单及其重复编码结论不再适用于当前来源数据。
- 调整：在 PMS 服务器 `C:\PMS\.runtime\initial-import\20260917\kingdee-initial-current-diagnostic-20260917.csv` 重新生成带中文问题标题和分类的诊断清单，未覆盖旧报告；只读检查目标 PMS 数据库的升级前置状态。
- 涉及文件：服务器本地上述诊断 CSV、`change.md`。没有改动服务器程序、数据库结构或业务记录。
- 验证：来源销售项目 937 条；按当前预检规则可作为候选 830 条，其中 643 条备注为空且按已确认规则允许期初留空；阻断异常 107 条，包含拟用名称重复 106 条（19 组）及与 PMS 现有编码冲突 1 条；项目编码重复、空编码或超长、现有名称冲突均为 0。CSV 重新读取为 107 行。当前 PMS 数据库没有 `data_origin` 列，也没有数据库版本表；正式导入仍须人工备份、升级、重新生成正式预检指纹并另行批准。

## 2026-09-17 项目档案同名放行及期初规则调整（开发机）

- 原因：业务确认项目名称可重复，项目档案以项目编码识别；旧预检将 106 条同名来源误列为异常，档案接口与数据库旧唯一索引也会阻止合法同名档案。
- 调整：取消档案名称唯一校验及模型索引，保持项目编码和序列号的原有唯一约束；SQLite/MSSQL 升级均移除旧名称唯一索引且不再重建，数据库版本提升为 `2026-09-17-02`。金蝶期初预检只隔离无效编码/长度、来源重复编码和 PMS 已有编码；不同编码同名允许，空备注不填充金蝶名称。重新生成服务器只读诊断文件 `C:\PMS\.runtime\initial-import\20260917\kingdee-initial-code-only-diagnostic-20260917.csv`，旧报告保留。
- 涉及文件：`backend/app/models/project.py`、`backend/app/services/project.py`、`backend/app/services/project_archive_initial_migration.py`、`backend/app/services/project_archive_semantic_migration.py`、`backend/app/services/kingdee_initial_archive.py`、`backend/app/services/database_revision.py`、`backend/scripts/import_kingdee_initial_archives.py`、`backend/tests/kingdee_initial_archive_contract.py`、`backend/tests/legacy_schema_upgrade_contract.py`、`backend/tests/project_archive_semantic_contract.py`、`docs/superpowers/plans/2026-09-17-kingdee-initial-archive.md`、`change.md`；服务器只读诊断 CSV。
- 验证：同名预检、接口校验和旧 SQLite 唯一索引迁移测试先失败后通过；SQL Server 升级路径断言仅删除旧名称索引；全部 26 项后端契约、前端档案筛选/风格/标准列表/系统 UI/金蝶期初契约和 `npm run build` 通过。新视图 937 条，候选 936 条（其中空名称 643 条），只剩测试编码冲突 1 条，CSV 重新读取为 1 行。服务器正式库仍有名称唯一索引；`msdb` 最近可见完整备份为 2026-09-15 13:41，未记录备份校验和，不能作为本次升级/导入的已验证备份。未部署、升级或导入。

## 2026-09-17 金蝶期初正式导入结果

- 原因：执行用户已批准并完成备份的期初导入，不重复请求批准。
- 调整：现场辅助脚本改用 SQLAlchemy URL 构造数据库连接，连接错误仅输出 SQLSTATE/原生错误码，区分权限检查失败，不输出密码。用户再次输入后连接成功；不能据此断定上次具体失败原因。服务器部署 `d300b413`，完成版本化数据库升级和期初写入。
- 涉及文件：服务器 `C:\PMS\.runtime\initial-import\20260917\release-d300b413\apply.py`、本地同批辅助脚本、上述提交中的生产文件及数据库；本计划与变更日志。
- 验证：现场依次返回 `database-upgrade_OK`、`runtime-schema_OK`、`initial-import_OK`、`import-verification_OK`。验证脚本确认新增 936 条、643 条名称为空、跳过测试编码 `PMS-ACCEPT-20260914-001`；原有 4 条档案未变、总档案 940 条、项目进度和 ERP 日志数量未变，936 条导入日志齐全，所有导入名称与金蝶备注一致。Mac 独立访问生产 `/api/health` 返回 HTTP 200/status ok，生产首页资源引用与本地本批发布首页一致。
- 边界：远程窗口最后可见输出停在导入核验成功，最终发布收据和 Git 清洁状态尚未读取；不因此重复导入。未推送 GitHub、未合并 master，未重启 SQL Server 或 OA。

## 2026-09-17 金蝶期初发布包与正式导入准备

- 原因：用户已确认生产升级和期初导入，允许不同编码同名并跳过唯一测试编码冲突。
- 调整：本地提交 `d300b413`，从该提交重新构建服务器发布包；包与源码增量传至服务器 `C:\PMS\.runtime\initial-import\20260917\release-d300b413`。形成正式预检报告、旧档案哈希核对记录和可连续执行升级/导入/核验的现场辅助脚本；辅助脚本名称避开 Python 标准库 `inspect`，不保存管理员密码。临时传输服务完成传输后关闭。
- 备份：确认用户提供的 `E:\PMS-bakup\PMS20260917.bak` 是 2026-09-17 16:49:11 的 PMS 完整备份，无校验和；另生成 `E:\PMS-bakup\PMS-preimport-20260917-173045.bak`（COPY_ONLY、CHECKSUM），SQL Server 返回备份及带校验和验证成功。程序备份在 `C:\PMS\.runtime\release-history\20260917\pre-initial-import\PMS-operations-20260917-174625`。
- 涉及文件：本地 `release/pms-server-d300b413-20260917.zip`、`.runtime/initial-import-release-20260917/`；服务器上述发布准备目录；`docs/superpowers/plans/2026-09-17-kingdee-initial-archive.md`、`change.md`。
- 验证：发布包构建和 ZIP 完整性检查通过，SHA256 为 `59a0d2a2942a0da7d23a4110db58db82cd69ebcb3bda1fe67bf72a3dd445dd31`；服务器增量源码和发布文件哈希检查通过。正式预检 937 条来源、936 条待导入、643 条空名称、1 条既有测试编码冲突；原有 4 条档案、1 条项目进度，操作人为已存在的 OA 用户 ID 2。现场输出 `STAGE_READY`。截至本记录，执行流程仍等待管理员密码，尚未停止服务、升级结构或写入期初档案；未推送 GitHub、未合并 master。
